/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * BBZ App Backend API
 * API documentation for BBZ App Backend
 * OpenAPI spec version: 1.0.0
 */
import type { Arguments, Key, SWRConfiguration } from 'swr'
import useSwr from 'swr'

import type { SWRMutationConfiguration } from 'swr/mutation'
import useSWRMutation from 'swr/mutation'

import type {
  CreateSpace201,
  CreateSpace400,
  CreateSpace401,
  CreateSpace403,
  CreateSpace409,
  CreateSpace422,
  CreateSpace500,
  CreateSpaceBody,
  DeleteSpace200,
  DeleteSpace400,
  DeleteSpace401,
  DeleteSpace403,
  DeleteSpace404,
  DeleteSpace422,
  DeleteSpace500,
  GetSpace200,
  GetSpace400,
  GetSpace401,
  GetSpace403,
  GetSpace404,
  GetSpace422,
  GetSpace500,
  ListSpaces200,
  ListSpaces400,
  ListSpaces401,
  ListSpaces403,
  ListSpaces422,
  ListSpaces500,
  ListSpacesParams,
  OpenDoor200,
  OpenDoor400,
  OpenDoor401,
  OpenDoor403,
  OpenDoor404,
  OpenDoor422,
  OpenDoor500,
  OpenDoorParams,
  SpaceQrcode200,
  SpaceQrcode400,
  SpaceQrcode401,
  SpaceQrcode403,
  SpaceQrcode404,
  SpaceQrcode422,
  SpaceQrcode500,
  UpdateSpace200,
  UpdateSpace400,
  UpdateSpace401,
  UpdateSpace403,
  UpdateSpace404,
  UpdateSpace409,
  UpdateSpace422,
  UpdateSpace500,
  UpdateSpaceBody,
} from '../bBZAppBackendAPI.schemas'

import { customFetch } from '../../mutator/custom-fetch'

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1]

/**
 * Este endpoint permite criar um novo espaço no sistema com as seguintes características:

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Restrito a usuários com perfil 'admin' ou 'dev'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Processo de criação**:
  1. Cria o registro de espaço com os dados fornecidos
  2. Retorna o espaço criado com seus dados completos
* **Resposta**: Retorna o espaço criado com seu ID e status

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserRole`: Restringe acesso aos perfis 'admin' e 'dev'
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Criar um novo espaço
 */
export type createSpaceResponse201 = {
  data: CreateSpace201
  status: 201
}

export type createSpaceResponse400 = {
  data: CreateSpace400
  status: 400
}

export type createSpaceResponse401 = {
  data: CreateSpace401
  status: 401
}

export type createSpaceResponse403 = {
  data: CreateSpace403
  status: 403
}

export type createSpaceResponse409 = {
  data: CreateSpace409
  status: 409
}

export type createSpaceResponse422 = {
  data: CreateSpace422
  status: 422
}

export type createSpaceResponse500 = {
  data: CreateSpace500
  status: 500
}

export type createSpaceResponseSuccess = createSpaceResponse201 & {
  headers: Headers
}
export type createSpaceResponseError = (
  | createSpaceResponse400
  | createSpaceResponse401
  | createSpaceResponse403
  | createSpaceResponse409
  | createSpaceResponse422
  | createSpaceResponse500
) & {
  headers: Headers
}

export const getCreateSpaceUrl = () => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space`
}

export const createSpace = async (
  createSpaceBody: CreateSpaceBody,
  options?: RequestInit,
): Promise<createSpaceResponseSuccess> => {
  return customFetch<createSpaceResponseSuccess>(getCreateSpaceUrl(), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createSpaceBody),
  })
}

export const getCreateSpaceMutationFetcher = (
  options?: SecondParameter<typeof customFetch>,
) => {
  return (_: Key, { arg }: { arg: CreateSpaceBody }) => {
    return createSpace(arg, options)
  }
}
export const getCreateSpaceMutationKey = () =>
  [`${process.env.NEXT_PUBLIC_API_URL}/v1/private/space`] as const

export type CreateSpaceMutationResult = NonNullable<
  Awaited<ReturnType<typeof createSpace>>
>
export type CreateSpaceMutationError =
  | CreateSpace400
  | CreateSpace401
  | CreateSpace403
  | CreateSpace409
  | CreateSpace422
  | CreateSpace500

/**
 * @summary Criar um novo espaço
 */
export const useCreateSpace = <
  TError =
    | CreateSpace400
    | CreateSpace401
    | CreateSpace403
    | CreateSpace409
    | CreateSpace422
    | CreateSpace500,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof createSpace>>,
    TError,
    Key,
    CreateSpaceBody,
    Awaited<ReturnType<typeof createSpace>>
  > & { swrKey?: string }
  request?: SecondParameter<typeof customFetch>
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getCreateSpaceMutationKey()
  const swrFn = getCreateSpaceMutationFetcher(requestOptions)

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite listar espaços com filtros e paginação.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Acessível a usuários com perfil 'admin', 'dev' ou 'user'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Recursos de busca**:
  1. Filtragem por termo de busca (name e description)
  2. Filtragem por status de ativação (isActive)
  3. Filtragem por faixa de capacidade
  4. Paginação com controle de página atual e tamanho da página
* **Resposta**: Retorna lista de espaços paginada com metadados de paginação

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserRole`: Permite acesso aos perfis 'admin', 'dev' e 'user'
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Listar espaços com filtros e paginação
 */
export type listSpacesResponse200 = {
  data: ListSpaces200
  status: 200
}

export type listSpacesResponse400 = {
  data: ListSpaces400
  status: 400
}

export type listSpacesResponse401 = {
  data: ListSpaces401
  status: 401
}

export type listSpacesResponse403 = {
  data: ListSpaces403
  status: 403
}

export type listSpacesResponse422 = {
  data: ListSpaces422
  status: 422
}

export type listSpacesResponse500 = {
  data: ListSpaces500
  status: 500
}

export type listSpacesResponseSuccess = listSpacesResponse200 & {
  headers: Headers
}
export type listSpacesResponseError = (
  | listSpacesResponse400
  | listSpacesResponse401
  | listSpacesResponse403
  | listSpacesResponse422
  | listSpacesResponse500
) & {
  headers: Headers
}

export const getListSpacesUrl = (params?: ListSpacesParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space?${stringifiedParams}`
    : `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space`
}

export const listSpaces = async (
  params?: ListSpacesParams,
  options?: RequestInit,
): Promise<listSpacesResponseSuccess> => {
  return customFetch<listSpacesResponseSuccess>(getListSpacesUrl(params), {
    ...options,
    method: 'GET',
  })
}

export const getListSpacesKey = (params?: ListSpacesParams) =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space`,
    ...(params ? [params] : []),
  ] as const

export type ListSpacesQueryResult = NonNullable<
  Awaited<ReturnType<typeof listSpaces>>
>
export type ListSpacesQueryError =
  ListSpaces400 | ListSpaces401 | ListSpaces403 | ListSpaces422 | ListSpaces500

/**
 * @summary Listar espaços com filtros e paginação
 */
export const useListSpaces = <
  TError =
    | ListSpaces400
    | ListSpaces401
    | ListSpaces403
    | ListSpaces422
    | ListSpaces500,
>(
  params?: ListSpacesParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof listSpaces>>, TError> & {
      swrKey?: Key
      enabled?: boolean
    }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const isEnabled = swrOptions?.enabled !== false
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getListSpacesKey(params) : null))
  const swrFn = () => listSpaces(params, requestOptions)

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  )

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite excluir um espaço específico permanentemente.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Restrito a usuários com perfil 'admin' ou 'dev'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Processo**:
  1. Valida o ID do espaço a ser excluído
  2. Verifica se o espaço existe no banco de dados
  3. Exclui permanentemente o espaço (hard delete)
  4. Retorna mensagem de confirmação

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserRole`: Restringe acesso aos perfis 'admin' e 'dev'
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Excluir um espaço
 */
export type deleteSpaceResponse200 = {
  data: DeleteSpace200
  status: 200
}

export type deleteSpaceResponse400 = {
  data: DeleteSpace400
  status: 400
}

export type deleteSpaceResponse401 = {
  data: DeleteSpace401
  status: 401
}

export type deleteSpaceResponse403 = {
  data: DeleteSpace403
  status: 403
}

export type deleteSpaceResponse404 = {
  data: DeleteSpace404
  status: 404
}

export type deleteSpaceResponse422 = {
  data: DeleteSpace422
  status: 422
}

export type deleteSpaceResponse500 = {
  data: DeleteSpace500
  status: 500
}

export type deleteSpaceResponseSuccess = deleteSpaceResponse200 & {
  headers: Headers
}
export type deleteSpaceResponseError = (
  | deleteSpaceResponse400
  | deleteSpaceResponse401
  | deleteSpaceResponse403
  | deleteSpaceResponse404
  | deleteSpaceResponse422
  | deleteSpaceResponse500
) & {
  headers: Headers
}

export const getDeleteSpaceUrl = (id: string) => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${id}`
}

export const deleteSpace = async (
  id: string,
  options?: RequestInit,
): Promise<deleteSpaceResponseSuccess> => {
  return customFetch<deleteSpaceResponseSuccess>(getDeleteSpaceUrl(id), {
    ...options,
    method: 'DELETE',
  })
}

export const getDeleteSpaceMutationFetcher = (
  id: string,
  options?: SecondParameter<typeof customFetch>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return deleteSpace(id, options)
  }
}
export const getDeleteSpaceMutationKey = (id: string) =>
  [`${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${id}`] as const

export type DeleteSpaceMutationResult = NonNullable<
  Awaited<ReturnType<typeof deleteSpace>>
>
export type DeleteSpaceMutationError =
  | DeleteSpace400
  | DeleteSpace401
  | DeleteSpace403
  | DeleteSpace404
  | DeleteSpace422
  | DeleteSpace500

/**
 * @summary Excluir um espaço
 */
export const useDeleteSpace = <
  TError =
    | DeleteSpace400
    | DeleteSpace401
    | DeleteSpace403
    | DeleteSpace404
    | DeleteSpace422
    | DeleteSpace500,
>(
  id: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof deleteSpace>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof deleteSpace>>
    > & { swrKey?: string }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getDeleteSpaceMutationKey(id)
  const swrFn = getDeleteSpaceMutationFetcher(id, requestOptions)

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite obter os detalhes completos de um espaço específico.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Acessível a usuários com perfil 'admin', 'dev' ou 'user'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Processo**:
  1. Valida o ID do espaço a ser consultado
  2. Verifica se o espaço existe no banco de dados
  3. Retorna os detalhes completos do espaço

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserRole`: Permite acesso aos perfis 'admin', 'dev' e 'user'
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Obter detalhes de um espaço
 */
export type getSpaceResponse200 = {
  data: GetSpace200
  status: 200
}

export type getSpaceResponse400 = {
  data: GetSpace400
  status: 400
}

export type getSpaceResponse401 = {
  data: GetSpace401
  status: 401
}

export type getSpaceResponse403 = {
  data: GetSpace403
  status: 403
}

export type getSpaceResponse404 = {
  data: GetSpace404
  status: 404
}

export type getSpaceResponse422 = {
  data: GetSpace422
  status: 422
}

export type getSpaceResponse500 = {
  data: GetSpace500
  status: 500
}

export type getSpaceResponseSuccess = getSpaceResponse200 & {
  headers: Headers
}
export type getSpaceResponseError = (
  | getSpaceResponse400
  | getSpaceResponse401
  | getSpaceResponse403
  | getSpaceResponse404
  | getSpaceResponse422
  | getSpaceResponse500
) & {
  headers: Headers
}

export const getGetSpaceUrl = (id: string) => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${id}`
}

export const getSpace = async (
  id: string,
  options?: RequestInit,
): Promise<getSpaceResponseSuccess> => {
  return customFetch<getSpaceResponseSuccess>(getGetSpaceUrl(id), {
    ...options,
    method: 'GET',
  })
}

export const getGetSpaceKey = (id: string) =>
  [`${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${id}`] as const

export type GetSpaceQueryResult = NonNullable<
  Awaited<ReturnType<typeof getSpace>>
>
export type GetSpaceQueryError =
  | GetSpace400
  | GetSpace401
  | GetSpace403
  | GetSpace404
  | GetSpace422
  | GetSpace500

/**
 * @summary Obter detalhes de um espaço
 */
export const useGetSpace = <
  TError =
    | GetSpace400
    | GetSpace401
    | GetSpace403
    | GetSpace404
    | GetSpace422
    | GetSpace500,
>(
  id: string,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof getSpace>>, TError> & {
      swrKey?: Key
      enabled?: boolean
    }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const isEnabled = swrOptions?.enabled !== false && !!id
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getGetSpaceKey(id) : null))
  const swrFn = () => getSpace(id, requestOptions)

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  )

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite atualizar parcialmente os dados de um espaço específico por meio de um PATCH.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Restrito a usuários com perfil 'admin' ou 'dev'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Atualização parcial**: Implementa o conceito de PATCH, atualizando apenas os campos enviados na requisição.
* **Processo**:
  1. Valida o ID do espaço a ser atualizado
  2. Verifica se o espaço existe no banco de dados
  3. Aplica apenas as alterações enviadas no corpo da requisição
  4. Retorna o espaço com os dados atualizados

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserRole`: Restringe acesso aos perfis 'admin' e 'dev'
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Atualizar dados parciais de um espaço
 */
export type updateSpaceResponse200 = {
  data: UpdateSpace200
  status: 200
}

export type updateSpaceResponse400 = {
  data: UpdateSpace400
  status: 400
}

export type updateSpaceResponse401 = {
  data: UpdateSpace401
  status: 401
}

export type updateSpaceResponse403 = {
  data: UpdateSpace403
  status: 403
}

export type updateSpaceResponse404 = {
  data: UpdateSpace404
  status: 404
}

export type updateSpaceResponse409 = {
  data: UpdateSpace409
  status: 409
}

export type updateSpaceResponse422 = {
  data: UpdateSpace422
  status: 422
}

export type updateSpaceResponse500 = {
  data: UpdateSpace500
  status: 500
}

export type updateSpaceResponseSuccess = updateSpaceResponse200 & {
  headers: Headers
}
export type updateSpaceResponseError = (
  | updateSpaceResponse400
  | updateSpaceResponse401
  | updateSpaceResponse403
  | updateSpaceResponse404
  | updateSpaceResponse409
  | updateSpaceResponse422
  | updateSpaceResponse500
) & {
  headers: Headers
}

export const getUpdateSpaceUrl = (id: string) => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${id}`
}

export const updateSpace = async (
  id: string,
  updateSpaceBody: UpdateSpaceBody,
  options?: RequestInit,
): Promise<updateSpaceResponseSuccess> => {
  return customFetch<updateSpaceResponseSuccess>(getUpdateSpaceUrl(id), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(updateSpaceBody),
  })
}

export const getUpdateSpaceMutationFetcher = (
  id: string,
  options?: SecondParameter<typeof customFetch>,
) => {
  return (_: Key, { arg }: { arg: UpdateSpaceBody }) => {
    return updateSpace(id, arg, options)
  }
}
export const getUpdateSpaceMutationKey = (id: string) =>
  [`${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${id}`] as const

export type UpdateSpaceMutationResult = NonNullable<
  Awaited<ReturnType<typeof updateSpace>>
>
export type UpdateSpaceMutationError =
  | UpdateSpace400
  | UpdateSpace401
  | UpdateSpace403
  | UpdateSpace404
  | UpdateSpace409
  | UpdateSpace422
  | UpdateSpace500

/**
 * @summary Atualizar dados parciais de um espaço
 */
export const useUpdateSpace = <
  TError =
    | UpdateSpace400
    | UpdateSpace401
    | UpdateSpace403
    | UpdateSpace404
    | UpdateSpace409
    | UpdateSpace422
    | UpdateSpace500,
>(
  id: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof updateSpace>>,
      TError,
      Key,
      UpdateSpaceBody,
      Awaited<ReturnType<typeof updateSpace>>
    > & { swrKey?: string }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getUpdateSpaceMutationKey(id)
  const swrFn = getUpdateSpaceMutationFetcher(id, requestOptions)

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite gerar um código temporário para abrir a porta de um espaço específico, utilizando integração com a API DLOCK.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Acessível a usuários com perfil 'admin', 'dev' ou 'user' (com validação adicional para usuários comuns).
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Parâmetros de consulta**:
  - `spaceName` (obrigatório): Nome do espaço para abrir a porta
  - `reservationId` (opcional): ID da reserva relacionada (obrigatório para usuários comuns)
* **Processo**:
  1. Valida o nome do espaço a ser aberto e o ID da reserva (quando aplicável)
  2. Verifica a role do usuário autenticado:
     - Para usuários 'admin' ou 'dev': acesso liberado sem verificações adicionais
     - Para usuários comuns ('user'): verifica a existência da reserva e do check-in
  3. Para usuários comuns, valida:
     - Se o usuário é proprietário ou convidado da reserva informada
     - Se existe um check-in ativo para esta reserva
     - Se não houver check-in ou o usuário não estiver associado à reserva, não gera o código
  4. Verifica se o espaço existe no banco de dados
  5. Verifica se existe uma conta DLOCK cadastrada no sistema
     - Caso não exista, cria uma nova conta usando as credenciais armazenadas no ambiente
     - Verifica se o usuário com o email configurado para DLOCK existe no sistema
  6. Gerencia o token de acesso à API DLOCK automaticamente:
     - Verifica se o token está próximo de expirar (menos de 30 dias)
     - Realiza refresh automático do token quando necessário
     - Atualiza os dados da conta no banco de dados
  7. Consulta a API DLOCK para obter a lista de fechaduras cadastradas
  8. Identifica a fechadura correspondente ao espaço solicitado através do nome normalizado
  9. Gera um código temporário de acesso através da API DLOCK:
     - Se a solicitação ocorrer antes dos últimos 10 minutos da hora, gera um código válido por 1 hora
     - Se a solicitação ocorrer nos últimos 10 minutos da hora, gera um código válido por 2 horas
  10. Formata a data de expiração no fuso horário de São Paulo
  11. Retorna o código de acesso temporário e informações de expiração

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Gerar código para abertura de porta de um espaço
 */
export type openDoorResponse200 = {
  data: OpenDoor200
  status: 200
}

export type openDoorResponse400 = {
  data: OpenDoor400
  status: 400
}

export type openDoorResponse401 = {
  data: OpenDoor401
  status: 401
}

export type openDoorResponse403 = {
  data: OpenDoor403
  status: 403
}

export type openDoorResponse404 = {
  data: OpenDoor404
  status: 404
}

export type openDoorResponse422 = {
  data: OpenDoor422
  status: 422
}

export type openDoorResponse500 = {
  data: OpenDoor500
  status: 500
}

export type openDoorResponseSuccess = openDoorResponse200 & {
  headers: Headers
}
export type openDoorResponseError = (
  | openDoorResponse400
  | openDoorResponse401
  | openDoorResponse403
  | openDoorResponse404
  | openDoorResponse422
  | openDoorResponse500
) & {
  headers: Headers
}

export const getOpenDoorUrl = (params: OpenDoorParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/open-door?${stringifiedParams}`
    : `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/open-door`
}

export const openDoor = async (
  params: OpenDoorParams,
  options?: RequestInit,
): Promise<openDoorResponseSuccess> => {
  return customFetch<openDoorResponseSuccess>(getOpenDoorUrl(params), {
    ...options,
    method: 'POST',
  })
}

export const getOpenDoorMutationFetcher = (
  params: OpenDoorParams,
  options?: SecondParameter<typeof customFetch>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return openDoor(params, options)
  }
}
export const getOpenDoorMutationKey = (params: OpenDoorParams) =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/open-door`,
    ...(params ? [params] : []),
  ] as const

export type OpenDoorMutationResult = NonNullable<
  Awaited<ReturnType<typeof openDoor>>
>
export type OpenDoorMutationError =
  | OpenDoor400
  | OpenDoor401
  | OpenDoor403
  | OpenDoor404
  | OpenDoor422
  | OpenDoor500

/**
 * @summary Gerar código para abertura de porta de um espaço
 */
export const useOpenDoor = <
  TError =
    | OpenDoor400
    | OpenDoor401
    | OpenDoor403
    | OpenDoor404
    | OpenDoor422
    | OpenDoor500,
>(
  params: OpenDoorParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof openDoor>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof openDoor>>
    > & { swrKey?: string }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getOpenDoorMutationKey(params)
  const swrFn = getOpenDoorMutationFetcher(params, requestOptions)

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
/**
 * Gera um QR Code para um espaço específico, salva-o no S3 e atualiza a URL do QR Code no registro do espaço.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Restrito a usuários com perfil 'admin', 'dev'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Processo**:
  1. Valida o ID do espaço fornecido
  2. Verifica se o espaço já possui um QR Code
  3. Se já existir, retorna o QR Code existente
  4. Caso contrário, gera um QR Code com link para o espaço
  5. Salva o novo QR Code no S3
  6. Atualiza o registro do espaço com a URL do QR Code
  7. Retorna a URL do QR Code

**Middlewares aplicados**:
- `verifyJWT`: Valida o token JWT e extrai os dados do usuário autenticado
- `validateUserRole`: Restringe acesso aos perfis especificados
- `validateUserAccount`: Verifica se a conta do usuário autenticado está ativa
 * @summary Gerar QR Code para um espaço
 */
export type spaceQrcodeResponse200 = {
  data: SpaceQrcode200
  status: 200
}

export type spaceQrcodeResponse400 = {
  data: SpaceQrcode400
  status: 400
}

export type spaceQrcodeResponse401 = {
  data: SpaceQrcode401
  status: 401
}

export type spaceQrcodeResponse403 = {
  data: SpaceQrcode403
  status: 403
}

export type spaceQrcodeResponse404 = {
  data: SpaceQrcode404
  status: 404
}

export type spaceQrcodeResponse422 = {
  data: SpaceQrcode422
  status: 422
}

export type spaceQrcodeResponse500 = {
  data: SpaceQrcode500
  status: 500
}

export type spaceQrcodeResponseSuccess = spaceQrcodeResponse200 & {
  headers: Headers
}
export type spaceQrcodeResponseError = (
  | spaceQrcodeResponse400
  | spaceQrcodeResponse401
  | spaceQrcodeResponse403
  | spaceQrcodeResponse404
  | spaceQrcodeResponse422
  | spaceQrcodeResponse500
) & {
  headers: Headers
}

export const getSpaceQrcodeUrl = (spaceId: string) => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${spaceId}/qrcode`
}

export const spaceQrcode = async (
  spaceId: string,
  options?: RequestInit,
): Promise<spaceQrcodeResponseSuccess> => {
  return customFetch<spaceQrcodeResponseSuccess>(getSpaceQrcodeUrl(spaceId), {
    ...options,
    method: 'PATCH',
  })
}

export const getSpaceQrcodeMutationFetcher = (
  spaceId: string,
  options?: SecondParameter<typeof customFetch>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return spaceQrcode(spaceId, options)
  }
}
export const getSpaceQrcodeMutationKey = (spaceId: string) =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space/${spaceId}/qrcode`,
  ] as const

export type SpaceQrcodeMutationResult = NonNullable<
  Awaited<ReturnType<typeof spaceQrcode>>
>
export type SpaceQrcodeMutationError =
  | SpaceQrcode400
  | SpaceQrcode401
  | SpaceQrcode403
  | SpaceQrcode404
  | SpaceQrcode422
  | SpaceQrcode500

/**
 * @summary Gerar QR Code para um espaço
 */
export const useSpaceQrcode = <
  TError =
    | SpaceQrcode400
    | SpaceQrcode401
    | SpaceQrcode403
    | SpaceQrcode404
    | SpaceQrcode422
    | SpaceQrcode500,
>(
  spaceId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof spaceQrcode>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof spaceQrcode>>
    > & { swrKey?: string }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getSpaceQrcodeMutationKey(spaceId)
  const swrFn = getSpaceQrcodeMutationFetcher(spaceId, requestOptions)

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
