/**
 * Generated by orval v7.3.0 🍺
 * Do not edit manually.
 * BBZ App Backend API
 * API documentation for BBZ App Backend
 * OpenAPI spec version: 1.0.0
 */
/**
 * Código de status HTTP 500.
 */
export type ReservationGetDetail500StatusCode =
  (typeof ReservationGetDetail500StatusCode)[keyof typeof ReservationGetDetail500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationGetDetail500Name =
  (typeof ReservationGetDetail500Name)[keyof typeof ReservationGetDetail500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ReservationGetDetail500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationGetDetail500Name
  /** Código de status HTTP 500. */
  status_code: ReservationGetDetail500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ReservationGetDetail422StatusCode =
  (typeof ReservationGetDetail422StatusCode)[keyof typeof ReservationGetDetail422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationGetDetail422Name =
  (typeof ReservationGetDetail422Name)[keyof typeof ReservationGetDetail422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ReservationGetDetail422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ReservationGetDetail422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ReservationGetDetail422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationGetDetail422Name
  /** Código de status HTTP 422. */
  status_code: ReservationGetDetail422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type ReservationGetDetail404StatusCode =
  (typeof ReservationGetDetail404StatusCode)[keyof typeof ReservationGetDetail404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationGetDetail404Name =
  (typeof ReservationGetDetail404Name)[keyof typeof ReservationGetDetail404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type ReservationGetDetail404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationGetDetail404Name
  /** Código de status HTTP 404. */
  status_code: ReservationGetDetail404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ReservationGetDetail403StatusCode =
  (typeof ReservationGetDetail403StatusCode)[keyof typeof ReservationGetDetail403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationGetDetail403Name =
  (typeof ReservationGetDetail403Name)[keyof typeof ReservationGetDetail403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ReservationGetDetail403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationGetDetail403Name
  /** Código de status HTTP 403. */
  status_code: ReservationGetDetail403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ReservationGetDetail401StatusCode =
  (typeof ReservationGetDetail401StatusCode)[keyof typeof ReservationGetDetail401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationGetDetail401Name =
  (typeof ReservationGetDetail401Name)[keyof typeof ReservationGetDetail401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ReservationGetDetail401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationGetDetail401Name
  /** Código de status HTTP 401. */
  status_code: ReservationGetDetail401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ReservationGetDetail400StatusCode =
  (typeof ReservationGetDetail400StatusCode)[keyof typeof ReservationGetDetail400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationGetDetail400Name =
  (typeof ReservationGetDetail400Name)[keyof typeof ReservationGetDetail400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ReservationGetDetail400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationGetDetail400Name
  /** Código de status HTTP 400. */
  status_code: ReservationGetDetail400StatusCode
}

/**
 * Detalhes da reserva recuperados com sucesso.
 */
export type ReservationGetDetail200 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  /** Detalhes completos de uma reserva paginada */
  reservation: ReservationGetDetail200Reservation
}

/**
 * Informações do usuário que fez a reserva
 */
export type ReservationGetDetail200ReservationUser = {
  /** E-mail do usuário */
  email: string
  /** Identificador único do usuário */
  id: string
  /** Nome do usuário */
  name: string
}

/**
 * Status atual da reserva
 */
export type ReservationGetDetail200ReservationStatus =
  (typeof ReservationGetDetail200ReservationStatus)[keyof typeof ReservationGetDetail200ReservationStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail200ReservationStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

/**
 * Tipo do espaço
 */
export type ReservationGetDetail200ReservationSpaceType =
  (typeof ReservationGetDetail200ReservationSpaceType)[keyof typeof ReservationGetDetail200ReservationSpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationGetDetail200ReservationSpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

/**
 * Informações básicas do espaço
 */
export type ReservationGetDetail200ReservationSpace = {
  /**
   * Andar do espaço
   * @nullable
   */
  floor: string | null
  /** Identificador único do espaço */
  id: string
  /** Nome do espaço */
  name: string
  /**
   * Posição do espaço
   * @nullable
   */
  position: string | null
  /** Tipo do espaço */
  type: ReservationGetDetail200ReservationSpaceType
  /**
   * Zona do espaço
   * @nullable
   */
  zone: string | null
}

/**
 * Usuário que cancelou a reserva (ou null)
 * @nullable
 */
export type ReservationGetDetail200ReservationCancelledBy = {
  /** ID do usuário que cancelou */
  id: string
  /** Nome do usuário que cancelou */
  name: string
} | null

/**
 * Detalhes completos de uma reserva paginada
 */
export type ReservationGetDetail200Reservation = {
  /** Lista de colaboradores da BBZ participantes */
  bbzCollaborators: string[]
  /**
   * Data/hora do cancelamento (ou null)
   * @nullable
   */
  cancelledAt: string | null
  /**
   * Usuário que cancelou a reserva (ou null)
   * @nullable
   */
  cancelledBy: ReservationGetDetail200ReservationCancelledBy
  /**
   * Motivo do cancelamento (ou null)
   * @nullable
   */
  cancelReason: string | null
  /**
   * Data/hora do fechamento da reserva (ou null)
   * @nullable
   */
  closedAt: string | null
  /** Data/hora de criação da reserva */
  createdAt: string
  /** Lista de convidados externos participantes */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Informações básicas do espaço */
  space: ReservationGetDetail200ReservationSpace
  /** Array de identificadores únicos dos slots de tempo reservados */
  spaceSlotIds: string[]
  /** Status atual da reserva */
  status: ReservationGetDetail200ReservationStatus
  /** Informações do usuário que fez a reserva */
  user: ReservationGetDetail200ReservationUser
}

/**
 * Código de status HTTP 500.
 */
export type ReservationCheckInOut500StatusCode =
  (typeof ReservationCheckInOut500StatusCode)[keyof typeof ReservationCheckInOut500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationCheckInOut500Name =
  (typeof ReservationCheckInOut500Name)[keyof typeof ReservationCheckInOut500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ReservationCheckInOut500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationCheckInOut500Name
  /** Código de status HTTP 500. */
  status_code: ReservationCheckInOut500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ReservationCheckInOut422StatusCode =
  (typeof ReservationCheckInOut422StatusCode)[keyof typeof ReservationCheckInOut422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationCheckInOut422Name =
  (typeof ReservationCheckInOut422Name)[keyof typeof ReservationCheckInOut422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ReservationCheckInOut422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ReservationCheckInOut422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ReservationCheckInOut422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationCheckInOut422Name
  /** Código de status HTTP 422. */
  status_code: ReservationCheckInOut422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type ReservationCheckInOut404StatusCode =
  (typeof ReservationCheckInOut404StatusCode)[keyof typeof ReservationCheckInOut404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationCheckInOut404Name =
  (typeof ReservationCheckInOut404Name)[keyof typeof ReservationCheckInOut404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type ReservationCheckInOut404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationCheckInOut404Name
  /** Código de status HTTP 404. */
  status_code: ReservationCheckInOut404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ReservationCheckInOut403StatusCode =
  (typeof ReservationCheckInOut403StatusCode)[keyof typeof ReservationCheckInOut403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationCheckInOut403Name =
  (typeof ReservationCheckInOut403Name)[keyof typeof ReservationCheckInOut403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ReservationCheckInOut403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationCheckInOut403Name
  /** Código de status HTTP 403. */
  status_code: ReservationCheckInOut403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ReservationCheckInOut401StatusCode =
  (typeof ReservationCheckInOut401StatusCode)[keyof typeof ReservationCheckInOut401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationCheckInOut401Name =
  (typeof ReservationCheckInOut401Name)[keyof typeof ReservationCheckInOut401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ReservationCheckInOut401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationCheckInOut401Name
  /** Código de status HTTP 401. */
  status_code: ReservationCheckInOut401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ReservationCheckInOut400StatusCode =
  (typeof ReservationCheckInOut400StatusCode)[keyof typeof ReservationCheckInOut400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ReservationCheckInOut400Name =
  (typeof ReservationCheckInOut400Name)[keyof typeof ReservationCheckInOut400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ReservationCheckInOut400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ReservationCheckInOut400Name
  /** Código de status HTTP 400. */
  status_code: ReservationCheckInOut400StatusCode
}

/**
 * Status atual da reserva. Campo obrigatório.
 */
export type ReservationCheckInOut200ReservationStatus =
  (typeof ReservationCheckInOut200ReservationStatus)[keyof typeof ReservationCheckInOut200ReservationStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ReservationCheckInOut200ReservationStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

/**
 * Informações da reserva após a operação de check-in ou check-out
 */
export type ReservationCheckInOut200Reservation = {
  /**
   * Data e hora do check-in no formato ISO 8601. Pode ser nulo se apenas o check-out foi realizado. Campo obrigatório.
   * @nullable
   */
  checkInAt: string | null
  /**
   * Data e hora do check-out no formato ISO 8601. Pode ser nulo se apenas o check-in foi realizado. Campo obrigatório.
   * @nullable
   */
  checkOutAt: string | null
  /** Identificador único da reserva no formato UUID v4. Campo obrigatório. */
  id: string
  /** Status atual da reserva. Campo obrigatório. */
  status: ReservationCheckInOut200ReservationStatus
}

/**
 * Operação de check-in/check-out realizada com sucesso
 */
export type ReservationCheckInOut200 = {
  /** Mensagem informativa sobre o resultado da operação (check-in ou check-out). Campo obrigatório. */
  message: string
  /** Informações da reserva após a operação de check-in ou check-out */
  reservation: ReservationCheckInOut200Reservation
}

/**
 * Código de status HTTP 500.
 */
export type GetSpaceReservationStats500StatusCode =
  (typeof GetSpaceReservationStats500StatusCode)[keyof typeof GetSpaceReservationStats500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceReservationStats500Name =
  (typeof GetSpaceReservationStats500Name)[keyof typeof GetSpaceReservationStats500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetSpaceReservationStats500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceReservationStats500Name
  /** Código de status HTTP 500. */
  status_code: GetSpaceReservationStats500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetSpaceReservationStats403StatusCode =
  (typeof GetSpaceReservationStats403StatusCode)[keyof typeof GetSpaceReservationStats403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceReservationStats403Name =
  (typeof GetSpaceReservationStats403Name)[keyof typeof GetSpaceReservationStats403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetSpaceReservationStats403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceReservationStats403Name
  /** Código de status HTTP 403. */
  status_code: GetSpaceReservationStats403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetSpaceReservationStats401StatusCode =
  (typeof GetSpaceReservationStats401StatusCode)[keyof typeof GetSpaceReservationStats401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceReservationStats401Name =
  (typeof GetSpaceReservationStats401Name)[keyof typeof GetSpaceReservationStats401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetSpaceReservationStats401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceReservationStats401Name
  /** Código de status HTTP 401. */
  status_code: GetSpaceReservationStats401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetSpaceReservationStats400StatusCode =
  (typeof GetSpaceReservationStats400StatusCode)[keyof typeof GetSpaceReservationStats400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceReservationStats400Name =
  (typeof GetSpaceReservationStats400Name)[keyof typeof GetSpaceReservationStats400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceReservationStats400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetSpaceReservationStats400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceReservationStats400Name
  /** Código de status HTTP 400. */
  status_code: GetSpaceReservationStats400StatusCode
}

export type GetSpaceReservationStats200 = {
  /**
   * Array com os três horários de início mais utilizados, do menor para o maior, formato HH:mm
   * @minItems 3
   * @maxItems 3
   */
  mostUsedStartTimes: string[]
  /**
   * Data e hora da próxima reserva no formato 'dd/MM/yyyy às HH:mm', ou null se não houver
   * @nullable
   */
  nextReservation: string | null
  /**
   * Número total de reservas para o usuário
   * @minimum 0
   */
  total: number
}

/**
 * Código de status HTTP 500.
 */
export type ListSpaceReservations500StatusCode =
  (typeof ListSpaceReservations500StatusCode)[keyof typeof ListSpaceReservations500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceReservations500Name =
  (typeof ListSpaceReservations500Name)[keyof typeof ListSpaceReservations500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListSpaceReservations500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceReservations500Name
  /** Código de status HTTP 500. */
  status_code: ListSpaceReservations500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListSpaceReservations422StatusCode =
  (typeof ListSpaceReservations422StatusCode)[keyof typeof ListSpaceReservations422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceReservations422Name =
  (typeof ListSpaceReservations422Name)[keyof typeof ListSpaceReservations422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListSpaceReservations422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListSpaceReservations422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListSpaceReservations422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceReservations422Name
  /** Código de status HTTP 422. */
  status_code: ListSpaceReservations422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type ListSpaceReservations404StatusCode =
  (typeof ListSpaceReservations404StatusCode)[keyof typeof ListSpaceReservations404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceReservations404Name =
  (typeof ListSpaceReservations404Name)[keyof typeof ListSpaceReservations404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type ListSpaceReservations404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceReservations404Name
  /** Código de status HTTP 404. */
  status_code: ListSpaceReservations404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListSpaceReservations403StatusCode =
  (typeof ListSpaceReservations403StatusCode)[keyof typeof ListSpaceReservations403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceReservations403Name =
  (typeof ListSpaceReservations403Name)[keyof typeof ListSpaceReservations403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListSpaceReservations403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceReservations403Name
  /** Código de status HTTP 403. */
  status_code: ListSpaceReservations403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListSpaceReservations401StatusCode =
  (typeof ListSpaceReservations401StatusCode)[keyof typeof ListSpaceReservations401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceReservations401Name =
  (typeof ListSpaceReservations401Name)[keyof typeof ListSpaceReservations401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListSpaceReservations401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceReservations401Name
  /** Código de status HTTP 401. */
  status_code: ListSpaceReservations401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListSpaceReservations400StatusCode =
  (typeof ListSpaceReservations400StatusCode)[keyof typeof ListSpaceReservations400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceReservations400Name =
  (typeof ListSpaceReservations400Name)[keyof typeof ListSpaceReservations400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListSpaceReservations400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceReservations400Name
  /** Código de status HTTP 400. */
  status_code: ListSpaceReservations400StatusCode
}

/**
 * Informações do usuário que fez a reserva
 */
export type ListSpaceReservations200ReservationsItemUser = {
  /** E-mail do usuário */
  email: string
  /** Identificador único do usuário */
  id: string
  /** Nome do usuário */
  name: string
}

/**
 * Status atual da reserva
 */
export type ListSpaceReservations200ReservationsItemStatus =
  (typeof ListSpaceReservations200ReservationsItemStatus)[keyof typeof ListSpaceReservations200ReservationsItemStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations200ReservationsItemStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

/**
 * Detalhes completos de uma reserva paginada
 */
export type ListSpaceReservations200ReservationsItem = {
  /** Lista de colaboradores da BBZ participantes */
  bbzCollaborators: string[]
  /**
   * Data/hora do cancelamento (ou null)
   * @nullable
   */
  cancelledAt: string | null
  /**
   * Usuário que cancelou a reserva (ou null)
   * @nullable
   */
  cancelledBy: ListSpaceReservations200ReservationsItemCancelledBy
  /**
   * Motivo do cancelamento (ou null)
   * @nullable
   */
  cancelReason: string | null
  /** Registros de check-in/check-out relacionados à reserva */
  checkInOuts: ListSpaceReservations200ReservationsItemCheckInOutsItem[]
  /**
   * Data/hora do fechamento da reserva (ou null)
   * @nullable
   */
  closedAt: string | null
  /** Data/hora de criação da reserva */
  createdAt: string
  /** Lista de convidados externos participantes */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Informações básicas do espaço */
  space: ListSpaceReservations200ReservationsItemSpace
  /** Array de identificadores únicos dos slots de tempo reservados */
  spaceSlotIds: string[]
  /** Status atual da reserva */
  status: ListSpaceReservations200ReservationsItemStatus
  /** Informações do usuário que fez a reserva */
  user: ListSpaceReservations200ReservationsItemUser
}

/**
 * Resposta paginada contendo reservas de espaço
 */
export type ListSpaceReservations200 = {
  /** Número da página atual */
  currentPage: number
  /** Lista de reservas encontradas na página atual */
  reservations: ListSpaceReservations200ReservationsItem[]
  /** Número total de reservas encontradas para o filtro */
  totalCount: number
  /** Número total de páginas disponíveis */
  totalPages: number
}

/**
 * Tipo do espaço
 */
export type ListSpaceReservations200ReservationsItemSpaceType =
  (typeof ListSpaceReservations200ReservationsItemSpaceType)[keyof typeof ListSpaceReservations200ReservationsItemSpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations200ReservationsItemSpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

/**
 * Informações básicas do espaço
 */
export type ListSpaceReservations200ReservationsItemSpace = {
  /**
   * Andar do espaço
   * @nullable
   */
  floor: string | null
  /** Identificador único do espaço */
  id: string
  /** Nome do espaço */
  name: string
  /**
   * Posição do espaço
   * @nullable
   */
  position: string | null
  /** Tipo do espaço */
  type: ListSpaceReservations200ReservationsItemSpaceType
  /**
   * Zona do espaço
   * @nullable
   */
  zone: string | null
}

/**
 * Tipo do registro (check-in ou check-out)
 */
export type ListSpaceReservations200ReservationsItemCheckInOutsItemType =
  (typeof ListSpaceReservations200ReservationsItemCheckInOutsItemType)[keyof typeof ListSpaceReservations200ReservationsItemCheckInOutsItemType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservations200ReservationsItemCheckInOutsItemType = {
  'check-in': 'check-in',
  'check-out': 'check-out',
} as const

export type ListSpaceReservations200ReservationsItemCheckInOutsItem = {
  /** Data/hora do registro */
  createdAt: string
  /** Identificador único do registro de check-in/check-out */
  id: string
  /** Identificador único da reserva */
  reservationId: string
  /** Identificador único do espaço */
  spaceId: string
  /** Tipo do registro (check-in ou check-out) */
  type: ListSpaceReservations200ReservationsItemCheckInOutsItemType
  /** Identificador único do usuário */
  userId: string
  /** Nome do usuário que fez o check-in/check-out */
  userName: string
}

/**
 * Usuário que cancelou a reserva (ou null)
 * @nullable
 */
export type ListSpaceReservations200ReservationsItemCancelledBy = {
  /** ID do usuário que cancelou */
  id: string
  /** Nome do usuário que cancelou */
  name: string
} | null

export type ListSpaceReservationsIncludeUserAsGuest =
  (typeof ListSpaceReservationsIncludeUserAsGuest)[keyof typeof ListSpaceReservationsIncludeUserAsGuest]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceReservationsIncludeUserAsGuest = {
  true: 'true',
  false: 'false',
} as const

export type ListSpaceReservationsParams = {
  /**
   * Número da página para paginação, começando em 1 (padrão: 1)
   */
  page?: string
  /**
   * Quantidade de resultados por página, entre 1 e 10000 (padrão: 10000)
   */
  pageSize?: string
  /**
   * ID do usuário para filtrar reservas (opcional)
   */
  userId?: string
  /**
   * ID do espaço para filtrar reservas (opcional)
   */
  spaceId?: string
  /**
   * Inclui reservas onde o usuário é convidado (email em bbz_collaborators ou external_guests)
   */
  includeUserAsGuest?: ListSpaceReservationsIncludeUserAsGuest
}

/**
 * Código de status HTTP 500.
 */
export type CancelSpaceReservation500StatusCode =
  (typeof CancelSpaceReservation500StatusCode)[keyof typeof CancelSpaceReservation500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelSpaceReservation500Name =
  (typeof CancelSpaceReservation500Name)[keyof typeof CancelSpaceReservation500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CancelSpaceReservation500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CancelSpaceReservation500Name
  /** Código de status HTTP 500. */
  status_code: CancelSpaceReservation500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CancelSpaceReservation422StatusCode =
  (typeof CancelSpaceReservation422StatusCode)[keyof typeof CancelSpaceReservation422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelSpaceReservation422Name =
  (typeof CancelSpaceReservation422Name)[keyof typeof CancelSpaceReservation422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CancelSpaceReservation422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CancelSpaceReservation422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CancelSpaceReservation422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CancelSpaceReservation422Name
  /** Código de status HTTP 422. */
  status_code: CancelSpaceReservation422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CancelSpaceReservation404StatusCode =
  (typeof CancelSpaceReservation404StatusCode)[keyof typeof CancelSpaceReservation404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelSpaceReservation404Name =
  (typeof CancelSpaceReservation404Name)[keyof typeof CancelSpaceReservation404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CancelSpaceReservation404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CancelSpaceReservation404Name
  /** Código de status HTTP 404. */
  status_code: CancelSpaceReservation404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CancelSpaceReservation403StatusCode =
  (typeof CancelSpaceReservation403StatusCode)[keyof typeof CancelSpaceReservation403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelSpaceReservation403Name =
  (typeof CancelSpaceReservation403Name)[keyof typeof CancelSpaceReservation403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CancelSpaceReservation403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CancelSpaceReservation403Name
  /** Código de status HTTP 403. */
  status_code: CancelSpaceReservation403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CancelSpaceReservation401StatusCode =
  (typeof CancelSpaceReservation401StatusCode)[keyof typeof CancelSpaceReservation401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelSpaceReservation401Name =
  (typeof CancelSpaceReservation401Name)[keyof typeof CancelSpaceReservation401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CancelSpaceReservation401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CancelSpaceReservation401Name
  /** Código de status HTTP 401. */
  status_code: CancelSpaceReservation401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CancelSpaceReservation400StatusCode =
  (typeof CancelSpaceReservation400StatusCode)[keyof typeof CancelSpaceReservation400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelSpaceReservation400Name =
  (typeof CancelSpaceReservation400Name)[keyof typeof CancelSpaceReservation400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CancelSpaceReservation400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CancelSpaceReservation400Name
  /** Código de status HTTP 400. */
  status_code: CancelSpaceReservation400StatusCode
}

/**
 * Status atual da reserva
 */
export type CancelSpaceReservation200ReservationStatus =
  (typeof CancelSpaceReservation200ReservationStatus)[keyof typeof CancelSpaceReservation200ReservationStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelSpaceReservation200ReservationStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

export type CancelSpaceReservation200Reservation = {
  /** Lista de colaboradores da BBZ */
  bbzCollaborators: string[]
  /** Data e hora do cancelamento da reserva */
  cancelledAt: string
  /** Identificador único do usuário que cancelou a reserva */
  cancelledBy: string
  /** Motivo do cancelamento */
  cancelReason: string
  /** Lista de convidados externos */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Identificador único do espaço */
  spaceId: string
  /** Array de identificadores únicos dos slots de tempo */
  spaceSlotIds: string[]
  /** Status atual da reserva */
  status: CancelSpaceReservation200ReservationStatus
  /** Identificador único do usuário que fez a reserva */
  userId: string
}

export type CancelSpaceReservation200 = {
  /** Mensagem de sucesso */
  message: string
  reservation: CancelSpaceReservation200Reservation
}

/**
 * Corpo da requisição para cancelar uma reserva
 */
export type CancelSpaceReservationBody = {
  /**
   * Motivo do cancelamento da reserva
   * @minLength 3
   * @maxLength 500
   */
  cancelReason: string
  /** Identificador único da reserva a ser cancelada */
  id?: string
  /** Array de identificadores únicos dos slots de tempo associados à reserva */
  spaceSlotIds?: string[]
}

/**
 * Código de status HTTP 500.
 */
export type CloseSpaceReservation500StatusCode =
  (typeof CloseSpaceReservation500StatusCode)[keyof typeof CloseSpaceReservation500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseSpaceReservation500Name =
  (typeof CloseSpaceReservation500Name)[keyof typeof CloseSpaceReservation500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CloseSpaceReservation500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CloseSpaceReservation500Name
  /** Código de status HTTP 500. */
  status_code: CloseSpaceReservation500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CloseSpaceReservation422StatusCode =
  (typeof CloseSpaceReservation422StatusCode)[keyof typeof CloseSpaceReservation422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseSpaceReservation422Name =
  (typeof CloseSpaceReservation422Name)[keyof typeof CloseSpaceReservation422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CloseSpaceReservation422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CloseSpaceReservation422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CloseSpaceReservation422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CloseSpaceReservation422Name
  /** Código de status HTTP 422. */
  status_code: CloseSpaceReservation422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CloseSpaceReservation404StatusCode =
  (typeof CloseSpaceReservation404StatusCode)[keyof typeof CloseSpaceReservation404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseSpaceReservation404Name =
  (typeof CloseSpaceReservation404Name)[keyof typeof CloseSpaceReservation404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CloseSpaceReservation404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CloseSpaceReservation404Name
  /** Código de status HTTP 404. */
  status_code: CloseSpaceReservation404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CloseSpaceReservation403StatusCode =
  (typeof CloseSpaceReservation403StatusCode)[keyof typeof CloseSpaceReservation403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseSpaceReservation403Name =
  (typeof CloseSpaceReservation403Name)[keyof typeof CloseSpaceReservation403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CloseSpaceReservation403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CloseSpaceReservation403Name
  /** Código de status HTTP 403. */
  status_code: CloseSpaceReservation403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CloseSpaceReservation401StatusCode =
  (typeof CloseSpaceReservation401StatusCode)[keyof typeof CloseSpaceReservation401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseSpaceReservation401Name =
  (typeof CloseSpaceReservation401Name)[keyof typeof CloseSpaceReservation401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CloseSpaceReservation401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CloseSpaceReservation401Name
  /** Código de status HTTP 401. */
  status_code: CloseSpaceReservation401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CloseSpaceReservation400StatusCode =
  (typeof CloseSpaceReservation400StatusCode)[keyof typeof CloseSpaceReservation400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseSpaceReservation400Name =
  (typeof CloseSpaceReservation400Name)[keyof typeof CloseSpaceReservation400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CloseSpaceReservation400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CloseSpaceReservation400Name
  /** Código de status HTTP 400. */
  status_code: CloseSpaceReservation400StatusCode
}

/**
 * Status atual da reserva
 */
export type CloseSpaceReservation200ReservationStatus =
  (typeof CloseSpaceReservation200ReservationStatus)[keyof typeof CloseSpaceReservation200ReservationStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseSpaceReservation200ReservationStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

export type CloseSpaceReservation200Reservation = {
  /** Lista de colaboradores da BBZ */
  bbzCollaborators: string[]
  /** Data e hora do fechamento da reserva */
  closedAt: string
  /** Lista de convidados externos */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Identificador único do espaço */
  spaceId: string
  /** Array de identificadores únicos dos slots de tempo */
  spaceSlotIds: string[]
  /** Status atual da reserva */
  status: CloseSpaceReservation200ReservationStatus
  /** Identificador único do usuário que fez a reserva */
  userId: string
}

export type CloseSpaceReservation200 = {
  /** Mensagem de sucesso */
  message: string
  reservation: CloseSpaceReservation200Reservation
}

/**
 * Corpo da requisição para fechar uma reserva (atualmente vazio, mas requerido para o formato PATCH)
 */
export type CloseSpaceReservationBody = {
  /** Identificador único da reserva a ser fechada */
  id?: string
  /** Array de identificadores únicos dos slots de tempo associados à reserva */
  spaceSlotIds?: string[]
}

/**
 * Código de status HTTP 500.
 */
export type CreateSpaceReservation500StatusCode =
  (typeof CreateSpaceReservation500StatusCode)[keyof typeof CreateSpaceReservation500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation500Name =
  (typeof CreateSpaceReservation500Name)[keyof typeof CreateSpaceReservation500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CreateSpaceReservation500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation500Name
  /** Código de status HTTP 500. */
  status_code: CreateSpaceReservation500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CreateSpaceReservation422StatusCode =
  (typeof CreateSpaceReservation422StatusCode)[keyof typeof CreateSpaceReservation422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation422Name =
  (typeof CreateSpaceReservation422Name)[keyof typeof CreateSpaceReservation422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CreateSpaceReservation422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CreateSpaceReservation422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CreateSpaceReservation422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation422Name
  /** Código de status HTTP 422. */
  status_code: CreateSpaceReservation422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type CreateSpaceReservation409StatusCode =
  (typeof CreateSpaceReservation409StatusCode)[keyof typeof CreateSpaceReservation409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation409Name =
  (typeof CreateSpaceReservation409Name)[keyof typeof CreateSpaceReservation409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type CreateSpaceReservation409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation409Name
  /** Código de status HTTP 409. */
  status_code: CreateSpaceReservation409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CreateSpaceReservation404StatusCode =
  (typeof CreateSpaceReservation404StatusCode)[keyof typeof CreateSpaceReservation404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation404Name =
  (typeof CreateSpaceReservation404Name)[keyof typeof CreateSpaceReservation404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CreateSpaceReservation404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation404Name
  /** Código de status HTTP 404. */
  status_code: CreateSpaceReservation404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CreateSpaceReservation403StatusCode =
  (typeof CreateSpaceReservation403StatusCode)[keyof typeof CreateSpaceReservation403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation403Name =
  (typeof CreateSpaceReservation403Name)[keyof typeof CreateSpaceReservation403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CreateSpaceReservation403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation403Name
  /** Código de status HTTP 403. */
  status_code: CreateSpaceReservation403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CreateSpaceReservation401StatusCode =
  (typeof CreateSpaceReservation401StatusCode)[keyof typeof CreateSpaceReservation401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation401Name =
  (typeof CreateSpaceReservation401Name)[keyof typeof CreateSpaceReservation401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CreateSpaceReservation401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation401Name
  /** Código de status HTTP 401. */
  status_code: CreateSpaceReservation401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CreateSpaceReservation400StatusCode =
  (typeof CreateSpaceReservation400StatusCode)[keyof typeof CreateSpaceReservation400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceReservation400Name =
  (typeof CreateSpaceReservation400Name)[keyof typeof CreateSpaceReservation400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CreateSpaceReservation400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceReservation400Name
  /** Código de status HTTP 400. */
  status_code: CreateSpaceReservation400StatusCode
}

/**
 * Status atual da reserva
 */
export type CreateSpaceReservation201ReservationsItemStatus =
  (typeof CreateSpaceReservation201ReservationsItemStatus)[keyof typeof CreateSpaceReservation201ReservationsItemStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceReservation201ReservationsItemStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

export type CreateSpaceReservation201ReservationsItem = {
  /** Lista de colaboradores da BBZ */
  bbzCollaborators: string[]
  /** Lista de convidados externos */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Identificador único do espaço */
  spaceId: string
  /** Array de identificadores únicos dos slots de tempo */
  spaceSlotIds: string[]
  /** Status atual da reserva */
  status: CreateSpaceReservation201ReservationsItemStatus
  /** Identificador único do usuário que fez a reserva */
  userId: string
}

export type CreateSpaceReservation201 = {
  /** Mensagem de sucesso */
  message: string
  reservations: CreateSpaceReservation201ReservationsItem[]
}

export type CreateSpaceReservationBody = {
  /** Lista de colaboradores da BBZ que participarão da reunião */
  bbzCollaborators?: string[]
  /** Lista de convidados externos que participarão da reunião */
  externalGuests?: string[]
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira?: boolean
  /** Identificador único do espaço a ser reservado */
  spaceId: string
  /**
   * Lista de identificadores únicos dos slots de tempo pré-reservados
   * @minItems 1
   */
  spaceSlotIds: string[]
}

/**
 * Código de status HTTP 500.
 */
export type DeleteSpaceSlotPreReserve500StatusCode =
  (typeof DeleteSpaceSlotPreReserve500StatusCode)[keyof typeof DeleteSpaceSlotPreReserve500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpaceSlotPreReserve500Name =
  (typeof DeleteSpaceSlotPreReserve500Name)[keyof typeof DeleteSpaceSlotPreReserve500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteSpaceSlotPreReserve500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpaceSlotPreReserve500Name
  /** Código de status HTTP 500. */
  status_code: DeleteSpaceSlotPreReserve500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteSpaceSlotPreReserve422StatusCode =
  (typeof DeleteSpaceSlotPreReserve422StatusCode)[keyof typeof DeleteSpaceSlotPreReserve422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpaceSlotPreReserve422Name =
  (typeof DeleteSpaceSlotPreReserve422Name)[keyof typeof DeleteSpaceSlotPreReserve422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteSpaceSlotPreReserve422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteSpaceSlotPreReserve422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteSpaceSlotPreReserve422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpaceSlotPreReserve422Name
  /** Código de status HTTP 422. */
  status_code: DeleteSpaceSlotPreReserve422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type DeleteSpaceSlotPreReserve404StatusCode =
  (typeof DeleteSpaceSlotPreReserve404StatusCode)[keyof typeof DeleteSpaceSlotPreReserve404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpaceSlotPreReserve404Name =
  (typeof DeleteSpaceSlotPreReserve404Name)[keyof typeof DeleteSpaceSlotPreReserve404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type DeleteSpaceSlotPreReserve404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpaceSlotPreReserve404Name
  /** Código de status HTTP 404. */
  status_code: DeleteSpaceSlotPreReserve404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteSpaceSlotPreReserve403StatusCode =
  (typeof DeleteSpaceSlotPreReserve403StatusCode)[keyof typeof DeleteSpaceSlotPreReserve403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpaceSlotPreReserve403Name =
  (typeof DeleteSpaceSlotPreReserve403Name)[keyof typeof DeleteSpaceSlotPreReserve403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteSpaceSlotPreReserve403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpaceSlotPreReserve403Name
  /** Código de status HTTP 403. */
  status_code: DeleteSpaceSlotPreReserve403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteSpaceSlotPreReserve401StatusCode =
  (typeof DeleteSpaceSlotPreReserve401StatusCode)[keyof typeof DeleteSpaceSlotPreReserve401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpaceSlotPreReserve401Name =
  (typeof DeleteSpaceSlotPreReserve401Name)[keyof typeof DeleteSpaceSlotPreReserve401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteSpaceSlotPreReserve401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpaceSlotPreReserve401Name
  /** Código de status HTTP 401. */
  status_code: DeleteSpaceSlotPreReserve401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteSpaceSlotPreReserve400StatusCode =
  (typeof DeleteSpaceSlotPreReserve400StatusCode)[keyof typeof DeleteSpaceSlotPreReserve400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpaceSlotPreReserve400Name =
  (typeof DeleteSpaceSlotPreReserve400Name)[keyof typeof DeleteSpaceSlotPreReserve400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpaceSlotPreReserve400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteSpaceSlotPreReserve400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpaceSlotPreReserve400Name
  /** Código de status HTTP 400. */
  status_code: DeleteSpaceSlotPreReserve400StatusCode
}

export type DeleteSpaceSlotPreReserve200 = {
  /** Mensagem de sucesso */
  message: string
  /** Identificador do slot deletado */
  slotId: string
}

/**
 * Código de status HTTP 500.
 */
export type CreateSpaceSlotPreReserve500StatusCode =
  (typeof CreateSpaceSlotPreReserve500StatusCode)[keyof typeof CreateSpaceSlotPreReserve500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve500Name =
  (typeof CreateSpaceSlotPreReserve500Name)[keyof typeof CreateSpaceSlotPreReserve500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CreateSpaceSlotPreReserve500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve500Name
  /** Código de status HTTP 500. */
  status_code: CreateSpaceSlotPreReserve500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CreateSpaceSlotPreReserve422StatusCode =
  (typeof CreateSpaceSlotPreReserve422StatusCode)[keyof typeof CreateSpaceSlotPreReserve422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve422Name =
  (typeof CreateSpaceSlotPreReserve422Name)[keyof typeof CreateSpaceSlotPreReserve422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CreateSpaceSlotPreReserve422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CreateSpaceSlotPreReserve422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CreateSpaceSlotPreReserve422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve422Name
  /** Código de status HTTP 422. */
  status_code: CreateSpaceSlotPreReserve422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type CreateSpaceSlotPreReserve409StatusCode =
  (typeof CreateSpaceSlotPreReserve409StatusCode)[keyof typeof CreateSpaceSlotPreReserve409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve409Name =
  (typeof CreateSpaceSlotPreReserve409Name)[keyof typeof CreateSpaceSlotPreReserve409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type CreateSpaceSlotPreReserve409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve409Name
  /** Código de status HTTP 409. */
  status_code: CreateSpaceSlotPreReserve409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CreateSpaceSlotPreReserve404StatusCode =
  (typeof CreateSpaceSlotPreReserve404StatusCode)[keyof typeof CreateSpaceSlotPreReserve404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve404Name =
  (typeof CreateSpaceSlotPreReserve404Name)[keyof typeof CreateSpaceSlotPreReserve404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CreateSpaceSlotPreReserve404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve404Name
  /** Código de status HTTP 404. */
  status_code: CreateSpaceSlotPreReserve404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CreateSpaceSlotPreReserve403StatusCode =
  (typeof CreateSpaceSlotPreReserve403StatusCode)[keyof typeof CreateSpaceSlotPreReserve403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve403Name =
  (typeof CreateSpaceSlotPreReserve403Name)[keyof typeof CreateSpaceSlotPreReserve403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CreateSpaceSlotPreReserve403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve403Name
  /** Código de status HTTP 403. */
  status_code: CreateSpaceSlotPreReserve403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CreateSpaceSlotPreReserve401StatusCode =
  (typeof CreateSpaceSlotPreReserve401StatusCode)[keyof typeof CreateSpaceSlotPreReserve401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve401Name =
  (typeof CreateSpaceSlotPreReserve401Name)[keyof typeof CreateSpaceSlotPreReserve401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CreateSpaceSlotPreReserve401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve401Name
  /** Código de status HTTP 401. */
  status_code: CreateSpaceSlotPreReserve401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CreateSpaceSlotPreReserve400StatusCode =
  (typeof CreateSpaceSlotPreReserve400StatusCode)[keyof typeof CreateSpaceSlotPreReserve400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpaceSlotPreReserve400Name =
  (typeof CreateSpaceSlotPreReserve400Name)[keyof typeof CreateSpaceSlotPreReserve400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CreateSpaceSlotPreReserve400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpaceSlotPreReserve400Name
  /** Código de status HTTP 400. */
  status_code: CreateSpaceSlotPreReserve400StatusCode
}

/**
 * Status do slot - sempre pre_reserved para este endpoint
 */
export type CreateSpaceSlotPreReserve201SlotStatus =
  (typeof CreateSpaceSlotPreReserve201SlotStatus)[keyof typeof CreateSpaceSlotPreReserve201SlotStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserve201SlotStatus = {
  pre_reserved: 'pre_reserved',
  reserved: 'reserved',
} as const

export type CreateSpaceSlotPreReserve201Slot = {
  /** Identificador único do slot */
  id: string
  /** Data e hora até quando o slot está pré-reservado */
  preReservedUntil: string
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Identificador único do espaço */
  spaceId: string
  /** Status do slot - sempre pre_reserved para este endpoint */
  status: CreateSpaceSlotPreReserve201SlotStatus
  /** ID do usuário que fez a pré-reserva */
  userId: string
}

export type CreateSpaceSlotPreReserve201 = {
  /** Mensagem de sucesso */
  message: string
  slot: CreateSpaceSlotPreReserve201Slot
}

/**
 * Status da reserva - deve ser pre_reserved para pré-reservas
 */
export type CreateSpaceSlotPreReserveBodyStatus =
  (typeof CreateSpaceSlotPreReserveBodyStatus)[keyof typeof CreateSpaceSlotPreReserveBodyStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceSlotPreReserveBodyStatus = {
  pre_reserved: 'pre_reserved',
  reserved: 'reserved',
} as const

export type CreateSpaceSlotPreReserveBody = {
  /** Horário de término da reserva no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início da reserva no formato ISO com timezone do usuário */
  slotStart: string
  spaceId: string
  /** Status da reserva - deve ser pre_reserved para pré-reservas */
  status?: CreateSpaceSlotPreReserveBodyStatus
}

/**
 * Código de status HTTP 500.
 */
export type GetSpaceSlotAvailability500StatusCode =
  (typeof GetSpaceSlotAvailability500StatusCode)[keyof typeof GetSpaceSlotAvailability500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceSlotAvailability500Name =
  (typeof GetSpaceSlotAvailability500Name)[keyof typeof GetSpaceSlotAvailability500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetSpaceSlotAvailability500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceSlotAvailability500Name
  /** Código de status HTTP 500. */
  status_code: GetSpaceSlotAvailability500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type GetSpaceSlotAvailability422StatusCode =
  (typeof GetSpaceSlotAvailability422StatusCode)[keyof typeof GetSpaceSlotAvailability422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceSlotAvailability422Name =
  (typeof GetSpaceSlotAvailability422Name)[keyof typeof GetSpaceSlotAvailability422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type GetSpaceSlotAvailability422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type GetSpaceSlotAvailability422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: GetSpaceSlotAvailability422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceSlotAvailability422Name
  /** Código de status HTTP 422. */
  status_code: GetSpaceSlotAvailability422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type GetSpaceSlotAvailability404StatusCode =
  (typeof GetSpaceSlotAvailability404StatusCode)[keyof typeof GetSpaceSlotAvailability404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceSlotAvailability404Name =
  (typeof GetSpaceSlotAvailability404Name)[keyof typeof GetSpaceSlotAvailability404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type GetSpaceSlotAvailability404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceSlotAvailability404Name
  /** Código de status HTTP 404. */
  status_code: GetSpaceSlotAvailability404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetSpaceSlotAvailability403StatusCode =
  (typeof GetSpaceSlotAvailability403StatusCode)[keyof typeof GetSpaceSlotAvailability403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceSlotAvailability403Name =
  (typeof GetSpaceSlotAvailability403Name)[keyof typeof GetSpaceSlotAvailability403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetSpaceSlotAvailability403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceSlotAvailability403Name
  /** Código de status HTTP 403. */
  status_code: GetSpaceSlotAvailability403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetSpaceSlotAvailability401StatusCode =
  (typeof GetSpaceSlotAvailability401StatusCode)[keyof typeof GetSpaceSlotAvailability401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceSlotAvailability401Name =
  (typeof GetSpaceSlotAvailability401Name)[keyof typeof GetSpaceSlotAvailability401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetSpaceSlotAvailability401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceSlotAvailability401Name
  /** Código de status HTTP 401. */
  status_code: GetSpaceSlotAvailability401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetSpaceSlotAvailability400StatusCode =
  (typeof GetSpaceSlotAvailability400StatusCode)[keyof typeof GetSpaceSlotAvailability400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpaceSlotAvailability400Name =
  (typeof GetSpaceSlotAvailability400Name)[keyof typeof GetSpaceSlotAvailability400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetSpaceSlotAvailability400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpaceSlotAvailability400Name
  /** Código de status HTTP 400. */
  status_code: GetSpaceSlotAvailability400StatusCode
}

/**
 * Tipo de espaço: sala ou estação de trabalho
 */
export type GetSpaceSlotAvailability200SpaceType =
  (typeof GetSpaceSlotAvailability200SpaceType)[keyof typeof GetSpaceSlotAvailability200SpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability200SpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type GetSpaceSlotAvailability200Space = {
  /** Capacidade máxima de pessoas no espaço */
  capacidade: number
  /**
   * Descrição do espaço (pode ser nulo)
   * @nullable
   */
  description: string | null
  /**
   * Andar onde o espaço está localizado
   * @nullable
   */
  floor: string | null
  /** Identificador único do espaço */
  id: string
  /** URLs das imagens do espaço */
  imagens: string[]
  /** Indica se o espaço está ativo ou inativo */
  isActive: boolean
  /** Nome do espaço */
  name: string
  /**
   * Posição específica do espaço
   * @nullable
   */
  position: string | null
  /** Lista de recursos disponíveis no espaço */
  recursos: string[]
  /** Tipo de espaço: sala ou estação de trabalho */
  type: GetSpaceSlotAvailability200SpaceType
  /**
   * Zona/setor do espaço
   * @nullable
   */
  zone: string | null
}

export type GetSpaceSlotAvailability200 = {
  slots: GetSpaceSlotAvailability200SlotsItem[]
  space: GetSpaceSlotAvailability200Space
}

/**
 * Usuário que fez a pré-reserva (null quando não há pré-reserva)
 */
export type GetSpaceSlotAvailability200SlotsItemUser = {
  email: string
  id: string
  name: string
}

export type GetSpaceSlotAvailability200SlotsItemStatus =
  (typeof GetSpaceSlotAvailability200SlotsItemStatus)[keyof typeof GetSpaceSlotAvailability200SlotsItemStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpaceSlotAvailability200SlotsItemStatus = {
  reserved: 'reserved',
  pre_reserved: 'pre_reserved',
} as const

export type GetSpaceSlotAvailability200SlotsItem = {
  /** Identificador único do slot */
  id: string
  /**
   * Data e hora da pré-reserva (null quando não há pré-reserva)
   * @nullable
   */
  preReservedUntil: string | null
  /** Data e hora do slot no formato ISO (UTC) */
  slotEnd: string
  /** Data e hora do slot no formato ISO (UTC) */
  slotStart: string
  status: GetSpaceSlotAvailability200SlotsItemStatus
  /** Usuário que fez a pré-reserva (null quando não há pré-reserva) */
  user: GetSpaceSlotAvailability200SlotsItemUser
}

export type GetSpaceSlotAvailabilityParams = {
  /**
   * Data inicial para verificação de disponibilidade no formato YYYY-MM-DD
   */
  startDate: string
  /**
   * Data final para verificação de disponibilidade no formato YYYY-MM-DD
   */
  endDate: string
}

/**
 * Código de status HTTP 500.
 */
export type ListSpaceSlots500StatusCode =
  (typeof ListSpaceSlots500StatusCode)[keyof typeof ListSpaceSlots500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceSlots500Name =
  (typeof ListSpaceSlots500Name)[keyof typeof ListSpaceSlots500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListSpaceSlots500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceSlots500Name
  /** Código de status HTTP 500. */
  status_code: ListSpaceSlots500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListSpaceSlots422StatusCode =
  (typeof ListSpaceSlots422StatusCode)[keyof typeof ListSpaceSlots422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceSlots422Name =
  (typeof ListSpaceSlots422Name)[keyof typeof ListSpaceSlots422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListSpaceSlots422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListSpaceSlots422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListSpaceSlots422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceSlots422Name
  /** Código de status HTTP 422. */
  status_code: ListSpaceSlots422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListSpaceSlots403StatusCode =
  (typeof ListSpaceSlots403StatusCode)[keyof typeof ListSpaceSlots403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceSlots403Name =
  (typeof ListSpaceSlots403Name)[keyof typeof ListSpaceSlots403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListSpaceSlots403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceSlots403Name
  /** Código de status HTTP 403. */
  status_code: ListSpaceSlots403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListSpaceSlots401StatusCode =
  (typeof ListSpaceSlots401StatusCode)[keyof typeof ListSpaceSlots401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceSlots401Name =
  (typeof ListSpaceSlots401Name)[keyof typeof ListSpaceSlots401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListSpaceSlots401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceSlots401Name
  /** Código de status HTTP 401. */
  status_code: ListSpaceSlots401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListSpaceSlots400StatusCode =
  (typeof ListSpaceSlots400StatusCode)[keyof typeof ListSpaceSlots400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaceSlots400Name =
  (typeof ListSpaceSlots400Name)[keyof typeof ListSpaceSlots400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListSpaceSlots400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaceSlots400Name
  /** Código de status HTTP 400. */
  status_code: ListSpaceSlots400StatusCode
}

/**
 * Tipo de espaço: sala ou estação de trabalho
 */
export type ListSpaceSlots200SpacesItemType =
  (typeof ListSpaceSlots200SpacesItemType)[keyof typeof ListSpaceSlots200SpacesItemType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlots200SpacesItemType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type ListSpaceSlots200SpacesItem = {
  /** Capacidade máxima de pessoas no espaço */
  capacidade: number
  /**
   * Descrição do espaço (pode ser nulo)
   * @nullable
   */
  description: string | null
  /**
   * Andar onde o espaço está localizado
   * @nullable
   */
  floor: string | null
  /** Identificador único do espaço */
  id: string
  /** URLs das imagens do espaço */
  imagens: string[]
  /** Nome do espaço */
  name: string
  /**
   * Posição específica do espaço
   * @nullable
   */
  position: string | null
  /** Lista de recursos disponíveis no espaço */
  recursos: string[]
  /** Tipo de espaço: sala ou estação de trabalho */
  type: ListSpaceSlots200SpacesItemType
  /**
   * Zona/setor do espaço
   * @nullable
   */
  zone: string | null
}

/**
 * Lista paginada de espaços disponíveis com metadados
 */
export type ListSpaceSlots200 = {
  /** Número da página atual sendo exibida */
  currentPage: number
  /** Mensagem informativa sobre o resultado da operação */
  message: string
  /** Array de espaços disponíveis no período solicitado */
  spaces: ListSpaceSlots200SpacesItem[]
  /** Número total de espaços disponíveis para a consulta */
  totalCount: number
  /** Número total de páginas com base no tamanho de página */
  totalPages: number
}

export type ListSpaceSlotsType =
  (typeof ListSpaceSlotsType)[keyof typeof ListSpaceSlotsType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaceSlotsType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type ListSpaceSlotsParams = {
  /**
   * Data/hora no formato ISO com timezone (suporta formatos como -03:00, +00:00 ou Z). Se enviar apenas a data com hora zerada (00:00:00), retorna espaços com pelo menos um horário disponível nessa data. Se enviar data com uma hora específica, retorna espaços disponíveis nesse horário específico. Exemplo para data: "2025-05-22T00:00:00-03:00", exemplo para hora específica: "2025-05-22T14:00:00-03:00"
   */
  datetime: string
  /**
   * Número da página para paginação, começando em 1 (padrão: 1)
   */
  page?: string
  /**
   * Quantidade de resultados por página, entre 1 e 100 (padrão: 12)
   */
  pageSize?: string
  /**
   * Tipo de espaço: sala ou estação de trabalho (padrão: room)
   */
  type?: ListSpaceSlotsType
}

/**
 * Código de status HTTP 500.
 */
export type SpaceQrcode500StatusCode =
  (typeof SpaceQrcode500StatusCode)[keyof typeof SpaceQrcode500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type SpaceQrcode500Name =
  (typeof SpaceQrcode500Name)[keyof typeof SpaceQrcode500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type SpaceQrcode500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: SpaceQrcode500Name
  /** Código de status HTTP 500. */
  status_code: SpaceQrcode500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type SpaceQrcode422StatusCode =
  (typeof SpaceQrcode422StatusCode)[keyof typeof SpaceQrcode422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type SpaceQrcode422Name =
  (typeof SpaceQrcode422Name)[keyof typeof SpaceQrcode422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type SpaceQrcode422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type SpaceQrcode422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: SpaceQrcode422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: SpaceQrcode422Name
  /** Código de status HTTP 422. */
  status_code: SpaceQrcode422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type SpaceQrcode404StatusCode =
  (typeof SpaceQrcode404StatusCode)[keyof typeof SpaceQrcode404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type SpaceQrcode404Name =
  (typeof SpaceQrcode404Name)[keyof typeof SpaceQrcode404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type SpaceQrcode404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: SpaceQrcode404Name
  /** Código de status HTTP 404. */
  status_code: SpaceQrcode404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type SpaceQrcode403StatusCode =
  (typeof SpaceQrcode403StatusCode)[keyof typeof SpaceQrcode403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type SpaceQrcode403Name =
  (typeof SpaceQrcode403Name)[keyof typeof SpaceQrcode403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type SpaceQrcode403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: SpaceQrcode403Name
  /** Código de status HTTP 403. */
  status_code: SpaceQrcode403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type SpaceQrcode401StatusCode =
  (typeof SpaceQrcode401StatusCode)[keyof typeof SpaceQrcode401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type SpaceQrcode401Name =
  (typeof SpaceQrcode401Name)[keyof typeof SpaceQrcode401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type SpaceQrcode401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: SpaceQrcode401Name
  /** Código de status HTTP 401. */
  status_code: SpaceQrcode401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type SpaceQrcode400StatusCode =
  (typeof SpaceQrcode400StatusCode)[keyof typeof SpaceQrcode400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type SpaceQrcode400Name =
  (typeof SpaceQrcode400Name)[keyof typeof SpaceQrcode400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpaceQrcode400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type SpaceQrcode400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: SpaceQrcode400Name
  /** Código de status HTTP 400. */
  status_code: SpaceQrcode400StatusCode
}

/**
 * QR code do espaço gerado com sucesso
 */
export type SpaceQrcode200 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  /** URL para o QR code gerado do espaço. Campo obrigatório. */
  qrcodeUrl: string
}

/**
 * Código de status HTTP 500.
 */
export type OpenDoor500StatusCode =
  (typeof OpenDoor500StatusCode)[keyof typeof OpenDoor500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor500Name =
  (typeof OpenDoor500Name)[keyof typeof OpenDoor500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type OpenDoor500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor500Name
  /** Código de status HTTP 500. */
  status_code: OpenDoor500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type OpenDoor422StatusCode =
  (typeof OpenDoor422StatusCode)[keyof typeof OpenDoor422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor422Name =
  (typeof OpenDoor422Name)[keyof typeof OpenDoor422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type OpenDoor422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type OpenDoor422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: OpenDoor422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor422Name
  /** Código de status HTTP 422. */
  status_code: OpenDoor422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type OpenDoor404StatusCode =
  (typeof OpenDoor404StatusCode)[keyof typeof OpenDoor404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor404Name =
  (typeof OpenDoor404Name)[keyof typeof OpenDoor404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type OpenDoor404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor404Name
  /** Código de status HTTP 404. */
  status_code: OpenDoor404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type OpenDoor403StatusCode =
  (typeof OpenDoor403StatusCode)[keyof typeof OpenDoor403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor403Name =
  (typeof OpenDoor403Name)[keyof typeof OpenDoor403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type OpenDoor403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor403Name
  /** Código de status HTTP 403. */
  status_code: OpenDoor403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type OpenDoor401StatusCode =
  (typeof OpenDoor401StatusCode)[keyof typeof OpenDoor401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor401Name =
  (typeof OpenDoor401Name)[keyof typeof OpenDoor401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type OpenDoor401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor401Name
  /** Código de status HTTP 401. */
  status_code: OpenDoor401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type OpenDoor400StatusCode =
  (typeof OpenDoor400StatusCode)[keyof typeof OpenDoor400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor400Name =
  (typeof OpenDoor400Name)[keyof typeof OpenDoor400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type OpenDoor400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor400Name
  /** Código de status HTTP 400. */
  status_code: OpenDoor400StatusCode
}

export type OpenDoor200SpaceType =
  (typeof OpenDoor200SpaceType)[keyof typeof OpenDoor200SpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor200SpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type OpenDoor200Space = {
  id: string
  name: string
  type: OpenDoor200SpaceType
}

/**
 * Código de abertura da porta gerado com sucesso
 */
export type OpenDoor200 = {
  doorCode: string
  expiresAt: string
  message: string
  space: OpenDoor200Space
}

export type OpenDoorParams = {
  spaceName: string
}

/**
 * Código de status HTTP 500.
 */
export type UpdateSpace500StatusCode =
  (typeof UpdateSpace500StatusCode)[keyof typeof UpdateSpace500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace500Name =
  (typeof UpdateSpace500Name)[keyof typeof UpdateSpace500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UpdateSpace500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace500Name
  /** Código de status HTTP 500. */
  status_code: UpdateSpace500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UpdateSpace422StatusCode =
  (typeof UpdateSpace422StatusCode)[keyof typeof UpdateSpace422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace422Name =
  (typeof UpdateSpace422Name)[keyof typeof UpdateSpace422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UpdateSpace422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UpdateSpace422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UpdateSpace422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace422Name
  /** Código de status HTTP 422. */
  status_code: UpdateSpace422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type UpdateSpace409StatusCode =
  (typeof UpdateSpace409StatusCode)[keyof typeof UpdateSpace409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace409Name =
  (typeof UpdateSpace409Name)[keyof typeof UpdateSpace409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type UpdateSpace409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace409Name
  /** Código de status HTTP 409. */
  status_code: UpdateSpace409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type UpdateSpace404StatusCode =
  (typeof UpdateSpace404StatusCode)[keyof typeof UpdateSpace404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace404Name =
  (typeof UpdateSpace404Name)[keyof typeof UpdateSpace404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type UpdateSpace404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace404Name
  /** Código de status HTTP 404. */
  status_code: UpdateSpace404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UpdateSpace403StatusCode =
  (typeof UpdateSpace403StatusCode)[keyof typeof UpdateSpace403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace403Name =
  (typeof UpdateSpace403Name)[keyof typeof UpdateSpace403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UpdateSpace403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace403Name
  /** Código de status HTTP 403. */
  status_code: UpdateSpace403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UpdateSpace401StatusCode =
  (typeof UpdateSpace401StatusCode)[keyof typeof UpdateSpace401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace401Name =
  (typeof UpdateSpace401Name)[keyof typeof UpdateSpace401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UpdateSpace401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace401Name
  /** Código de status HTTP 401. */
  status_code: UpdateSpace401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UpdateSpace400StatusCode =
  (typeof UpdateSpace400StatusCode)[keyof typeof UpdateSpace400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateSpace400Name =
  (typeof UpdateSpace400Name)[keyof typeof UpdateSpace400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UpdateSpace400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateSpace400Name
  /** Código de status HTTP 400. */
  status_code: UpdateSpace400StatusCode
}

export type UpdateSpace200SpaceType =
  (typeof UpdateSpace200SpaceType)[keyof typeof UpdateSpace200SpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpace200SpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type UpdateSpace200Space = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  /** @nullable */
  floor: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  /** @nullable */
  position: string | null
  /** @nullable */
  qrcodeUrl: string | null
  recursos: string[]
  type: UpdateSpace200SpaceType
  updatedAt: string
  userId: string
  /** @nullable */
  zone: string | null
}

/**
 * Espaço atualizado com sucesso
 */
export type UpdateSpace200 = {
  message: string
  space: UpdateSpace200Space
}

/**
 * Tipo do espaço: sala (room) ou estação de trabalho (workstation)
 */
export type UpdateSpaceBodyType =
  (typeof UpdateSpaceBodyType)[keyof typeof UpdateSpaceBodyType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateSpaceBodyType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type UpdateSpaceBody = {
  /**
   * @minimum 0
   * @exclusiveMinimum
   */
  capacidade?: number
  /**
   * @maxLength 500
   * @nullable
   */
  description?: string | null
  /**
   * Andar onde o espaço está localizado
   * @maxLength 10
   * @nullable
   */
  floor?: string | null
  /** @maxItems 5 */
  imagens?: string[]
  isActive?: boolean
  /**
   * @minLength 3
   * @maxLength 100
   */
  name?: string
  /**
   * Posição específica do espaço
   * @maxLength 50
   * @nullable
   */
  position?: string | null
  /**
   * URL do QR Code do espaço
   * @nullable
   */
  qrcodeUrl?: string | null
  recursos?: string[]
  /** Tipo do espaço: sala (room) ou estação de trabalho (workstation) */
  type?: UpdateSpaceBodyType
  /**
   * Zona ou setor onde o espaço está localizado
   * @maxLength 50
   * @nullable
   */
  zone?: string | null
}

/**
 * Código de status HTTP 500.
 */
export type GetSpace500StatusCode =
  (typeof GetSpace500StatusCode)[keyof typeof GetSpace500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpace500Name =
  (typeof GetSpace500Name)[keyof typeof GetSpace500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetSpace500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpace500Name
  /** Código de status HTTP 500. */
  status_code: GetSpace500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type GetSpace422StatusCode =
  (typeof GetSpace422StatusCode)[keyof typeof GetSpace422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpace422Name =
  (typeof GetSpace422Name)[keyof typeof GetSpace422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type GetSpace422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type GetSpace422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: GetSpace422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpace422Name
  /** Código de status HTTP 422. */
  status_code: GetSpace422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type GetSpace404StatusCode =
  (typeof GetSpace404StatusCode)[keyof typeof GetSpace404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpace404Name =
  (typeof GetSpace404Name)[keyof typeof GetSpace404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type GetSpace404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpace404Name
  /** Código de status HTTP 404. */
  status_code: GetSpace404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetSpace403StatusCode =
  (typeof GetSpace403StatusCode)[keyof typeof GetSpace403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpace403Name =
  (typeof GetSpace403Name)[keyof typeof GetSpace403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetSpace403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpace403Name
  /** Código de status HTTP 403. */
  status_code: GetSpace403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetSpace401StatusCode =
  (typeof GetSpace401StatusCode)[keyof typeof GetSpace401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpace401Name =
  (typeof GetSpace401Name)[keyof typeof GetSpace401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetSpace401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpace401Name
  /** Código de status HTTP 401. */
  status_code: GetSpace401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetSpace400StatusCode =
  (typeof GetSpace400StatusCode)[keyof typeof GetSpace400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetSpace400Name =
  (typeof GetSpace400Name)[keyof typeof GetSpace400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetSpace400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetSpace400Name
  /** Código de status HTTP 400. */
  status_code: GetSpace400StatusCode
}

export type GetSpace200SpaceType =
  (typeof GetSpace200SpaceType)[keyof typeof GetSpace200SpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetSpace200SpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type GetSpace200Space = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  /** @nullable */
  floor: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  /** @nullable */
  position: string | null
  /** @nullable */
  qrcodeUrl: string | null
  recursos: string[]
  type: GetSpace200SpaceType
  updatedAt: string
  userId: string
  userName: string
  /** @nullable */
  zone: string | null
}

/**
 * Detalhes do espaço
 */
export type GetSpace200 = {
  space: GetSpace200Space
}

/**
 * Código de status HTTP 500.
 */
export type DeleteSpace500StatusCode =
  (typeof DeleteSpace500StatusCode)[keyof typeof DeleteSpace500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpace500Name =
  (typeof DeleteSpace500Name)[keyof typeof DeleteSpace500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteSpace500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpace500Name
  /** Código de status HTTP 500. */
  status_code: DeleteSpace500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteSpace422StatusCode =
  (typeof DeleteSpace422StatusCode)[keyof typeof DeleteSpace422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpace422Name =
  (typeof DeleteSpace422Name)[keyof typeof DeleteSpace422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteSpace422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteSpace422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteSpace422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpace422Name
  /** Código de status HTTP 422. */
  status_code: DeleteSpace422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type DeleteSpace404StatusCode =
  (typeof DeleteSpace404StatusCode)[keyof typeof DeleteSpace404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpace404Name =
  (typeof DeleteSpace404Name)[keyof typeof DeleteSpace404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type DeleteSpace404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpace404Name
  /** Código de status HTTP 404. */
  status_code: DeleteSpace404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteSpace403StatusCode =
  (typeof DeleteSpace403StatusCode)[keyof typeof DeleteSpace403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpace403Name =
  (typeof DeleteSpace403Name)[keyof typeof DeleteSpace403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteSpace403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpace403Name
  /** Código de status HTTP 403. */
  status_code: DeleteSpace403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteSpace401StatusCode =
  (typeof DeleteSpace401StatusCode)[keyof typeof DeleteSpace401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpace401Name =
  (typeof DeleteSpace401Name)[keyof typeof DeleteSpace401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteSpace401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpace401Name
  /** Código de status HTTP 401. */
  status_code: DeleteSpace401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteSpace400StatusCode =
  (typeof DeleteSpace400StatusCode)[keyof typeof DeleteSpace400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteSpace400Name =
  (typeof DeleteSpace400Name)[keyof typeof DeleteSpace400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteSpace400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteSpace400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteSpace400Name
  /** Código de status HTTP 400. */
  status_code: DeleteSpace400StatusCode
}

/**
 * Espaço excluído com sucesso
 */
export type DeleteSpace200 = {
  message: string
}

/**
 * Código de status HTTP 500.
 */
export type ListSpaces500StatusCode =
  (typeof ListSpaces500StatusCode)[keyof typeof ListSpaces500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaces500Name =
  (typeof ListSpaces500Name)[keyof typeof ListSpaces500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListSpaces500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaces500Name
  /** Código de status HTTP 500. */
  status_code: ListSpaces500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListSpaces422StatusCode =
  (typeof ListSpaces422StatusCode)[keyof typeof ListSpaces422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaces422Name =
  (typeof ListSpaces422Name)[keyof typeof ListSpaces422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListSpaces422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListSpaces422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListSpaces422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaces422Name
  /** Código de status HTTP 422. */
  status_code: ListSpaces422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListSpaces403StatusCode =
  (typeof ListSpaces403StatusCode)[keyof typeof ListSpaces403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaces403Name =
  (typeof ListSpaces403Name)[keyof typeof ListSpaces403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListSpaces403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaces403Name
  /** Código de status HTTP 403. */
  status_code: ListSpaces403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListSpaces401StatusCode =
  (typeof ListSpaces401StatusCode)[keyof typeof ListSpaces401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaces401Name =
  (typeof ListSpaces401Name)[keyof typeof ListSpaces401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListSpaces401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaces401Name
  /** Código de status HTTP 401. */
  status_code: ListSpaces401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListSpaces400StatusCode =
  (typeof ListSpaces400StatusCode)[keyof typeof ListSpaces400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListSpaces400Name =
  (typeof ListSpaces400Name)[keyof typeof ListSpaces400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListSpaces400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListSpaces400Name
  /** Código de status HTTP 400. */
  status_code: ListSpaces400StatusCode
}

export type ListSpaces200SpacesItemType =
  (typeof ListSpaces200SpacesItemType)[keyof typeof ListSpaces200SpacesItemType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListSpaces200SpacesItemType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type ListSpaces200SpacesItem = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  /** @nullable */
  floor: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  /** @nullable */
  position: string | null
  /** @nullable */
  qrcodeUrl: string | null
  recursos: string[]
  type: ListSpaces200SpacesItemType
  updatedAt: string
  userId: string
  userName: string
  /** @nullable */
  zone: string | null
}

/**
 * Lista de espaços
 */
export type ListSpaces200 = {
  currentPage: number
  spaces: ListSpaces200SpacesItem[]
  totalCount: number
  totalPages: number
}

export type ListSpacesParams = {
  page?: string
  pageSize?: string
  searchTerm?: string
  isActive?: string
  capacidade?: string
  type?: string
  floor?: string
  zone?: string
}

/**
 * Código de status HTTP 500.
 */
export type CreateSpace500StatusCode =
  (typeof CreateSpace500StatusCode)[keyof typeof CreateSpace500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpace500Name =
  (typeof CreateSpace500Name)[keyof typeof CreateSpace500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CreateSpace500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpace500Name
  /** Código de status HTTP 500. */
  status_code: CreateSpace500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CreateSpace422StatusCode =
  (typeof CreateSpace422StatusCode)[keyof typeof CreateSpace422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpace422Name =
  (typeof CreateSpace422Name)[keyof typeof CreateSpace422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CreateSpace422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CreateSpace422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CreateSpace422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpace422Name
  /** Código de status HTTP 422. */
  status_code: CreateSpace422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type CreateSpace409StatusCode =
  (typeof CreateSpace409StatusCode)[keyof typeof CreateSpace409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpace409Name =
  (typeof CreateSpace409Name)[keyof typeof CreateSpace409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type CreateSpace409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpace409Name
  /** Código de status HTTP 409. */
  status_code: CreateSpace409StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CreateSpace403StatusCode =
  (typeof CreateSpace403StatusCode)[keyof typeof CreateSpace403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpace403Name =
  (typeof CreateSpace403Name)[keyof typeof CreateSpace403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CreateSpace403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpace403Name
  /** Código de status HTTP 403. */
  status_code: CreateSpace403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CreateSpace401StatusCode =
  (typeof CreateSpace401StatusCode)[keyof typeof CreateSpace401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpace401Name =
  (typeof CreateSpace401Name)[keyof typeof CreateSpace401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CreateSpace401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpace401Name
  /** Código de status HTTP 401. */
  status_code: CreateSpace401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CreateSpace400StatusCode =
  (typeof CreateSpace400StatusCode)[keyof typeof CreateSpace400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateSpace400Name =
  (typeof CreateSpace400Name)[keyof typeof CreateSpace400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CreateSpace400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CreateSpace400Name
  /** Código de status HTTP 400. */
  status_code: CreateSpace400StatusCode
}

export type CreateSpace201SpaceType =
  (typeof CreateSpace201SpaceType)[keyof typeof CreateSpace201SpaceType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpace201SpaceType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type CreateSpace201Space = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  /** @nullable */
  floor: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  /** @nullable */
  position: string | null
  /** @nullable */
  qrcodeUrl: string | null
  recursos: string[]
  type: CreateSpace201SpaceType
  updatedAt: string
  userId: string
  userName: string
  /** @nullable */
  zone: string | null
}

/**
 * Espaço criado com sucesso
 */
export type CreateSpace201 = {
  message: string
  space: CreateSpace201Space
}

/**
 * Tipo do espaço: sala (room) ou estação de trabalho (workstation)
 */
export type CreateSpaceBodyType =
  (typeof CreateSpaceBodyType)[keyof typeof CreateSpaceBodyType]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateSpaceBodyType = {
  room: 'room',
  workstation: 'workstation',
} as const

export type CreateSpaceBody = {
  /**
   * @minimum 0
   * @exclusiveMinimum
   */
  capacidade: number
  /**
   * @maxLength 500
   * @nullable
   */
  description?: string | null
  /**
   * Andar onde o espaço está localizado
   * @maxLength 10
   * @nullable
   */
  floor?: string | null
  /** @maxItems 5 */
  imagens?: string[]
  isActive?: boolean
  /**
   * @minLength 3
   * @maxLength 100
   */
  name: string
  /**
   * Posição específica do espaço
   * @maxLength 50
   * @nullable
   */
  position?: string | null
  recursos?: string[]
  /** Tipo do espaço: sala (room) ou estação de trabalho (workstation) */
  type: CreateSpaceBodyType
  /**
   * Zona ou setor onde o espaço está localizado
   * @maxLength 50
   * @nullable
   */
  zone?: string | null
}

/**
 * Código de status HTTP 500.
 */
export type DeleteImage500StatusCode =
  (typeof DeleteImage500StatusCode)[keyof typeof DeleteImage500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage500Name =
  (typeof DeleteImage500Name)[keyof typeof DeleteImage500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteImage500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage500Name
  /** Código de status HTTP 500. */
  status_code: DeleteImage500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteImage422StatusCode =
  (typeof DeleteImage422StatusCode)[keyof typeof DeleteImage422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage422Name =
  (typeof DeleteImage422Name)[keyof typeof DeleteImage422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteImage422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteImage422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteImage422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage422Name
  /** Código de status HTTP 422. */
  status_code: DeleteImage422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteImage403StatusCode =
  (typeof DeleteImage403StatusCode)[keyof typeof DeleteImage403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage403Name =
  (typeof DeleteImage403Name)[keyof typeof DeleteImage403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteImage403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage403Name
  /** Código de status HTTP 403. */
  status_code: DeleteImage403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteImage401StatusCode =
  (typeof DeleteImage401StatusCode)[keyof typeof DeleteImage401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage401Name =
  (typeof DeleteImage401Name)[keyof typeof DeleteImage401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteImage401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage401Name
  /** Código de status HTTP 401. */
  status_code: DeleteImage401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteImage400StatusCode =
  (typeof DeleteImage400StatusCode)[keyof typeof DeleteImage400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage400Name =
  (typeof DeleteImage400Name)[keyof typeof DeleteImage400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteImage400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage400Name
  /** Código de status HTTP 400. */
  status_code: DeleteImage400StatusCode
}

/**
 * Image deleted successfully
 */
export type DeleteImage200 = {
  message: string
}

/**
 * Payload to delete an image
 */
export type DeleteImageBody = {
  /** @minLength 1 */
  imagePath: string
}

/**
 * Código de status HTTP 500.
 */
export type UploadImage500StatusCode =
  (typeof UploadImage500StatusCode)[keyof typeof UploadImage500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage500Name =
  (typeof UploadImage500Name)[keyof typeof UploadImage500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UploadImage500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage500Name
  /** Código de status HTTP 500. */
  status_code: UploadImage500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UploadImage422StatusCode =
  (typeof UploadImage422StatusCode)[keyof typeof UploadImage422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage422Name =
  (typeof UploadImage422Name)[keyof typeof UploadImage422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UploadImage422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UploadImage422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UploadImage422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage422Name
  /** Código de status HTTP 422. */
  status_code: UploadImage422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UploadImage403StatusCode =
  (typeof UploadImage403StatusCode)[keyof typeof UploadImage403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage403Name =
  (typeof UploadImage403Name)[keyof typeof UploadImage403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UploadImage403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage403Name
  /** Código de status HTTP 403. */
  status_code: UploadImage403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UploadImage401StatusCode =
  (typeof UploadImage401StatusCode)[keyof typeof UploadImage401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage401Name =
  (typeof UploadImage401Name)[keyof typeof UploadImage401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UploadImage401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage401Name
  /** Código de status HTTP 401. */
  status_code: UploadImage401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UploadImage400StatusCode =
  (typeof UploadImage400StatusCode)[keyof typeof UploadImage400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage400Name =
  (typeof UploadImage400Name)[keyof typeof UploadImage400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UploadImage400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage400Name
  /** Código de status HTTP 400. */
  status_code: UploadImage400StatusCode
}

/**
 * Image uploaded successfully
 */
export type UploadImage201 = {
  imagePath: string
  message: string
}

export type UploadImageBody = {
  file: Blob
}

export type UploadImageParams = {
  /**
   * Group of asset. Ex: 'avatar', 'logo', 'product'
   */
  group: string
  /**
   * Subtitle of asset. Ex: 'profile', 'og', 'drink'
   */
  subtitle?: string
  /**
   * Custom folder to store the image. Default: userId
   */
  folder?: string
}

/**
 * Código de status HTTP 500.
 */
export type UserUpdate500StatusCode =
  (typeof UserUpdate500StatusCode)[keyof typeof UserUpdate500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate500Name =
  (typeof UserUpdate500Name)[keyof typeof UserUpdate500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserUpdate500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate500Name
  /** Código de status HTTP 500. */
  status_code: UserUpdate500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UserUpdate422StatusCode =
  (typeof UserUpdate422StatusCode)[keyof typeof UserUpdate422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate422Name =
  (typeof UserUpdate422Name)[keyof typeof UserUpdate422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UserUpdate422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UserUpdate422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UserUpdate422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate422Name
  /** Código de status HTTP 422. */
  status_code: UserUpdate422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type UserUpdate409StatusCode =
  (typeof UserUpdate409StatusCode)[keyof typeof UserUpdate409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate409Name =
  (typeof UserUpdate409Name)[keyof typeof UserUpdate409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type UserUpdate409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate409Name
  /** Código de status HTTP 409. */
  status_code: UserUpdate409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type UserUpdate404StatusCode =
  (typeof UserUpdate404StatusCode)[keyof typeof UserUpdate404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate404Name =
  (typeof UserUpdate404Name)[keyof typeof UserUpdate404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type UserUpdate404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate404Name
  /** Código de status HTTP 404. */
  status_code: UserUpdate404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserUpdate403StatusCode =
  (typeof UserUpdate403StatusCode)[keyof typeof UserUpdate403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate403Name =
  (typeof UserUpdate403Name)[keyof typeof UserUpdate403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserUpdate403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate403Name
  /** Código de status HTTP 403. */
  status_code: UserUpdate403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserUpdate401StatusCode =
  (typeof UserUpdate401StatusCode)[keyof typeof UserUpdate401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate401Name =
  (typeof UserUpdate401Name)[keyof typeof UserUpdate401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserUpdate401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate401Name
  /** Código de status HTTP 401. */
  status_code: UserUpdate401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserUpdate400StatusCode =
  (typeof UserUpdate400StatusCode)[keyof typeof UserUpdate400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserUpdate400Name =
  (typeof UserUpdate400Name)[keyof typeof UserUpdate400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserUpdate400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserUpdate400Name
  /** Código de status HTTP 400. */
  status_code: UserUpdate400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type UserUpdate200UserRole =
  (typeof UserUpdate200UserRole)[keyof typeof UserUpdate200UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdate200UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserUpdate200User = {
  /** Indica se a conta do usuário está ativa. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Pode ser nulo. Campo obrigatório.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /** Email do usuário. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /** Nome completo do usuário. Campo obrigatório. */
  name: string
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: UserUpdate200UserRole
}

/**
 * Detalhes do usuário atualizado com sucesso
 */
export type UserUpdate200 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  user: UserUpdate200User
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo opcional.
 */
export type UserUpdateBodyRole =
  (typeof UserUpdateBodyRole)[keyof typeof UserUpdateBodyRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserUpdateBodyRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserUpdateBody = {
  /** Indica se a conta do usuário está ativa. Valor booleano. Campo opcional. */
  accountStatus?: boolean
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Pode ser nulo. Campo opcional.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf?: string | null
  /**
   * Email do usuário. Endereço de email válido. Campo opcional.
   * @minLength 3
   * @maxLength 254
   */
  email?: string
  /**
   * Nome completo do usuário. String com nome e sobrenome. Campo opcional.
   * @minLength 2
   * @maxLength 100
   * @pattern ^\S+\s+\S+
   */
  name?: string
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo opcional. */
  role?: UserUpdateBodyRole
}

/**
 * Código de status HTTP 500.
 */
export type UserGet500StatusCode =
  (typeof UserGet500StatusCode)[keyof typeof UserGet500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserGet500Name =
  (typeof UserGet500Name)[keyof typeof UserGet500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserGet500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserGet500Name
  /** Código de status HTTP 500. */
  status_code: UserGet500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UserGet422StatusCode =
  (typeof UserGet422StatusCode)[keyof typeof UserGet422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UserGet422Name =
  (typeof UserGet422Name)[keyof typeof UserGet422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UserGet422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UserGet422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UserGet422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UserGet422Name
  /** Código de status HTTP 422. */
  status_code: UserGet422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type UserGet404StatusCode =
  (typeof UserGet404StatusCode)[keyof typeof UserGet404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type UserGet404Name =
  (typeof UserGet404Name)[keyof typeof UserGet404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type UserGet404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: UserGet404Name
  /** Código de status HTTP 404. */
  status_code: UserGet404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserGet403StatusCode =
  (typeof UserGet403StatusCode)[keyof typeof UserGet403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserGet403Name =
  (typeof UserGet403Name)[keyof typeof UserGet403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserGet403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserGet403Name
  /** Código de status HTTP 403. */
  status_code: UserGet403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserGet401StatusCode =
  (typeof UserGet401StatusCode)[keyof typeof UserGet401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserGet401Name =
  (typeof UserGet401Name)[keyof typeof UserGet401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserGet401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserGet401Name
  /** Código de status HTTP 401. */
  status_code: UserGet401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserGet400StatusCode =
  (typeof UserGet400StatusCode)[keyof typeof UserGet400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserGet400Name =
  (typeof UserGet400Name)[keyof typeof UserGet400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserGet400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserGet400Name
  /** Código de status HTTP 400. */
  status_code: UserGet400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type UserGet200UserRole =
  (typeof UserGet200UserRole)[keyof typeof UserGet200UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserGet200UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserGet200User = {
  /** Indica se a conta do usuário está ativa no sistema. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Pode ser nulo. Campo opcional.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /** Email do usuário para acesso ao sistema. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /**
   * Nome completo do usuário com nome e sobrenome. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  name: string | null
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: UserGet200UserRole
}

/**
 * Detalhes do usuário recuperados com sucesso
 */
export type UserGet200 = {
  /** Mensagem informativa sobre o resultado da operação de consulta do usuário. Campo obrigatório. */
  message: string
  user: UserGet200User
}

/**
 * Código de status HTTP 500.
 */
export type DeleteUser500StatusCode =
  (typeof DeleteUser500StatusCode)[keyof typeof DeleteUser500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser500Name =
  (typeof DeleteUser500Name)[keyof typeof DeleteUser500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteUser500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser500Name
  /** Código de status HTTP 500. */
  status_code: DeleteUser500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteUser422StatusCode =
  (typeof DeleteUser422StatusCode)[keyof typeof DeleteUser422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser422Name =
  (typeof DeleteUser422Name)[keyof typeof DeleteUser422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteUser422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteUser422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteUser422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser422Name
  /** Código de status HTTP 422. */
  status_code: DeleteUser422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type DeleteUser409StatusCode =
  (typeof DeleteUser409StatusCode)[keyof typeof DeleteUser409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser409Name =
  (typeof DeleteUser409Name)[keyof typeof DeleteUser409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type DeleteUser409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser409Name
  /** Código de status HTTP 409. */
  status_code: DeleteUser409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type DeleteUser404StatusCode =
  (typeof DeleteUser404StatusCode)[keyof typeof DeleteUser404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser404Name =
  (typeof DeleteUser404Name)[keyof typeof DeleteUser404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type DeleteUser404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser404Name
  /** Código de status HTTP 404. */
  status_code: DeleteUser404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteUser403StatusCode =
  (typeof DeleteUser403StatusCode)[keyof typeof DeleteUser403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser403Name =
  (typeof DeleteUser403Name)[keyof typeof DeleteUser403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteUser403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser403Name
  /** Código de status HTTP 403. */
  status_code: DeleteUser403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteUser401StatusCode =
  (typeof DeleteUser401StatusCode)[keyof typeof DeleteUser401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser401Name =
  (typeof DeleteUser401Name)[keyof typeof DeleteUser401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteUser401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser401Name
  /** Código de status HTTP 401. */
  status_code: DeleteUser401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteUser400StatusCode =
  (typeof DeleteUser400StatusCode)[keyof typeof DeleteUser400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser400Name =
  (typeof DeleteUser400Name)[keyof typeof DeleteUser400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteUser400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser400Name
  /** Código de status HTTP 400. */
  status_code: DeleteUser400StatusCode
}

/**
 * Usuário excluído com sucesso
 */
export type DeleteUser200 = {
  message: string
  userId: string
}

/**
 * Código de status HTTP 500.
 */
export type UserList500StatusCode =
  (typeof UserList500StatusCode)[keyof typeof UserList500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserList500Name =
  (typeof UserList500Name)[keyof typeof UserList500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserList500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserList500Name
  /** Código de status HTTP 500. */
  status_code: UserList500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UserList422StatusCode =
  (typeof UserList422StatusCode)[keyof typeof UserList422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UserList422Name =
  (typeof UserList422Name)[keyof typeof UserList422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UserList422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UserList422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UserList422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UserList422Name
  /** Código de status HTTP 422. */
  status_code: UserList422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserList403StatusCode =
  (typeof UserList403StatusCode)[keyof typeof UserList403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserList403Name =
  (typeof UserList403Name)[keyof typeof UserList403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserList403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserList403Name
  /** Código de status HTTP 403. */
  status_code: UserList403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserList401StatusCode =
  (typeof UserList401StatusCode)[keyof typeof UserList401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserList401Name =
  (typeof UserList401Name)[keyof typeof UserList401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserList401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserList401Name
  /** Código de status HTTP 401. */
  status_code: UserList401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserList400StatusCode =
  (typeof UserList400StatusCode)[keyof typeof UserList400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserList400Name =
  (typeof UserList400Name)[keyof typeof UserList400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserList400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserList400Name
  /** Código de status HTTP 400. */
  status_code: UserList400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type UserList200UsersItemRole =
  (typeof UserList200UsersItemRole)[keyof typeof UserList200UsersItemRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserList200UsersItemRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserList200UsersItem = {
  /** Indica se a conta do usuário está ativa (true) ou inativa (false). Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Pode ser nulo. Campo opcional.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /** Data e hora de criação do registro no formato ISO 8601. Campo obrigatório. */
  createdAt: string
  /** Endereço de email do usuário. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /**
   * Nome completo do usuário. Pode ser nulo se não fornecido. Campo obrigatório.
   * @nullable
   */
  name: string | null
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: UserList200UsersItemRole
}

/**
 * Lista de usuários recuperada com sucesso
 */
export type UserList200 = {
  /** Número da página atual da consulta. Campo obrigatório. */
  currentPage: number
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  /** Número total de páginas disponíveis com base no tamanho da página. Campo obrigatório. */
  totalPages: number
  /** Lista de usuários recuperados conforme os filtros aplicados. */
  users: UserList200UsersItem[]
}

export type UserListRole = (typeof UserListRole)[keyof typeof UserListRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserListRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserListParams = {
  /**
   * Número da página para paginação, começando em 1. Valor padrão: 1. Campo opcional.
   */
  page?: string
  /**
   * Quantidade de registros por página. Valor padrão: 1000. Campo opcional.
   */
  pageSize?: string
  /**
   * Termo de busca para filtrar usuários por nome ou email. Campo opcional.
   */
  search?: string
  /**
   * Função do usuário no sistema para filtrar resultados. Aceita apenas: admin, user ou dev. Campo opcional.
   */
  role?: UserListRole
  /**
   * Status da conta do usuário para filtrar resultados. Valores aceitos: "true" (ativo) ou "false" (inativo). Campo opcional.
   */
  accountStatus?: string
}

/**
 * Código de status HTTP 500.
 */
export type UserCreate500StatusCode =
  (typeof UserCreate500StatusCode)[keyof typeof UserCreate500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate500Name =
  (typeof UserCreate500Name)[keyof typeof UserCreate500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserCreate500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate500Name
  /** Código de status HTTP 500. */
  status_code: UserCreate500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UserCreate422StatusCode =
  (typeof UserCreate422StatusCode)[keyof typeof UserCreate422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate422Name =
  (typeof UserCreate422Name)[keyof typeof UserCreate422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UserCreate422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UserCreate422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UserCreate422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate422Name
  /** Código de status HTTP 422. */
  status_code: UserCreate422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type UserCreate409StatusCode =
  (typeof UserCreate409StatusCode)[keyof typeof UserCreate409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate409Name =
  (typeof UserCreate409Name)[keyof typeof UserCreate409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type UserCreate409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate409Name
  /** Código de status HTTP 409. */
  status_code: UserCreate409StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserCreate403StatusCode =
  (typeof UserCreate403StatusCode)[keyof typeof UserCreate403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate403Name =
  (typeof UserCreate403Name)[keyof typeof UserCreate403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserCreate403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate403Name
  /** Código de status HTTP 403. */
  status_code: UserCreate403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserCreate401StatusCode =
  (typeof UserCreate401StatusCode)[keyof typeof UserCreate401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate401Name =
  (typeof UserCreate401Name)[keyof typeof UserCreate401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserCreate401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate401Name
  /** Código de status HTTP 401. */
  status_code: UserCreate401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserCreate400StatusCode =
  (typeof UserCreate400StatusCode)[keyof typeof UserCreate400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate400Name =
  (typeof UserCreate400Name)[keyof typeof UserCreate400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserCreate400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate400Name
  /** Código de status HTTP 400. */
  status_code: UserCreate400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type UserCreate201UserRole =
  (typeof UserCreate201UserRole)[keyof typeof UserCreate201UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate201UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserCreate201User = {
  /** Indica se a conta do usuário está ativa no sistema. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Campo opcional.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /** Email do usuário para acesso ao sistema. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /** Nome completo do usuário com nome e sobrenome. Campo obrigatório. */
  name: string
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: UserCreate201UserRole
}

/**
 * Detalhes do usuário criado com sucesso
 */
export type UserCreate201 = {
  /** Mensagem informativa sobre o resultado da operação de criação do usuário. Campo obrigatório. */
  message: string
  user: UserCreate201User
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Valor padrão: user. Campo obrigatório.
 */
export type UserCreateBodyRole =
  (typeof UserCreateBodyRole)[keyof typeof UserCreateBodyRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreateBodyRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserCreateBody = {
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Pode ser nulo. Campo opcional.
   * @minLength 1
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /**
   * Email do usuário para acesso ao sistema. Deve ser um endereço de email válido entre 3 e 254 caracteres. Campo obrigatório.
   * @minLength 3
   * @maxLength 254
   */
  email: string
  /**
   * Nome completo do usuário. String com nome e sobrenome separados por espaço. Campo obrigatório.
   * @minLength 2
   * @maxLength 100
   * @pattern ^\S+\s+\S+
   */
  name: string
  /**
   * Senha do usuário para autenticação no sistema. Deve ter pelo menos 8 caracteres, incluindo letras maiúsculas, minúsculas, números e símbolos. Campo obrigatório.
   * @minLength 8
   * @maxLength 100
   */
  password: string
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Valor padrão: user. Campo obrigatório. */
  role?: UserCreateBodyRole
}

/**
 * Código de status HTTP 500.
 */
export type UserMe500StatusCode =
  (typeof UserMe500StatusCode)[keyof typeof UserMe500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe500Name = (typeof UserMe500Name)[keyof typeof UserMe500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserMe500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe500Name
  /** Código de status HTTP 500. */
  status_code: UserMe500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserMe403StatusCode =
  (typeof UserMe403StatusCode)[keyof typeof UserMe403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe403Name = (typeof UserMe403Name)[keyof typeof UserMe403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserMe403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe403Name
  /** Código de status HTTP 403. */
  status_code: UserMe403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserMe401StatusCode =
  (typeof UserMe401StatusCode)[keyof typeof UserMe401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe401Name = (typeof UserMe401Name)[keyof typeof UserMe401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserMe401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe401Name
  /** Código de status HTTP 401. */
  status_code: UserMe401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserMe400StatusCode =
  (typeof UserMe400StatusCode)[keyof typeof UserMe400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe400Name = (typeof UserMe400Name)[keyof typeof UserMe400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserMe400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe400Name
  /** Código de status HTTP 400. */
  status_code: UserMe400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type UserMe200UserRole =
  (typeof UserMe200UserRole)[keyof typeof UserMe200UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe200UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserMe200User = {
  /** Indica se a conta do usuário está ativa. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário para identificação fiscal. Deve conter 11 dígitos numéricos sem pontuação. Pode ser nulo. Campo opcional.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /** Endereço de email do usuário. Email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /**
   * Nome completo do usuário. String de texto. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  name: string | null
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: UserMe200UserRole
}

/**
 * Dados do usuário recuperados com sucesso
 */
export type UserMe200 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  user: UserMe200User
}

/**
 * Código de status HTTP 500.
 */
export type RefreshUserSession500StatusCode =
  (typeof RefreshUserSession500StatusCode)[keyof typeof RefreshUserSession500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession500Name =
  (typeof RefreshUserSession500Name)[keyof typeof RefreshUserSession500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type RefreshUserSession500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession500Name
  /** Código de status HTTP 500. */
  status_code: RefreshUserSession500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type RefreshUserSession403StatusCode =
  (typeof RefreshUserSession403StatusCode)[keyof typeof RefreshUserSession403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession403Name =
  (typeof RefreshUserSession403Name)[keyof typeof RefreshUserSession403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type RefreshUserSession403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession403Name
  /** Código de status HTTP 403. */
  status_code: RefreshUserSession403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type RefreshUserSession401StatusCode =
  (typeof RefreshUserSession401StatusCode)[keyof typeof RefreshUserSession401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession401Name =
  (typeof RefreshUserSession401Name)[keyof typeof RefreshUserSession401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type RefreshUserSession401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession401Name
  /** Código de status HTTP 401. */
  status_code: RefreshUserSession401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type RefreshUserSession400StatusCode =
  (typeof RefreshUserSession400StatusCode)[keyof typeof RefreshUserSession400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession400Name =
  (typeof RefreshUserSession400Name)[keyof typeof RefreshUserSession400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type RefreshUserSession400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession400Name
  /** Código de status HTTP 400. */
  status_code: RefreshUserSession400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type RefreshUserSession201UserRole =
  (typeof RefreshUserSession201UserRole)[keyof typeof RefreshUserSession201UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession201UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type RefreshUserSession201User = {
  /** Indica se a conta do usuário está ativa. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário. String no formato de CPF brasileiro. Pode ser nulo. Campo opcional.
   * @nullable
   */
  cpf: string | null
  /** Email do usuário. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /** Nome completo do usuário. String. Campo obrigatório. */
  name: string
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: RefreshUserSession201UserRole
}

/**
 * Sessão atualizada com sucesso
 */
export type RefreshUserSession201 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  /** Identificador único da sessão no formato UUID v4. Campo obrigatório. */
  sessionId: string
  user: RefreshUserSession201User
}

/**
 * Código de status HTTP 500.
 */
export type LogoutUser500StatusCode =
  (typeof LogoutUser500StatusCode)[keyof typeof LogoutUser500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser500Name =
  (typeof LogoutUser500Name)[keyof typeof LogoutUser500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type LogoutUser500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser500Name
  /** Código de status HTTP 500. */
  status_code: LogoutUser500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type LogoutUser403StatusCode =
  (typeof LogoutUser403StatusCode)[keyof typeof LogoutUser403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser403Name =
  (typeof LogoutUser403Name)[keyof typeof LogoutUser403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type LogoutUser403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser403Name
  /** Código de status HTTP 403. */
  status_code: LogoutUser403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type LogoutUser401StatusCode =
  (typeof LogoutUser401StatusCode)[keyof typeof LogoutUser401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser401Name =
  (typeof LogoutUser401Name)[keyof typeof LogoutUser401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type LogoutUser401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser401Name
  /** Código de status HTTP 401. */
  status_code: LogoutUser401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type LogoutUser400StatusCode =
  (typeof LogoutUser400StatusCode)[keyof typeof LogoutUser400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser400Name =
  (typeof LogoutUser400Name)[keyof typeof LogoutUser400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type LogoutUser400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser400Name
  /** Código de status HTTP 400. */
  status_code: LogoutUser400StatusCode
}

/**
 * Usuário desconectado com sucesso
 */
export type LogoutUser200 = {
  message: string
}

/**
 * Código de status HTTP 500.
 */
export type AuthResetPassword500StatusCode =
  (typeof AuthResetPassword500StatusCode)[keyof typeof AuthResetPassword500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthResetPassword500Name =
  (typeof AuthResetPassword500Name)[keyof typeof AuthResetPassword500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type AuthResetPassword500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: AuthResetPassword500Name
  /** Código de status HTTP 500. */
  status_code: AuthResetPassword500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type AuthResetPassword422StatusCode =
  (typeof AuthResetPassword422StatusCode)[keyof typeof AuthResetPassword422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthResetPassword422Name =
  (typeof AuthResetPassword422Name)[keyof typeof AuthResetPassword422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type AuthResetPassword422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type AuthResetPassword422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: AuthResetPassword422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: AuthResetPassword422Name
  /** Código de status HTTP 422. */
  status_code: AuthResetPassword422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type AuthResetPassword404StatusCode =
  (typeof AuthResetPassword404StatusCode)[keyof typeof AuthResetPassword404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthResetPassword404Name =
  (typeof AuthResetPassword404Name)[keyof typeof AuthResetPassword404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type AuthResetPassword404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: AuthResetPassword404Name
  /** Código de status HTTP 404. */
  status_code: AuthResetPassword404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type AuthResetPassword403StatusCode =
  (typeof AuthResetPassword403StatusCode)[keyof typeof AuthResetPassword403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthResetPassword403Name =
  (typeof AuthResetPassword403Name)[keyof typeof AuthResetPassword403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type AuthResetPassword403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: AuthResetPassword403Name
  /** Código de status HTTP 403. */
  status_code: AuthResetPassword403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type AuthResetPassword401StatusCode =
  (typeof AuthResetPassword401StatusCode)[keyof typeof AuthResetPassword401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthResetPassword401Name =
  (typeof AuthResetPassword401Name)[keyof typeof AuthResetPassword401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthResetPassword401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type AuthResetPassword401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: AuthResetPassword401Name
  /** Código de status HTTP 401. */
  status_code: AuthResetPassword401StatusCode
}

/**
 * Senha redefinida com sucesso
 */
export type AuthResetPassword200 = {
  /** Mensagem informativa sobre o resultado da redefinição de senha. Campo obrigatório. */
  message: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  userId: string
}

export type AuthResetPasswordBody = {
  /**
   * Nova senha do usuário. Deve conter pelo menos 8 caracteres, uma letra maiúscula, uma letra minúscula, um número e um símbolo. Campo obrigatório.
   * @minLength 8
   * @maxLength 100
   */
  password: string
}

/**
 * Código de status HTTP 500.
 */
export type AuthForgotPassword500StatusCode =
  (typeof AuthForgotPassword500StatusCode)[keyof typeof AuthForgotPassword500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthForgotPassword500Name =
  (typeof AuthForgotPassword500Name)[keyof typeof AuthForgotPassword500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type AuthForgotPassword500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: AuthForgotPassword500Name
  /** Código de status HTTP 500. */
  status_code: AuthForgotPassword500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type AuthForgotPassword422StatusCode =
  (typeof AuthForgotPassword422StatusCode)[keyof typeof AuthForgotPassword422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthForgotPassword422Name =
  (typeof AuthForgotPassword422Name)[keyof typeof AuthForgotPassword422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type AuthForgotPassword422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type AuthForgotPassword422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: AuthForgotPassword422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: AuthForgotPassword422Name
  /** Código de status HTTP 422. */
  status_code: AuthForgotPassword422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type AuthForgotPassword403StatusCode =
  (typeof AuthForgotPassword403StatusCode)[keyof typeof AuthForgotPassword403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthForgotPassword403Name =
  (typeof AuthForgotPassword403Name)[keyof typeof AuthForgotPassword403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type AuthForgotPassword403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: AuthForgotPassword403Name
  /** Código de status HTTP 403. */
  status_code: AuthForgotPassword403StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type AuthForgotPassword400StatusCode =
  (typeof AuthForgotPassword400StatusCode)[keyof typeof AuthForgotPassword400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthForgotPassword400Name =
  (typeof AuthForgotPassword400Name)[keyof typeof AuthForgotPassword400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthForgotPassword400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type AuthForgotPassword400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: AuthForgotPassword400Name
  /** Código de status HTTP 400. */
  status_code: AuthForgotPassword400StatusCode
}

/**
 * Email para redefinição de senha enviado com sucesso
 */
export type AuthForgotPassword201 = {
  /** Mensagem informativa sobre o envio do email de redefinição de senha. Campo obrigatório. */
  message: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  userId: string
}

export type AuthForgotPasswordBody = {
  /**
   * Email do usuário para envio do token de redefinição de senha. Deve ser um endereço de email válido entre 3 e 254 caracteres. Campo obrigatório.
   * @minLength 3
   * @maxLength 254
   */
  email: string
}

/**
 * Código de status HTTP 500.
 */
export type AuthLoginCredential500StatusCode =
  (typeof AuthLoginCredential500StatusCode)[keyof typeof AuthLoginCredential500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthLoginCredential500Name =
  (typeof AuthLoginCredential500Name)[keyof typeof AuthLoginCredential500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type AuthLoginCredential500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: AuthLoginCredential500Name
  /** Código de status HTTP 500. */
  status_code: AuthLoginCredential500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type AuthLoginCredential422StatusCode =
  (typeof AuthLoginCredential422StatusCode)[keyof typeof AuthLoginCredential422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthLoginCredential422Name =
  (typeof AuthLoginCredential422Name)[keyof typeof AuthLoginCredential422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type AuthLoginCredential422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type AuthLoginCredential422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: AuthLoginCredential422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: AuthLoginCredential422Name
  /** Código de status HTTP 422. */
  status_code: AuthLoginCredential422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type AuthLoginCredential403StatusCode =
  (typeof AuthLoginCredential403StatusCode)[keyof typeof AuthLoginCredential403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthLoginCredential403Name =
  (typeof AuthLoginCredential403Name)[keyof typeof AuthLoginCredential403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type AuthLoginCredential403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: AuthLoginCredential403Name
  /** Código de status HTTP 403. */
  status_code: AuthLoginCredential403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type AuthLoginCredential401StatusCode =
  (typeof AuthLoginCredential401StatusCode)[keyof typeof AuthLoginCredential401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthLoginCredential401Name =
  (typeof AuthLoginCredential401Name)[keyof typeof AuthLoginCredential401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type AuthLoginCredential401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: AuthLoginCredential401Name
  /** Código de status HTTP 401. */
  status_code: AuthLoginCredential401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type AuthLoginCredential400StatusCode =
  (typeof AuthLoginCredential400StatusCode)[keyof typeof AuthLoginCredential400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type AuthLoginCredential400Name =
  (typeof AuthLoginCredential400Name)[keyof typeof AuthLoginCredential400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type AuthLoginCredential400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: AuthLoginCredential400Name
  /** Código de status HTTP 400. */
  status_code: AuthLoginCredential400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type AuthLoginCredential200UserRole =
  (typeof AuthLoginCredential200UserRole)[keyof typeof AuthLoginCredential200UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthLoginCredential200UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type AuthLoginCredential200User = {
  /** Indica se a conta do usuário está ativa. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * CPF do usuário. Deve conter 11 dígitos numéricos. Pode ser nulo. Campo opcional.
   * @nullable
   * @pattern ^\d{11}$
   */
  cpf: string | null
  /** Email do usuário. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /**
   * Nome completo do usuário. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  name: string | null
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: AuthLoginCredential200UserRole
}

/**
 * Autenticação com credenciais realizada com sucesso
 */
export type AuthLoginCredential200 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  user: AuthLoginCredential200User
}

export type AuthLoginCredentialBody = {
  /**
   * Email do usuário para autenticação. Deve ser um endereço de email válido entre 3 e 254 caracteres. Campo obrigatório.
   * @minLength 3
   * @maxLength 254
   */
  email: string
  /**
   * Senha do usuário para autenticação. Deve conter pelo menos 8 caracteres, uma letra maiúscula, uma letra minúscula, um número e um símbolo. Campo obrigatório.
   * @minLength 8
   * @maxLength 100
   */
  password: string
}

/**
 * Código de status HTTP 500.
 */
export type LoginUserGoogleCallback500StatusCode =
  (typeof LoginUserGoogleCallback500StatusCode)[keyof typeof LoginUserGoogleCallback500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type LoginUserGoogleCallback500Name =
  (typeof LoginUserGoogleCallback500Name)[keyof typeof LoginUserGoogleCallback500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type LoginUserGoogleCallback500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: LoginUserGoogleCallback500Name
  /** Código de status HTTP 500. */
  status_code: LoginUserGoogleCallback500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type LoginUserGoogleCallback422StatusCode =
  (typeof LoginUserGoogleCallback422StatusCode)[keyof typeof LoginUserGoogleCallback422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type LoginUserGoogleCallback422Name =
  (typeof LoginUserGoogleCallback422Name)[keyof typeof LoginUserGoogleCallback422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type LoginUserGoogleCallback422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type LoginUserGoogleCallback422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: LoginUserGoogleCallback422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: LoginUserGoogleCallback422Name
  /** Código de status HTTP 422. */
  status_code: LoginUserGoogleCallback422StatusCode
}

/**
 * Redireciona para o frontend após configurar cookies de sessão
 * @nullable
 */
export type LoginUserGoogleCallback302 =
  | (typeof LoginUserGoogleCallback302)[keyof typeof LoginUserGoogleCallback302]
  | null

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback302 = {
  null: 'null',
} as const

export type LoginUserGoogleCallbackParams = {
  /**
   * Código de autorização do Google
   */
  code?: string
  /**
   * Escopos autorizados pelo usuário
   */
  scope?: string
  /**
   * Índice da conta Google usada na autenticação
   */
  authuser?: string
  /**
   * Tipo de prompt utilizado no login
   */
  prompt?: string
  /**
   * Valor usado para proteção contra CSRF
   */
  state?: string
}

/**
 * Código de status HTTP 500.
 */
export type LoginUserGoogle500StatusCode =
  (typeof LoginUserGoogle500StatusCode)[keyof typeof LoginUserGoogle500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogle500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type LoginUserGoogle500Name =
  (typeof LoginUserGoogle500Name)[keyof typeof LoginUserGoogle500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogle500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type LoginUserGoogle500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: LoginUserGoogle500Name
  /** Código de status HTTP 500. */
  status_code: LoginUserGoogle500StatusCode
}

/**
 * Redirecionamento para o Google OAuth
 * @nullable
 */
export type LoginUserGoogle302 =
  | (typeof LoginUserGoogle302)[keyof typeof LoginUserGoogle302]
  | null

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogle302 = {
  null: 'null',
} as const

/**
 * Código de status HTTP 500.
 */
export type ServerHealthCheck500StatusCode =
  (typeof ServerHealthCheck500StatusCode)[keyof typeof ServerHealthCheck500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ServerHealthCheck500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ServerHealthCheck500Name =
  (typeof ServerHealthCheck500Name)[keyof typeof ServerHealthCheck500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ServerHealthCheck500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ServerHealthCheck500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ServerHealthCheck500Name
  /** Código de status HTTP 500. */
  status_code: ServerHealthCheck500StatusCode
}

/**
 * Server health check
 */
export type ServerHealthCheck200 = {
  message: string
}
