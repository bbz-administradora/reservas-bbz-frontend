/**
 * Generated by orval v7.3.0 🍺
 * Do not edit manually.
 * BBZ App Backend API
 * API documentation for BBZ App Backend
 * OpenAPI spec version: 1.0.0
 */
/**
 * Código de status HTTP 500.
 */
export type GetRoomReservationStats500StatusCode =
  (typeof GetRoomReservationStats500StatusCode)[keyof typeof GetRoomReservationStats500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomReservationStats500Name =
  (typeof GetRoomReservationStats500Name)[keyof typeof GetRoomReservationStats500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetRoomReservationStats500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomReservationStats500Name
  /** Código de status HTTP 500. */
  status_code: GetRoomReservationStats500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetRoomReservationStats403StatusCode =
  (typeof GetRoomReservationStats403StatusCode)[keyof typeof GetRoomReservationStats403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomReservationStats403Name =
  (typeof GetRoomReservationStats403Name)[keyof typeof GetRoomReservationStats403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetRoomReservationStats403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomReservationStats403Name
  /** Código de status HTTP 403. */
  status_code: GetRoomReservationStats403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetRoomReservationStats401StatusCode =
  (typeof GetRoomReservationStats401StatusCode)[keyof typeof GetRoomReservationStats401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomReservationStats401Name =
  (typeof GetRoomReservationStats401Name)[keyof typeof GetRoomReservationStats401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetRoomReservationStats401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomReservationStats401Name
  /** Código de status HTTP 401. */
  status_code: GetRoomReservationStats401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetRoomReservationStats400StatusCode =
  (typeof GetRoomReservationStats400StatusCode)[keyof typeof GetRoomReservationStats400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomReservationStats400Name =
  (typeof GetRoomReservationStats400Name)[keyof typeof GetRoomReservationStats400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomReservationStats400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetRoomReservationStats400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomReservationStats400Name
  /** Código de status HTTP 400. */
  status_code: GetRoomReservationStats400StatusCode
}

export type GetRoomReservationStats200 = {
  /**
   * Array with the three most used start times, from smallest to largest, format HH:mm
   * @minItems 3
   * @maxItems 3
   */
  mostUsedStartTimes: string[]
  /**
   * Start date and time of the next reservation in the format 'dd/MM/yyyy às HH:mm', or null if none
   * @nullable
   */
  nextReservation: string | null
  /**
   * Total number of reservations for the user
   * @minimum 0
   */
  total: number
}

/**
 * Código de status HTTP 500.
 */
export type ListRoomReservations500StatusCode =
  (typeof ListRoomReservations500StatusCode)[keyof typeof ListRoomReservations500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomReservations500Name =
  (typeof ListRoomReservations500Name)[keyof typeof ListRoomReservations500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListRoomReservations500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomReservations500Name
  /** Código de status HTTP 500. */
  status_code: ListRoomReservations500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListRoomReservations422StatusCode =
  (typeof ListRoomReservations422StatusCode)[keyof typeof ListRoomReservations422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomReservations422Name =
  (typeof ListRoomReservations422Name)[keyof typeof ListRoomReservations422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListRoomReservations422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListRoomReservations422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListRoomReservations422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomReservations422Name
  /** Código de status HTTP 422. */
  status_code: ListRoomReservations422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type ListRoomReservations404StatusCode =
  (typeof ListRoomReservations404StatusCode)[keyof typeof ListRoomReservations404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomReservations404Name =
  (typeof ListRoomReservations404Name)[keyof typeof ListRoomReservations404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type ListRoomReservations404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomReservations404Name
  /** Código de status HTTP 404. */
  status_code: ListRoomReservations404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListRoomReservations403StatusCode =
  (typeof ListRoomReservations403StatusCode)[keyof typeof ListRoomReservations403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomReservations403Name =
  (typeof ListRoomReservations403Name)[keyof typeof ListRoomReservations403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListRoomReservations403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomReservations403Name
  /** Código de status HTTP 403. */
  status_code: ListRoomReservations403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListRoomReservations401StatusCode =
  (typeof ListRoomReservations401StatusCode)[keyof typeof ListRoomReservations401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomReservations401Name =
  (typeof ListRoomReservations401Name)[keyof typeof ListRoomReservations401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListRoomReservations401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomReservations401Name
  /** Código de status HTTP 401. */
  status_code: ListRoomReservations401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListRoomReservations400StatusCode =
  (typeof ListRoomReservations400StatusCode)[keyof typeof ListRoomReservations400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomReservations400Name =
  (typeof ListRoomReservations400Name)[keyof typeof ListRoomReservations400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListRoomReservations400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomReservations400Name
  /** Código de status HTTP 400. */
  status_code: ListRoomReservations400StatusCode
}

/**
 * Informações do usuário que fez a reserva
 */
export type ListRoomReservations200ReservationsItemUser = {
  /** E-mail do usuário */
  email: string
  /** Identificador único do usuário */
  id: string
  /** Nome do usuário */
  name: string
}

/**
 * Status atual da reserva
 */
export type ListRoomReservations200ReservationsItemStatus =
  (typeof ListRoomReservations200ReservationsItemStatus)[keyof typeof ListRoomReservations200ReservationsItemStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations200ReservationsItemStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

/**
 * Status do slot
 */
export type ListRoomReservations200ReservationsItemSlotStatus =
  (typeof ListRoomReservations200ReservationsItemSlotStatus)[keyof typeof ListRoomReservations200ReservationsItemSlotStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomReservations200ReservationsItemSlotStatus = {
  pre_reserved: 'pre_reserved',
  reserved: 'reserved',
} as const

/**
 * Informações do slot de tempo reservado (ou null se não houver)
 * @nullable
 */
export type ListRoomReservations200ReservationsItemSlot = {
  /** Data/hora de criação do slot */
  createdAt: string
  /** Identificador único do slot de tempo */
  id: string
  /**
   * Data/hora até quando o slot está pré-reservado (ou null)
   * @nullable
   */
  preReservedUntil: string | null
  /**
   * Intervalo de datas do slot no formato [início, fim]
   * @minItems 2
   * @maxItems 2
   */
  slotRange: string[]
  /** Status do slot */
  status: ListRoomReservations200ReservationsItemSlotStatus
} | null

/**
 * Informações básicas da sala
 */
export type ListRoomReservations200ReservationsItemRoom = {
  /** Identificador único da sala */
  id: string
  /** Nome da sala */
  name: string
}

/**
 * Usuário que cancelou a reserva (ou null)
 * @nullable
 */
export type ListRoomReservations200ReservationsItemCancelledBy = {
  /** ID do usuário que cancelou */
  id: string
  /** Nome do usuário que cancelou */
  name: string
} | null

/**
 * Detalhes completos de uma reserva paginada
 */
export type ListRoomReservations200ReservationsItem = {
  /** Lista de colaboradores da BBZ participantes */
  bbzCollaborators: string[]
  /**
   * Data/hora do cancelamento (ou null)
   * @nullable
   */
  cancelledAt: string | null
  /**
   * Usuário que cancelou a reserva (ou null)
   * @nullable
   */
  cancelledBy: ListRoomReservations200ReservationsItemCancelledBy
  /**
   * Motivo do cancelamento (ou null)
   * @nullable
   */
  cancelReason: string | null
  /**
   * Data/hora do fechamento da reserva (ou null)
   * @nullable
   */
  closedAt: string | null
  /** Data/hora de criação da reserva */
  createdAt: string
  /** Lista de convidados externos participantes */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Informações básicas da sala */
  room: ListRoomReservations200ReservationsItemRoom
  /**
   * Informações do slot de tempo reservado (ou null se não houver)
   * @nullable
   */
  slot: ListRoomReservations200ReservationsItemSlot
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Status atual da reserva */
  status: ListRoomReservations200ReservationsItemStatus
  /** Informações do usuário que fez a reserva */
  user: ListRoomReservations200ReservationsItemUser
}

/**
 * Resposta paginada contendo reservas de sala
 */
export type ListRoomReservations200 = {
  /** Número da página atual */
  currentPage: number
  /** Lista de reservas encontradas na página atual */
  reservations: ListRoomReservations200ReservationsItem[]
  /** Número total de reservas encontradas para o filtro */
  totalCount: number
  /** Número total de páginas disponíveis */
  totalPages: number
}

export type ListRoomReservationsParams = {
  /**
   * Número da página para paginação, começando em 1 (padrão: 1)
   */
  page?: string
  /**
   * Quantidade de resultados por página, entre 1 e 1000 (padrão: 1000)
   */
  pageSize?: string
}

/**
 * Código de status HTTP 500.
 */
export type CancelRoomReservation500StatusCode =
  (typeof CancelRoomReservation500StatusCode)[keyof typeof CancelRoomReservation500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelRoomReservation500Name =
  (typeof CancelRoomReservation500Name)[keyof typeof CancelRoomReservation500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CancelRoomReservation500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CancelRoomReservation500Name
  /** Código de status HTTP 500. */
  status_code: CancelRoomReservation500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CancelRoomReservation422StatusCode =
  (typeof CancelRoomReservation422StatusCode)[keyof typeof CancelRoomReservation422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelRoomReservation422Name =
  (typeof CancelRoomReservation422Name)[keyof typeof CancelRoomReservation422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CancelRoomReservation422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CancelRoomReservation422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CancelRoomReservation422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CancelRoomReservation422Name
  /** Código de status HTTP 422. */
  status_code: CancelRoomReservation422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CancelRoomReservation404StatusCode =
  (typeof CancelRoomReservation404StatusCode)[keyof typeof CancelRoomReservation404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelRoomReservation404Name =
  (typeof CancelRoomReservation404Name)[keyof typeof CancelRoomReservation404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CancelRoomReservation404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CancelRoomReservation404Name
  /** Código de status HTTP 404. */
  status_code: CancelRoomReservation404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CancelRoomReservation403StatusCode =
  (typeof CancelRoomReservation403StatusCode)[keyof typeof CancelRoomReservation403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelRoomReservation403Name =
  (typeof CancelRoomReservation403Name)[keyof typeof CancelRoomReservation403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CancelRoomReservation403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CancelRoomReservation403Name
  /** Código de status HTTP 403. */
  status_code: CancelRoomReservation403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CancelRoomReservation401StatusCode =
  (typeof CancelRoomReservation401StatusCode)[keyof typeof CancelRoomReservation401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelRoomReservation401Name =
  (typeof CancelRoomReservation401Name)[keyof typeof CancelRoomReservation401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CancelRoomReservation401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CancelRoomReservation401Name
  /** Código de status HTTP 401. */
  status_code: CancelRoomReservation401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CancelRoomReservation400StatusCode =
  (typeof CancelRoomReservation400StatusCode)[keyof typeof CancelRoomReservation400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CancelRoomReservation400Name =
  (typeof CancelRoomReservation400Name)[keyof typeof CancelRoomReservation400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CancelRoomReservation400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CancelRoomReservation400Name
  /** Código de status HTTP 400. */
  status_code: CancelRoomReservation400StatusCode
}

/**
 * Status atual da reserva
 */
export type CancelRoomReservation200ReservationStatus =
  (typeof CancelRoomReservation200ReservationStatus)[keyof typeof CancelRoomReservation200ReservationStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CancelRoomReservation200ReservationStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

export type CancelRoomReservation200Reservation = {
  /** Lista de colaboradores da BBZ */
  bbzCollaborators: string[]
  /** Data e hora do cancelamento da reserva */
  cancelledAt: string
  /** Identificador único do usuário que cancelou a reserva */
  cancelledBy: string
  /** Motivo do cancelamento */
  cancelReason: string
  /** Lista de convidados externos */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Identificador único da sala */
  roomId: string
  /** Identificador único do slot de tempo */
  roomSlotId: string
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Status atual da reserva */
  status: CancelRoomReservation200ReservationStatus
  /** Identificador único do usuário que fez a reserva */
  userId: string
}

export type CancelRoomReservation200 = {
  /** Mensagem de sucesso */
  message: string
  reservation: CancelRoomReservation200Reservation
}

/**
 * Corpo da requisição para cancelar uma reserva
 */
export type CancelRoomReservationBody = {
  /**
   * Motivo do cancelamento da reserva
   * @minLength 3
   * @maxLength 500
   */
  cancelReason: string
  /** Identificador único da reserva a ser cancelada */
  id?: string
  /** Identificador único do slot de tempo associado à reserva */
  roomSlotId?: string
}

/**
 * Código de status HTTP 500.
 */
export type CloseRoomReservation500StatusCode =
  (typeof CloseRoomReservation500StatusCode)[keyof typeof CloseRoomReservation500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseRoomReservation500Name =
  (typeof CloseRoomReservation500Name)[keyof typeof CloseRoomReservation500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CloseRoomReservation500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CloseRoomReservation500Name
  /** Código de status HTTP 500. */
  status_code: CloseRoomReservation500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CloseRoomReservation422StatusCode =
  (typeof CloseRoomReservation422StatusCode)[keyof typeof CloseRoomReservation422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseRoomReservation422Name =
  (typeof CloseRoomReservation422Name)[keyof typeof CloseRoomReservation422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CloseRoomReservation422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CloseRoomReservation422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CloseRoomReservation422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CloseRoomReservation422Name
  /** Código de status HTTP 422. */
  status_code: CloseRoomReservation422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CloseRoomReservation404StatusCode =
  (typeof CloseRoomReservation404StatusCode)[keyof typeof CloseRoomReservation404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseRoomReservation404Name =
  (typeof CloseRoomReservation404Name)[keyof typeof CloseRoomReservation404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CloseRoomReservation404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CloseRoomReservation404Name
  /** Código de status HTTP 404. */
  status_code: CloseRoomReservation404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CloseRoomReservation403StatusCode =
  (typeof CloseRoomReservation403StatusCode)[keyof typeof CloseRoomReservation403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseRoomReservation403Name =
  (typeof CloseRoomReservation403Name)[keyof typeof CloseRoomReservation403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CloseRoomReservation403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CloseRoomReservation403Name
  /** Código de status HTTP 403. */
  status_code: CloseRoomReservation403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CloseRoomReservation401StatusCode =
  (typeof CloseRoomReservation401StatusCode)[keyof typeof CloseRoomReservation401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseRoomReservation401Name =
  (typeof CloseRoomReservation401Name)[keyof typeof CloseRoomReservation401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CloseRoomReservation401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CloseRoomReservation401Name
  /** Código de status HTTP 401. */
  status_code: CloseRoomReservation401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CloseRoomReservation400StatusCode =
  (typeof CloseRoomReservation400StatusCode)[keyof typeof CloseRoomReservation400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CloseRoomReservation400Name =
  (typeof CloseRoomReservation400Name)[keyof typeof CloseRoomReservation400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CloseRoomReservation400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CloseRoomReservation400Name
  /** Código de status HTTP 400. */
  status_code: CloseRoomReservation400StatusCode
}

/**
 * Status atual da reserva
 */
export type CloseRoomReservation200ReservationStatus =
  (typeof CloseRoomReservation200ReservationStatus)[keyof typeof CloseRoomReservation200ReservationStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CloseRoomReservation200ReservationStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

export type CloseRoomReservation200Reservation = {
  /** Lista de colaboradores da BBZ */
  bbzCollaborators: string[]
  /** Data e hora do fechamento da reserva */
  closedAt: string
  /** Lista de convidados externos */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Identificador único da sala */
  roomId: string
  /** Identificador único do slot de tempo */
  roomSlotId: string
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Status atual da reserva */
  status: CloseRoomReservation200ReservationStatus
  /** Identificador único do usuário que fez a reserva */
  userId: string
}

export type CloseRoomReservation200 = {
  /** Mensagem de sucesso */
  message: string
  reservation: CloseRoomReservation200Reservation
}

/**
 * Corpo da requisição para fechar uma reserva (atualmente vazio, mas requerido para o formato PATCH)
 */
export type CloseRoomReservationBody = {
  /** Identificador único da reserva a ser fechada */
  id?: string
  /** Identificador único do slot de tempo associado à reserva */
  roomSlotId?: string
}

/**
 * Código de status HTTP 500.
 */
export type CreateRoomReservation500StatusCode =
  (typeof CreateRoomReservation500StatusCode)[keyof typeof CreateRoomReservation500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation500Name =
  (typeof CreateRoomReservation500Name)[keyof typeof CreateRoomReservation500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CreateRoomReservation500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation500Name
  /** Código de status HTTP 500. */
  status_code: CreateRoomReservation500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CreateRoomReservation422StatusCode =
  (typeof CreateRoomReservation422StatusCode)[keyof typeof CreateRoomReservation422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation422Name =
  (typeof CreateRoomReservation422Name)[keyof typeof CreateRoomReservation422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CreateRoomReservation422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CreateRoomReservation422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CreateRoomReservation422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation422Name
  /** Código de status HTTP 422. */
  status_code: CreateRoomReservation422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type CreateRoomReservation409StatusCode =
  (typeof CreateRoomReservation409StatusCode)[keyof typeof CreateRoomReservation409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation409Name =
  (typeof CreateRoomReservation409Name)[keyof typeof CreateRoomReservation409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type CreateRoomReservation409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation409Name
  /** Código de status HTTP 409. */
  status_code: CreateRoomReservation409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CreateRoomReservation404StatusCode =
  (typeof CreateRoomReservation404StatusCode)[keyof typeof CreateRoomReservation404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation404Name =
  (typeof CreateRoomReservation404Name)[keyof typeof CreateRoomReservation404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CreateRoomReservation404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation404Name
  /** Código de status HTTP 404. */
  status_code: CreateRoomReservation404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CreateRoomReservation403StatusCode =
  (typeof CreateRoomReservation403StatusCode)[keyof typeof CreateRoomReservation403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation403Name =
  (typeof CreateRoomReservation403Name)[keyof typeof CreateRoomReservation403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CreateRoomReservation403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation403Name
  /** Código de status HTTP 403. */
  status_code: CreateRoomReservation403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CreateRoomReservation401StatusCode =
  (typeof CreateRoomReservation401StatusCode)[keyof typeof CreateRoomReservation401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation401Name =
  (typeof CreateRoomReservation401Name)[keyof typeof CreateRoomReservation401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CreateRoomReservation401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation401Name
  /** Código de status HTTP 401. */
  status_code: CreateRoomReservation401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CreateRoomReservation400StatusCode =
  (typeof CreateRoomReservation400StatusCode)[keyof typeof CreateRoomReservation400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomReservation400Name =
  (typeof CreateRoomReservation400Name)[keyof typeof CreateRoomReservation400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CreateRoomReservation400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomReservation400Name
  /** Código de status HTTP 400. */
  status_code: CreateRoomReservation400StatusCode
}

/**
 * Status atual da reserva
 */
export type CreateRoomReservation201ReservationsItemStatus =
  (typeof CreateRoomReservation201ReservationsItemStatus)[keyof typeof CreateRoomReservation201ReservationsItemStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomReservation201ReservationsItemStatus = {
  reserved: 'reserved',
  cancelled: 'cancelled',
  closed: 'closed',
} as const

export type CreateRoomReservation201ReservationsItem = {
  /** Lista de colaboradores da BBZ */
  bbzCollaborators: string[]
  /** Lista de convidados externos */
  externalGuests: string[]
  /** Identificador único da reserva */
  id: string
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira: boolean
  /** Identificador único da sala */
  roomId: string
  /** Identificador único do slot de tempo */
  roomSlotId: string
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Status atual da reserva */
  status: CreateRoomReservation201ReservationsItemStatus
  /** Identificador único do usuário que fez a reserva */
  userId: string
}

export type CreateRoomReservation201 = {
  /** Mensagem de sucesso */
  message: string
  reservations: CreateRoomReservation201ReservationsItem[]
}

export type CreateRoomReservationBody = {
  /** Lista de colaboradores da BBZ que participarão da reunião */
  bbzCollaborators?: string[]
  /** Lista de convidados externos que participarão da reunião */
  externalGuests?: string[]
  /** Indica se a reserva necessita de serviço de copeira */
  needsCopeira?: boolean
  /** Identificador único da sala a ser reservada */
  roomId: string
  /**
   * Lista de identificadores únicos dos slots de tempo pré-reservados
   * @minItems 1
   */
  roomSlotIds: string[]
}

/**
 * Código de status HTTP 500.
 */
export type DeleteRoomSlotPreReserve500StatusCode =
  (typeof DeleteRoomSlotPreReserve500StatusCode)[keyof typeof DeleteRoomSlotPreReserve500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoomSlotPreReserve500Name =
  (typeof DeleteRoomSlotPreReserve500Name)[keyof typeof DeleteRoomSlotPreReserve500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteRoomSlotPreReserve500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoomSlotPreReserve500Name
  /** Código de status HTTP 500. */
  status_code: DeleteRoomSlotPreReserve500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteRoomSlotPreReserve422StatusCode =
  (typeof DeleteRoomSlotPreReserve422StatusCode)[keyof typeof DeleteRoomSlotPreReserve422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoomSlotPreReserve422Name =
  (typeof DeleteRoomSlotPreReserve422Name)[keyof typeof DeleteRoomSlotPreReserve422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteRoomSlotPreReserve422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteRoomSlotPreReserve422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteRoomSlotPreReserve422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoomSlotPreReserve422Name
  /** Código de status HTTP 422. */
  status_code: DeleteRoomSlotPreReserve422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type DeleteRoomSlotPreReserve404StatusCode =
  (typeof DeleteRoomSlotPreReserve404StatusCode)[keyof typeof DeleteRoomSlotPreReserve404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoomSlotPreReserve404Name =
  (typeof DeleteRoomSlotPreReserve404Name)[keyof typeof DeleteRoomSlotPreReserve404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type DeleteRoomSlotPreReserve404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoomSlotPreReserve404Name
  /** Código de status HTTP 404. */
  status_code: DeleteRoomSlotPreReserve404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteRoomSlotPreReserve403StatusCode =
  (typeof DeleteRoomSlotPreReserve403StatusCode)[keyof typeof DeleteRoomSlotPreReserve403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoomSlotPreReserve403Name =
  (typeof DeleteRoomSlotPreReserve403Name)[keyof typeof DeleteRoomSlotPreReserve403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteRoomSlotPreReserve403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoomSlotPreReserve403Name
  /** Código de status HTTP 403. */
  status_code: DeleteRoomSlotPreReserve403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteRoomSlotPreReserve401StatusCode =
  (typeof DeleteRoomSlotPreReserve401StatusCode)[keyof typeof DeleteRoomSlotPreReserve401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoomSlotPreReserve401Name =
  (typeof DeleteRoomSlotPreReserve401Name)[keyof typeof DeleteRoomSlotPreReserve401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteRoomSlotPreReserve401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoomSlotPreReserve401Name
  /** Código de status HTTP 401. */
  status_code: DeleteRoomSlotPreReserve401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteRoomSlotPreReserve400StatusCode =
  (typeof DeleteRoomSlotPreReserve400StatusCode)[keyof typeof DeleteRoomSlotPreReserve400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoomSlotPreReserve400Name =
  (typeof DeleteRoomSlotPreReserve400Name)[keyof typeof DeleteRoomSlotPreReserve400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoomSlotPreReserve400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteRoomSlotPreReserve400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoomSlotPreReserve400Name
  /** Código de status HTTP 400. */
  status_code: DeleteRoomSlotPreReserve400StatusCode
}

export type DeleteRoomSlotPreReserve200 = {
  /** Mensagem de sucesso */
  message: string
  /** Identificador do slot deletado */
  slotId: string
}

/**
 * Código de status HTTP 500.
 */
export type CreateRoomSlotPreReserve500StatusCode =
  (typeof CreateRoomSlotPreReserve500StatusCode)[keyof typeof CreateRoomSlotPreReserve500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve500Name =
  (typeof CreateRoomSlotPreReserve500Name)[keyof typeof CreateRoomSlotPreReserve500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CreateRoomSlotPreReserve500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve500Name
  /** Código de status HTTP 500. */
  status_code: CreateRoomSlotPreReserve500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CreateRoomSlotPreReserve422StatusCode =
  (typeof CreateRoomSlotPreReserve422StatusCode)[keyof typeof CreateRoomSlotPreReserve422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve422Name =
  (typeof CreateRoomSlotPreReserve422Name)[keyof typeof CreateRoomSlotPreReserve422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CreateRoomSlotPreReserve422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CreateRoomSlotPreReserve422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CreateRoomSlotPreReserve422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve422Name
  /** Código de status HTTP 422. */
  status_code: CreateRoomSlotPreReserve422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type CreateRoomSlotPreReserve409StatusCode =
  (typeof CreateRoomSlotPreReserve409StatusCode)[keyof typeof CreateRoomSlotPreReserve409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve409Name =
  (typeof CreateRoomSlotPreReserve409Name)[keyof typeof CreateRoomSlotPreReserve409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type CreateRoomSlotPreReserve409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve409Name
  /** Código de status HTTP 409. */
  status_code: CreateRoomSlotPreReserve409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type CreateRoomSlotPreReserve404StatusCode =
  (typeof CreateRoomSlotPreReserve404StatusCode)[keyof typeof CreateRoomSlotPreReserve404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve404Name =
  (typeof CreateRoomSlotPreReserve404Name)[keyof typeof CreateRoomSlotPreReserve404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type CreateRoomSlotPreReserve404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve404Name
  /** Código de status HTTP 404. */
  status_code: CreateRoomSlotPreReserve404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CreateRoomSlotPreReserve403StatusCode =
  (typeof CreateRoomSlotPreReserve403StatusCode)[keyof typeof CreateRoomSlotPreReserve403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve403Name =
  (typeof CreateRoomSlotPreReserve403Name)[keyof typeof CreateRoomSlotPreReserve403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CreateRoomSlotPreReserve403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve403Name
  /** Código de status HTTP 403. */
  status_code: CreateRoomSlotPreReserve403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CreateRoomSlotPreReserve401StatusCode =
  (typeof CreateRoomSlotPreReserve401StatusCode)[keyof typeof CreateRoomSlotPreReserve401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve401Name =
  (typeof CreateRoomSlotPreReserve401Name)[keyof typeof CreateRoomSlotPreReserve401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CreateRoomSlotPreReserve401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve401Name
  /** Código de status HTTP 401. */
  status_code: CreateRoomSlotPreReserve401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CreateRoomSlotPreReserve400StatusCode =
  (typeof CreateRoomSlotPreReserve400StatusCode)[keyof typeof CreateRoomSlotPreReserve400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoomSlotPreReserve400Name =
  (typeof CreateRoomSlotPreReserve400Name)[keyof typeof CreateRoomSlotPreReserve400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CreateRoomSlotPreReserve400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoomSlotPreReserve400Name
  /** Código de status HTTP 400. */
  status_code: CreateRoomSlotPreReserve400StatusCode
}

/**
 * Status do slot - sempre pre_reserved para este endpoint
 */
export type CreateRoomSlotPreReserve201SlotStatus =
  (typeof CreateRoomSlotPreReserve201SlotStatus)[keyof typeof CreateRoomSlotPreReserve201SlotStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserve201SlotStatus = {
  pre_reserved: 'pre_reserved',
  reserved: 'reserved',
} as const

export type CreateRoomSlotPreReserve201Slot = {
  /** Identificador único do slot */
  id: string
  /** Data e hora até quando o slot está pré-reservado */
  preReservedUntil: string
  /** Identificador único da sala */
  roomId: string
  /** Horário de término do slot no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início do slot no formato ISO com timezone do usuário */
  slotStart: string
  /** Status do slot - sempre pre_reserved para este endpoint */
  status: CreateRoomSlotPreReserve201SlotStatus
  /** ID do usuário que fez a pré-reserva */
  userId: string
}

export type CreateRoomSlotPreReserve201 = {
  /** Mensagem de sucesso */
  message: string
  slot: CreateRoomSlotPreReserve201Slot
}

/**
 * Status da reserva - deve ser pre_reserved para pré-reservas
 */
export type CreateRoomSlotPreReserveBodyStatus =
  (typeof CreateRoomSlotPreReserveBodyStatus)[keyof typeof CreateRoomSlotPreReserveBodyStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoomSlotPreReserveBodyStatus = {
  pre_reserved: 'pre_reserved',
  reserved: 'reserved',
} as const

export type CreateRoomSlotPreReserveBody = {
  roomId: string
  /** Horário de término da reserva no formato ISO com timezone do usuário */
  slotEnd: string
  /** Horário de início da reserva no formato ISO com timezone do usuário */
  slotStart: string
  /** Status da reserva - deve ser pre_reserved para pré-reservas */
  status?: CreateRoomSlotPreReserveBodyStatus
}

/**
 * Código de status HTTP 500.
 */
export type GetRoomSlotAvailability500StatusCode =
  (typeof GetRoomSlotAvailability500StatusCode)[keyof typeof GetRoomSlotAvailability500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomSlotAvailability500Name =
  (typeof GetRoomSlotAvailability500Name)[keyof typeof GetRoomSlotAvailability500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetRoomSlotAvailability500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomSlotAvailability500Name
  /** Código de status HTTP 500. */
  status_code: GetRoomSlotAvailability500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type GetRoomSlotAvailability422StatusCode =
  (typeof GetRoomSlotAvailability422StatusCode)[keyof typeof GetRoomSlotAvailability422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomSlotAvailability422Name =
  (typeof GetRoomSlotAvailability422Name)[keyof typeof GetRoomSlotAvailability422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type GetRoomSlotAvailability422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type GetRoomSlotAvailability422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: GetRoomSlotAvailability422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomSlotAvailability422Name
  /** Código de status HTTP 422. */
  status_code: GetRoomSlotAvailability422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type GetRoomSlotAvailability404StatusCode =
  (typeof GetRoomSlotAvailability404StatusCode)[keyof typeof GetRoomSlotAvailability404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomSlotAvailability404Name =
  (typeof GetRoomSlotAvailability404Name)[keyof typeof GetRoomSlotAvailability404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type GetRoomSlotAvailability404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomSlotAvailability404Name
  /** Código de status HTTP 404. */
  status_code: GetRoomSlotAvailability404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetRoomSlotAvailability403StatusCode =
  (typeof GetRoomSlotAvailability403StatusCode)[keyof typeof GetRoomSlotAvailability403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomSlotAvailability403Name =
  (typeof GetRoomSlotAvailability403Name)[keyof typeof GetRoomSlotAvailability403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetRoomSlotAvailability403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomSlotAvailability403Name
  /** Código de status HTTP 403. */
  status_code: GetRoomSlotAvailability403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetRoomSlotAvailability401StatusCode =
  (typeof GetRoomSlotAvailability401StatusCode)[keyof typeof GetRoomSlotAvailability401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomSlotAvailability401Name =
  (typeof GetRoomSlotAvailability401Name)[keyof typeof GetRoomSlotAvailability401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetRoomSlotAvailability401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomSlotAvailability401Name
  /** Código de status HTTP 401. */
  status_code: GetRoomSlotAvailability401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetRoomSlotAvailability400StatusCode =
  (typeof GetRoomSlotAvailability400StatusCode)[keyof typeof GetRoomSlotAvailability400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoomSlotAvailability400Name =
  (typeof GetRoomSlotAvailability400Name)[keyof typeof GetRoomSlotAvailability400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetRoomSlotAvailability400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoomSlotAvailability400Name
  /** Código de status HTTP 400. */
  status_code: GetRoomSlotAvailability400StatusCode
}

/**
 * Usuário que fez a pré-reserva (null quando não há pré-reserva)
 */
export type GetRoomSlotAvailability200SlotsItemUser = {
  email: string
  id: string
  name: string
}

export type GetRoomSlotAvailability200SlotsItemStatus =
  (typeof GetRoomSlotAvailability200SlotsItemStatus)[keyof typeof GetRoomSlotAvailability200SlotsItemStatus]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoomSlotAvailability200SlotsItemStatus = {
  reserved: 'reserved',
  pre_reserved: 'pre_reserved',
} as const

export type GetRoomSlotAvailability200SlotsItem = {
  /** Identificador único do slot */
  id: string
  /**
   * Data e hora da pré-reserva (null quando não há pré-reserva)
   * @nullable
   */
  preReservedUntil: string | null
  /** Data e hora do slot no formato ISO (UTC) */
  slotEnd: string
  /** Data e hora do slot no formato ISO (UTC) */
  slotStart: string
  status: GetRoomSlotAvailability200SlotsItemStatus
  /** Usuário que fez a pré-reserva (null quando não há pré-reserva) */
  user: GetRoomSlotAvailability200SlotsItemUser
}

export type GetRoomSlotAvailability200Room = {
  /** Capacidade máxima de pessoas na sala */
  capacidade: number
  /**
   * Descrição da sala (pode ser nulo)
   * @nullable
   */
  description: string | null
  /** Identificador único da sala */
  id: string
  /** URLs das imagens da sala */
  imagens: string[]
  /** Indica se a sala está ativa ou inativa */
  isActive: boolean
  /** Nome da sala */
  name: string
  /** Lista de recursos disponíveis na sala */
  recursos: string[]
}

export type GetRoomSlotAvailability200 = {
  room: GetRoomSlotAvailability200Room
  slots: GetRoomSlotAvailability200SlotsItem[]
}

export type GetRoomSlotAvailabilityParams = {
  /**
   * Data inicial para verificação de disponibilidade no formato YYYY-MM-DD
   */
  startDate: string
  /**
   * Data final para verificação de disponibilidade no formato YYYY-MM-DD
   */
  endDate: string
}

/**
 * Código de status HTTP 500.
 */
export type ListRoomSlots500StatusCode =
  (typeof ListRoomSlots500StatusCode)[keyof typeof ListRoomSlots500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomSlots500Name =
  (typeof ListRoomSlots500Name)[keyof typeof ListRoomSlots500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListRoomSlots500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomSlots500Name
  /** Código de status HTTP 500. */
  status_code: ListRoomSlots500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListRoomSlots422StatusCode =
  (typeof ListRoomSlots422StatusCode)[keyof typeof ListRoomSlots422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomSlots422Name =
  (typeof ListRoomSlots422Name)[keyof typeof ListRoomSlots422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListRoomSlots422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListRoomSlots422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListRoomSlots422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomSlots422Name
  /** Código de status HTTP 422. */
  status_code: ListRoomSlots422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListRoomSlots403StatusCode =
  (typeof ListRoomSlots403StatusCode)[keyof typeof ListRoomSlots403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomSlots403Name =
  (typeof ListRoomSlots403Name)[keyof typeof ListRoomSlots403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListRoomSlots403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomSlots403Name
  /** Código de status HTTP 403. */
  status_code: ListRoomSlots403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListRoomSlots401StatusCode =
  (typeof ListRoomSlots401StatusCode)[keyof typeof ListRoomSlots401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomSlots401Name =
  (typeof ListRoomSlots401Name)[keyof typeof ListRoomSlots401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListRoomSlots401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomSlots401Name
  /** Código de status HTTP 401. */
  status_code: ListRoomSlots401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListRoomSlots400StatusCode =
  (typeof ListRoomSlots400StatusCode)[keyof typeof ListRoomSlots400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRoomSlots400Name =
  (typeof ListRoomSlots400Name)[keyof typeof ListRoomSlots400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRoomSlots400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListRoomSlots400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListRoomSlots400Name
  /** Código de status HTTP 400. */
  status_code: ListRoomSlots400StatusCode
}

export type ListRoomSlots200RoomsItem = {
  /** Capacidade máxima de pessoas na sala */
  capacidade: number
  /**
   * Descrição da sala (pode ser nulo)
   * @nullable
   */
  description: string | null
  /** Identificador único da sala */
  id: string
  /** URLs das imagens da sala */
  imagens: string[]
  /** Nome da sala */
  name: string
  /** Lista de recursos disponíveis na sala */
  recursos: string[]
}

/**
 * Lista paginada de salas disponíveis com metadados
 */
export type ListRoomSlots200 = {
  /** Número da página atual sendo exibida */
  currentPage: number
  /** Mensagem informativa sobre o resultado da operação */
  message: string
  /** Array de salas disponíveis no período solicitado */
  rooms: ListRoomSlots200RoomsItem[]
  /** Número total de salas disponíveis para a consulta */
  totalCount: number
  /** Número total de páginas com base no tamanho de página */
  totalPages: number
}

export type ListRoomSlotsParams = {
  /**
   * Data/hora no formato ISO com timezone (suporta formatos como -03:00, +00:00 ou Z). Se enviar apenas a data com hora zerada (00:00:00), retorna salas com pelo menos um horário disponível nessa data. Se enviar data com uma hora específica, retorna salas disponíveis nesse horário específico. Exemplo para data: "2025-05-22T00:00:00-03:00", exemplo para hora específica: "2025-05-22T14:00:00-03:00"
   */
  datetime: string
  /**
   * Número da página para paginação, começando em 1 (padrão: 1)
   */
  page?: string
  /**
   * Quantidade de resultados por página, entre 1 e 100 (padrão: 12)
   */
  pageSize?: string
}

/**
 * Código de status HTTP 500.
 */
export type OpenDoor500StatusCode =
  (typeof OpenDoor500StatusCode)[keyof typeof OpenDoor500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor500Name =
  (typeof OpenDoor500Name)[keyof typeof OpenDoor500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type OpenDoor500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor500Name
  /** Código de status HTTP 500. */
  status_code: OpenDoor500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type OpenDoor422StatusCode =
  (typeof OpenDoor422StatusCode)[keyof typeof OpenDoor422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor422Name =
  (typeof OpenDoor422Name)[keyof typeof OpenDoor422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type OpenDoor422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type OpenDoor422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: OpenDoor422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor422Name
  /** Código de status HTTP 422. */
  status_code: OpenDoor422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type OpenDoor404StatusCode =
  (typeof OpenDoor404StatusCode)[keyof typeof OpenDoor404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor404Name =
  (typeof OpenDoor404Name)[keyof typeof OpenDoor404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type OpenDoor404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor404Name
  /** Código de status HTTP 404. */
  status_code: OpenDoor404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type OpenDoor403StatusCode =
  (typeof OpenDoor403StatusCode)[keyof typeof OpenDoor403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor403Name =
  (typeof OpenDoor403Name)[keyof typeof OpenDoor403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type OpenDoor403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor403Name
  /** Código de status HTTP 403. */
  status_code: OpenDoor403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type OpenDoor401StatusCode =
  (typeof OpenDoor401StatusCode)[keyof typeof OpenDoor401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor401Name =
  (typeof OpenDoor401Name)[keyof typeof OpenDoor401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type OpenDoor401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor401Name
  /** Código de status HTTP 401. */
  status_code: OpenDoor401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type OpenDoor400StatusCode =
  (typeof OpenDoor400StatusCode)[keyof typeof OpenDoor400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type OpenDoor400Name =
  (typeof OpenDoor400Name)[keyof typeof OpenDoor400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OpenDoor400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type OpenDoor400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: OpenDoor400Name
  /** Código de status HTTP 400. */
  status_code: OpenDoor400StatusCode
}

export type OpenDoor200Room = {
  id: string
  name: string
}

/**
 * Código de abertura da porta gerado com sucesso
 */
export type OpenDoor200 = {
  doorCode: string
  expiresAt: string
  message: string
  room: OpenDoor200Room
}

export type OpenDoorParams = {
  roomName: string
}

/**
 * Código de status HTTP 500.
 */
export type UpdateRoom500StatusCode =
  (typeof UpdateRoom500StatusCode)[keyof typeof UpdateRoom500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom500Name =
  (typeof UpdateRoom500Name)[keyof typeof UpdateRoom500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UpdateRoom500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom500Name
  /** Código de status HTTP 500. */
  status_code: UpdateRoom500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UpdateRoom422StatusCode =
  (typeof UpdateRoom422StatusCode)[keyof typeof UpdateRoom422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom422Name =
  (typeof UpdateRoom422Name)[keyof typeof UpdateRoom422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UpdateRoom422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UpdateRoom422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UpdateRoom422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom422Name
  /** Código de status HTTP 422. */
  status_code: UpdateRoom422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type UpdateRoom409StatusCode =
  (typeof UpdateRoom409StatusCode)[keyof typeof UpdateRoom409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom409Name =
  (typeof UpdateRoom409Name)[keyof typeof UpdateRoom409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type UpdateRoom409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom409Name
  /** Código de status HTTP 409. */
  status_code: UpdateRoom409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type UpdateRoom404StatusCode =
  (typeof UpdateRoom404StatusCode)[keyof typeof UpdateRoom404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom404Name =
  (typeof UpdateRoom404Name)[keyof typeof UpdateRoom404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type UpdateRoom404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom404Name
  /** Código de status HTTP 404. */
  status_code: UpdateRoom404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UpdateRoom403StatusCode =
  (typeof UpdateRoom403StatusCode)[keyof typeof UpdateRoom403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom403Name =
  (typeof UpdateRoom403Name)[keyof typeof UpdateRoom403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UpdateRoom403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom403Name
  /** Código de status HTTP 403. */
  status_code: UpdateRoom403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UpdateRoom401StatusCode =
  (typeof UpdateRoom401StatusCode)[keyof typeof UpdateRoom401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom401Name =
  (typeof UpdateRoom401Name)[keyof typeof UpdateRoom401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UpdateRoom401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom401Name
  /** Código de status HTTP 401. */
  status_code: UpdateRoom401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UpdateRoom400StatusCode =
  (typeof UpdateRoom400StatusCode)[keyof typeof UpdateRoom400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateRoom400Name =
  (typeof UpdateRoom400Name)[keyof typeof UpdateRoom400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateRoom400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UpdateRoom400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateRoom400Name
  /** Código de status HTTP 400. */
  status_code: UpdateRoom400StatusCode
}

export type UpdateRoom200Room = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  recursos: string[]
  updatedAt: string
  userId: string
}

/**
 * Sala atualizada com sucesso
 */
export type UpdateRoom200 = {
  message: string
  room: UpdateRoom200Room
}

export type UpdateRoomBody = {
  /**
   * @minimum 0
   * @exclusiveMinimum
   */
  capacidade?: number
  /**
   * @maxLength 500
   * @nullable
   */
  description?: string | null
  /** @maxItems 5 */
  imagens?: string[]
  isActive?: boolean
  /**
   * @minLength 3
   * @maxLength 100
   */
  name?: string
  recursos?: string[]
}

/**
 * Código de status HTTP 500.
 */
export type GetRoom500StatusCode =
  (typeof GetRoom500StatusCode)[keyof typeof GetRoom500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoom500Name =
  (typeof GetRoom500Name)[keyof typeof GetRoom500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetRoom500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoom500Name
  /** Código de status HTTP 500. */
  status_code: GetRoom500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type GetRoom422StatusCode =
  (typeof GetRoom422StatusCode)[keyof typeof GetRoom422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoom422Name =
  (typeof GetRoom422Name)[keyof typeof GetRoom422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type GetRoom422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type GetRoom422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: GetRoom422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoom422Name
  /** Código de status HTTP 422. */
  status_code: GetRoom422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type GetRoom404StatusCode =
  (typeof GetRoom404StatusCode)[keyof typeof GetRoom404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoom404Name =
  (typeof GetRoom404Name)[keyof typeof GetRoom404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type GetRoom404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoom404Name
  /** Código de status HTTP 404. */
  status_code: GetRoom404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetRoom403StatusCode =
  (typeof GetRoom403StatusCode)[keyof typeof GetRoom403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoom403Name =
  (typeof GetRoom403Name)[keyof typeof GetRoom403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetRoom403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoom403Name
  /** Código de status HTTP 403. */
  status_code: GetRoom403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetRoom401StatusCode =
  (typeof GetRoom401StatusCode)[keyof typeof GetRoom401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoom401Name =
  (typeof GetRoom401Name)[keyof typeof GetRoom401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetRoom401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoom401Name
  /** Código de status HTTP 401. */
  status_code: GetRoom401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetRoom400StatusCode =
  (typeof GetRoom400StatusCode)[keyof typeof GetRoom400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetRoom400Name =
  (typeof GetRoom400Name)[keyof typeof GetRoom400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetRoom400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetRoom400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetRoom400Name
  /** Código de status HTTP 400. */
  status_code: GetRoom400StatusCode
}

export type GetRoom200Room = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  recursos: string[]
  updatedAt: string
  userId: string
  userName: string
}

/**
 * Detalhes da sala
 */
export type GetRoom200 = {
  room: GetRoom200Room
}

/**
 * Código de status HTTP 500.
 */
export type DeleteRoom500StatusCode =
  (typeof DeleteRoom500StatusCode)[keyof typeof DeleteRoom500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoom500Name =
  (typeof DeleteRoom500Name)[keyof typeof DeleteRoom500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteRoom500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoom500Name
  /** Código de status HTTP 500. */
  status_code: DeleteRoom500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteRoom422StatusCode =
  (typeof DeleteRoom422StatusCode)[keyof typeof DeleteRoom422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoom422Name =
  (typeof DeleteRoom422Name)[keyof typeof DeleteRoom422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteRoom422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteRoom422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteRoom422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoom422Name
  /** Código de status HTTP 422. */
  status_code: DeleteRoom422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type DeleteRoom404StatusCode =
  (typeof DeleteRoom404StatusCode)[keyof typeof DeleteRoom404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoom404Name =
  (typeof DeleteRoom404Name)[keyof typeof DeleteRoom404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type DeleteRoom404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoom404Name
  /** Código de status HTTP 404. */
  status_code: DeleteRoom404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteRoom403StatusCode =
  (typeof DeleteRoom403StatusCode)[keyof typeof DeleteRoom403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoom403Name =
  (typeof DeleteRoom403Name)[keyof typeof DeleteRoom403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteRoom403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoom403Name
  /** Código de status HTTP 403. */
  status_code: DeleteRoom403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteRoom401StatusCode =
  (typeof DeleteRoom401StatusCode)[keyof typeof DeleteRoom401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoom401Name =
  (typeof DeleteRoom401Name)[keyof typeof DeleteRoom401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteRoom401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoom401Name
  /** Código de status HTTP 401. */
  status_code: DeleteRoom401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteRoom400StatusCode =
  (typeof DeleteRoom400StatusCode)[keyof typeof DeleteRoom400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteRoom400Name =
  (typeof DeleteRoom400Name)[keyof typeof DeleteRoom400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteRoom400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteRoom400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteRoom400Name
  /** Código de status HTTP 400. */
  status_code: DeleteRoom400StatusCode
}

/**
 * Sala excluída com sucesso
 */
export type DeleteRoom200 = {
  message: string
}

/**
 * Código de status HTTP 500.
 */
export type ListRooms500StatusCode =
  (typeof ListRooms500StatusCode)[keyof typeof ListRooms500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRooms500Name =
  (typeof ListRooms500Name)[keyof typeof ListRooms500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListRooms500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListRooms500Name
  /** Código de status HTTP 500. */
  status_code: ListRooms500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListRooms422StatusCode =
  (typeof ListRooms422StatusCode)[keyof typeof ListRooms422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRooms422Name =
  (typeof ListRooms422Name)[keyof typeof ListRooms422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListRooms422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListRooms422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListRooms422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListRooms422Name
  /** Código de status HTTP 422. */
  status_code: ListRooms422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListRooms403StatusCode =
  (typeof ListRooms403StatusCode)[keyof typeof ListRooms403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRooms403Name =
  (typeof ListRooms403Name)[keyof typeof ListRooms403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListRooms403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListRooms403Name
  /** Código de status HTTP 403. */
  status_code: ListRooms403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListRooms401StatusCode =
  (typeof ListRooms401StatusCode)[keyof typeof ListRooms401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRooms401Name =
  (typeof ListRooms401Name)[keyof typeof ListRooms401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListRooms401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListRooms401Name
  /** Código de status HTTP 401. */
  status_code: ListRooms401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListRooms400StatusCode =
  (typeof ListRooms400StatusCode)[keyof typeof ListRooms400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListRooms400Name =
  (typeof ListRooms400Name)[keyof typeof ListRooms400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListRooms400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListRooms400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListRooms400Name
  /** Código de status HTTP 400. */
  status_code: ListRooms400StatusCode
}

export type ListRooms200RoomsItem = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  recursos: string[]
  updatedAt: string
  userId: string
  userName: string
}

/**
 * Lista de salas
 */
export type ListRooms200 = {
  currentPage: number
  rooms: ListRooms200RoomsItem[]
  totalCount: number
  totalPages: number
}

export type ListRoomsParams = {
  page?: string
  pageSize?: string
  searchTerm?: string
  isActive?: string
  capacidade?: string
}

/**
 * Código de status HTTP 500.
 */
export type CreateRoom500StatusCode =
  (typeof CreateRoom500StatusCode)[keyof typeof CreateRoom500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoom500Name =
  (typeof CreateRoom500Name)[keyof typeof CreateRoom500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type CreateRoom500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoom500Name
  /** Código de status HTTP 500. */
  status_code: CreateRoom500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type CreateRoom422StatusCode =
  (typeof CreateRoom422StatusCode)[keyof typeof CreateRoom422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoom422Name =
  (typeof CreateRoom422Name)[keyof typeof CreateRoom422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type CreateRoom422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type CreateRoom422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: CreateRoom422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoom422Name
  /** Código de status HTTP 422. */
  status_code: CreateRoom422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type CreateRoom409StatusCode =
  (typeof CreateRoom409StatusCode)[keyof typeof CreateRoom409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoom409Name =
  (typeof CreateRoom409Name)[keyof typeof CreateRoom409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type CreateRoom409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoom409Name
  /** Código de status HTTP 409. */
  status_code: CreateRoom409StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type CreateRoom403StatusCode =
  (typeof CreateRoom403StatusCode)[keyof typeof CreateRoom403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoom403Name =
  (typeof CreateRoom403Name)[keyof typeof CreateRoom403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type CreateRoom403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoom403Name
  /** Código de status HTTP 403. */
  status_code: CreateRoom403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type CreateRoom401StatusCode =
  (typeof CreateRoom401StatusCode)[keyof typeof CreateRoom401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoom401Name =
  (typeof CreateRoom401Name)[keyof typeof CreateRoom401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type CreateRoom401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoom401Name
  /** Código de status HTTP 401. */
  status_code: CreateRoom401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type CreateRoom400StatusCode =
  (typeof CreateRoom400StatusCode)[keyof typeof CreateRoom400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type CreateRoom400Name =
  (typeof CreateRoom400Name)[keyof typeof CreateRoom400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CreateRoom400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type CreateRoom400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: CreateRoom400Name
  /** Código de status HTTP 400. */
  status_code: CreateRoom400StatusCode
}

export type CreateRoom201Room = {
  capacidade: number
  createdAt: string
  /** @nullable */
  description: string | null
  id: string
  imagens: string[]
  isActive: boolean
  name: string
  recursos: string[]
  updatedAt: string
  userId: string
  userName: string
}

/**
 * Sala criada com sucesso
 */
export type CreateRoom201 = {
  message: string
  room: CreateRoom201Room
}

export type CreateRoomBody = {
  /**
   * @minimum 0
   * @exclusiveMinimum
   */
  capacidade: number
  /**
   * @maxLength 500
   * @nullable
   */
  description?: string | null
  /** @maxItems 5 */
  imagens?: string[]
  isActive?: boolean
  /**
   * @minLength 3
   * @maxLength 100
   */
  name: string
  recursos?: string[]
}

/**
 * Código de status HTTP 500.
 */
export type DeleteImage500StatusCode =
  (typeof DeleteImage500StatusCode)[keyof typeof DeleteImage500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage500Name =
  (typeof DeleteImage500Name)[keyof typeof DeleteImage500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteImage500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage500Name
  /** Código de status HTTP 500. */
  status_code: DeleteImage500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteImage422StatusCode =
  (typeof DeleteImage422StatusCode)[keyof typeof DeleteImage422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage422Name =
  (typeof DeleteImage422Name)[keyof typeof DeleteImage422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteImage422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteImage422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteImage422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage422Name
  /** Código de status HTTP 422. */
  status_code: DeleteImage422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteImage403StatusCode =
  (typeof DeleteImage403StatusCode)[keyof typeof DeleteImage403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage403Name =
  (typeof DeleteImage403Name)[keyof typeof DeleteImage403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteImage403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage403Name
  /** Código de status HTTP 403. */
  status_code: DeleteImage403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteImage401StatusCode =
  (typeof DeleteImage401StatusCode)[keyof typeof DeleteImage401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage401Name =
  (typeof DeleteImage401Name)[keyof typeof DeleteImage401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteImage401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage401Name
  /** Código de status HTTP 401. */
  status_code: DeleteImage401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteImage400StatusCode =
  (typeof DeleteImage400StatusCode)[keyof typeof DeleteImage400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteImage400Name =
  (typeof DeleteImage400Name)[keyof typeof DeleteImage400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteImage400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteImage400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteImage400Name
  /** Código de status HTTP 400. */
  status_code: DeleteImage400StatusCode
}

/**
 * Image deleted successfully
 */
export type DeleteImage200 = {
  message: string
}

/**
 * Payload to delete an image
 */
export type DeleteImageBody = {
  /** @minLength 1 */
  imagePath: string
}

/**
 * Código de status HTTP 500.
 */
export type UploadImage500StatusCode =
  (typeof UploadImage500StatusCode)[keyof typeof UploadImage500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage500Name =
  (typeof UploadImage500Name)[keyof typeof UploadImage500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UploadImage500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage500Name
  /** Código de status HTTP 500. */
  status_code: UploadImage500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UploadImage422StatusCode =
  (typeof UploadImage422StatusCode)[keyof typeof UploadImage422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage422Name =
  (typeof UploadImage422Name)[keyof typeof UploadImage422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UploadImage422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UploadImage422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UploadImage422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage422Name
  /** Código de status HTTP 422. */
  status_code: UploadImage422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UploadImage403StatusCode =
  (typeof UploadImage403StatusCode)[keyof typeof UploadImage403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage403Name =
  (typeof UploadImage403Name)[keyof typeof UploadImage403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UploadImage403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage403Name
  /** Código de status HTTP 403. */
  status_code: UploadImage403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UploadImage401StatusCode =
  (typeof UploadImage401StatusCode)[keyof typeof UploadImage401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage401Name =
  (typeof UploadImage401Name)[keyof typeof UploadImage401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UploadImage401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage401Name
  /** Código de status HTTP 401. */
  status_code: UploadImage401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UploadImage400StatusCode =
  (typeof UploadImage400StatusCode)[keyof typeof UploadImage400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UploadImage400Name =
  (typeof UploadImage400Name)[keyof typeof UploadImage400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UploadImage400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UploadImage400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UploadImage400Name
  /** Código de status HTTP 400. */
  status_code: UploadImage400StatusCode
}

/**
 * Image uploaded successfully
 */
export type UploadImage201 = {
  imagePath: string
  message: string
}

export type UploadImageBody = {
  file: Blob
}

export type UploadImageParams = {
  /**
   * Group of asset. Ex: 'avatar', 'logo', 'product'
   */
  group: string
  /**
   * Subtitle of asset. Ex: 'profile', 'og', 'drink'
   */
  subtitle?: string
  /**
   * Custom folder to store the image. Default: userId
   */
  folder?: string
}

/**
 * Código de status HTTP 500.
 */
export type UpdateUser500StatusCode =
  (typeof UpdateUser500StatusCode)[keyof typeof UpdateUser500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser500Name =
  (typeof UpdateUser500Name)[keyof typeof UpdateUser500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UpdateUser500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser500Name
  /** Código de status HTTP 500. */
  status_code: UpdateUser500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UpdateUser422StatusCode =
  (typeof UpdateUser422StatusCode)[keyof typeof UpdateUser422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser422Name =
  (typeof UpdateUser422Name)[keyof typeof UpdateUser422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UpdateUser422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UpdateUser422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UpdateUser422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser422Name
  /** Código de status HTTP 422. */
  status_code: UpdateUser422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type UpdateUser409StatusCode =
  (typeof UpdateUser409StatusCode)[keyof typeof UpdateUser409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser409Name =
  (typeof UpdateUser409Name)[keyof typeof UpdateUser409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type UpdateUser409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser409Name
  /** Código de status HTTP 409. */
  status_code: UpdateUser409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type UpdateUser404StatusCode =
  (typeof UpdateUser404StatusCode)[keyof typeof UpdateUser404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser404Name =
  (typeof UpdateUser404Name)[keyof typeof UpdateUser404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type UpdateUser404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser404Name
  /** Código de status HTTP 404. */
  status_code: UpdateUser404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UpdateUser403StatusCode =
  (typeof UpdateUser403StatusCode)[keyof typeof UpdateUser403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser403Name =
  (typeof UpdateUser403Name)[keyof typeof UpdateUser403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UpdateUser403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser403Name
  /** Código de status HTTP 403. */
  status_code: UpdateUser403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UpdateUser401StatusCode =
  (typeof UpdateUser401StatusCode)[keyof typeof UpdateUser401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser401Name =
  (typeof UpdateUser401Name)[keyof typeof UpdateUser401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UpdateUser401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser401Name
  /** Código de status HTTP 401. */
  status_code: UpdateUser401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UpdateUser400StatusCode =
  (typeof UpdateUser400StatusCode)[keyof typeof UpdateUser400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UpdateUser400Name =
  (typeof UpdateUser400Name)[keyof typeof UpdateUser400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UpdateUser400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UpdateUser400Name
  /** Código de status HTTP 400. */
  status_code: UpdateUser400StatusCode
}

export type UpdateUser200UserRole =
  (typeof UpdateUser200UserRole)[keyof typeof UpdateUser200UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUser200UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UpdateUser200User = {
  accountStatus: boolean
  /** @nullable */
  avatar: string | null
  email: string
  id: string
  /** @nullable */
  name: string | null
  /** @nullable */
  nickName: string | null
  passwordResetRequired: boolean
  /** @nullable */
  phone: string | null
  role: UpdateUser200UserRole
}

/**
 * Usuário atualizado com sucesso
 */
export type UpdateUser200 = {
  message: string
  user: UpdateUser200User
}

export type UpdateUserBodyRole =
  (typeof UpdateUserBodyRole)[keyof typeof UpdateUserBodyRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UpdateUserBodyRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UpdateUserBody = {
  accountStatus?: boolean
  /** @nullable */
  avatar?: string | null
  /**
   * @minLength 3
   * @maxLength 254
   */
  email?: string
  /**
   * @minLength 2
   * @maxLength 100
   * @pattern ^\S+\s+\S+
   */
  name?: string
  /** @nullable */
  nickName?: string | null
  passwordResetRequired?: boolean
  /** @nullable */
  phone?: string | null
  role?: UpdateUserBodyRole
}

/**
 * Código de status HTTP 500.
 */
export type GetUser500StatusCode =
  (typeof GetUser500StatusCode)[keyof typeof GetUser500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type GetUser500Name =
  (typeof GetUser500Name)[keyof typeof GetUser500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type GetUser500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: GetUser500Name
  /** Código de status HTTP 500. */
  status_code: GetUser500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type GetUser422StatusCode =
  (typeof GetUser422StatusCode)[keyof typeof GetUser422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type GetUser422Name =
  (typeof GetUser422Name)[keyof typeof GetUser422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type GetUser422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type GetUser422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: GetUser422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: GetUser422Name
  /** Código de status HTTP 422. */
  status_code: GetUser422StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type GetUser404StatusCode =
  (typeof GetUser404StatusCode)[keyof typeof GetUser404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type GetUser404Name =
  (typeof GetUser404Name)[keyof typeof GetUser404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type GetUser404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: GetUser404Name
  /** Código de status HTTP 404. */
  status_code: GetUser404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type GetUser403StatusCode =
  (typeof GetUser403StatusCode)[keyof typeof GetUser403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type GetUser403Name =
  (typeof GetUser403Name)[keyof typeof GetUser403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type GetUser403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: GetUser403Name
  /** Código de status HTTP 403. */
  status_code: GetUser403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type GetUser401StatusCode =
  (typeof GetUser401StatusCode)[keyof typeof GetUser401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type GetUser401Name =
  (typeof GetUser401Name)[keyof typeof GetUser401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type GetUser401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: GetUser401Name
  /** Código de status HTTP 401. */
  status_code: GetUser401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type GetUser400StatusCode =
  (typeof GetUser400StatusCode)[keyof typeof GetUser400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type GetUser400Name =
  (typeof GetUser400Name)[keyof typeof GetUser400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type GetUser400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: GetUser400Name
  /** Código de status HTTP 400. */
  status_code: GetUser400StatusCode
}

export type GetUser200UserRole =
  (typeof GetUser200UserRole)[keyof typeof GetUser200UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GetUser200UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type GetUser200User = {
  accountStatus: boolean
  /** @nullable */
  avatar: string | null
  email: string
  id: string
  /** @nullable */
  name: string | null
  /** @nullable */
  nickName: string | null
  passwordResetRequired: boolean
  /** @nullable */
  phone: string | null
  role: GetUser200UserRole
}

/**
 * Dados do usuário recuperados com sucesso
 */
export type GetUser200 = {
  message: string
  user: GetUser200User
}

/**
 * Código de status HTTP 500.
 */
export type DeleteUser500StatusCode =
  (typeof DeleteUser500StatusCode)[keyof typeof DeleteUser500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser500Name =
  (typeof DeleteUser500Name)[keyof typeof DeleteUser500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type DeleteUser500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser500Name
  /** Código de status HTTP 500. */
  status_code: DeleteUser500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type DeleteUser422StatusCode =
  (typeof DeleteUser422StatusCode)[keyof typeof DeleteUser422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser422Name =
  (typeof DeleteUser422Name)[keyof typeof DeleteUser422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type DeleteUser422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type DeleteUser422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: DeleteUser422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser422Name
  /** Código de status HTTP 422. */
  status_code: DeleteUser422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type DeleteUser409StatusCode =
  (typeof DeleteUser409StatusCode)[keyof typeof DeleteUser409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser409Name =
  (typeof DeleteUser409Name)[keyof typeof DeleteUser409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type DeleteUser409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser409Name
  /** Código de status HTTP 409. */
  status_code: DeleteUser409StatusCode
}

/**
 * Código de status HTTP 404.
 */
export type DeleteUser404StatusCode =
  (typeof DeleteUser404StatusCode)[keyof typeof DeleteUser404StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser404StatusCode = {
  NUMBER_404: 404,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser404Name =
  (typeof DeleteUser404Name)[keyof typeof DeleteUser404Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser404Name = {
  NotFoundError: 'NotFoundError',
} as const

/**
 * Schema to represent Not Found errors (HTTP 404)
 */
export type DeleteUser404 = {
  /** Ação recomendada para o recurso não encontrado. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o recurso não foi encontrado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser404Name
  /** Código de status HTTP 404. */
  status_code: DeleteUser404StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type DeleteUser403StatusCode =
  (typeof DeleteUser403StatusCode)[keyof typeof DeleteUser403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser403Name =
  (typeof DeleteUser403Name)[keyof typeof DeleteUser403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type DeleteUser403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser403Name
  /** Código de status HTTP 403. */
  status_code: DeleteUser403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type DeleteUser401StatusCode =
  (typeof DeleteUser401StatusCode)[keyof typeof DeleteUser401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser401Name =
  (typeof DeleteUser401Name)[keyof typeof DeleteUser401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type DeleteUser401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser401Name
  /** Código de status HTTP 401. */
  status_code: DeleteUser401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type DeleteUser400StatusCode =
  (typeof DeleteUser400StatusCode)[keyof typeof DeleteUser400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type DeleteUser400Name =
  (typeof DeleteUser400Name)[keyof typeof DeleteUser400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeleteUser400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type DeleteUser400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: DeleteUser400Name
  /** Código de status HTTP 400. */
  status_code: DeleteUser400StatusCode
}

/**
 * Usuário excluído com sucesso
 */
export type DeleteUser200 = {
  message: string
  userId: string
}

/**
 * Código de status HTTP 500.
 */
export type ListUsers500StatusCode =
  (typeof ListUsers500StatusCode)[keyof typeof ListUsers500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ListUsers500Name =
  (typeof ListUsers500Name)[keyof typeof ListUsers500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ListUsers500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ListUsers500Name
  /** Código de status HTTP 500. */
  status_code: ListUsers500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type ListUsers422StatusCode =
  (typeof ListUsers422StatusCode)[keyof typeof ListUsers422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type ListUsers422Name =
  (typeof ListUsers422Name)[keyof typeof ListUsers422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type ListUsers422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type ListUsers422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: ListUsers422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: ListUsers422Name
  /** Código de status HTTP 422. */
  status_code: ListUsers422StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type ListUsers403StatusCode =
  (typeof ListUsers403StatusCode)[keyof typeof ListUsers403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type ListUsers403Name =
  (typeof ListUsers403Name)[keyof typeof ListUsers403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type ListUsers403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: ListUsers403Name
  /** Código de status HTTP 403. */
  status_code: ListUsers403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type ListUsers401StatusCode =
  (typeof ListUsers401StatusCode)[keyof typeof ListUsers401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type ListUsers401Name =
  (typeof ListUsers401Name)[keyof typeof ListUsers401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type ListUsers401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: ListUsers401Name
  /** Código de status HTTP 401. */
  status_code: ListUsers401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type ListUsers400StatusCode =
  (typeof ListUsers400StatusCode)[keyof typeof ListUsers400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type ListUsers400Name =
  (typeof ListUsers400Name)[keyof typeof ListUsers400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type ListUsers400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: ListUsers400Name
  /** Código de status HTTP 400. */
  status_code: ListUsers400StatusCode
}

export type ListUsers201UsersItemRole =
  (typeof ListUsers201UsersItemRole)[keyof typeof ListUsers201UsersItemRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsers201UsersItemRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type ListUsers201UsersItem = {
  accountStatus: boolean
  /** @nullable */
  avatar: string | null
  createdAt: string
  email: string
  id: string
  /** @nullable */
  name: string | null
  /** @nullable */
  nickName: string | null
  passwordResetRequired: boolean
  /** @nullable */
  phone: string | null
  role: ListUsers201UsersItemRole
}

/**
 * Lista de usuários recuperada com sucesso
 */
export type ListUsers201 = {
  currentPage: number
  message: string
  totalPages: number
  users: ListUsers201UsersItem[]
}

export type ListUsersRole = (typeof ListUsersRole)[keyof typeof ListUsersRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListUsersRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type ListUsersParams = {
  page?: string
  pageSize?: string
  search?: string
  role?: ListUsersRole
  accountStatus?: string
}

/**
 * Código de status HTTP 500.
 */
export type UserCreate500StatusCode =
  (typeof UserCreate500StatusCode)[keyof typeof UserCreate500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate500Name =
  (typeof UserCreate500Name)[keyof typeof UserCreate500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserCreate500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate500Name
  /** Código de status HTTP 500. */
  status_code: UserCreate500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type UserCreate422StatusCode =
  (typeof UserCreate422StatusCode)[keyof typeof UserCreate422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate422Name =
  (typeof UserCreate422Name)[keyof typeof UserCreate422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type UserCreate422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type UserCreate422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: UserCreate422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate422Name
  /** Código de status HTTP 422. */
  status_code: UserCreate422StatusCode
}

/**
 * Código de status HTTP 409.
 */
export type UserCreate409StatusCode =
  (typeof UserCreate409StatusCode)[keyof typeof UserCreate409StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate409StatusCode = {
  NUMBER_409: 409,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate409Name =
  (typeof UserCreate409Name)[keyof typeof UserCreate409Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate409Name = {
  ConflictError: 'ConflictError',
} as const

/**
 * Schema to represent Conflict errors (HTTP 409)
 */
export type UserCreate409 = {
  /** Ação recomendada para resolver o conflito. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que houve um conflito. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate409Name
  /** Código de status HTTP 409. */
  status_code: UserCreate409StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserCreate403StatusCode =
  (typeof UserCreate403StatusCode)[keyof typeof UserCreate403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate403Name =
  (typeof UserCreate403Name)[keyof typeof UserCreate403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserCreate403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate403Name
  /** Código de status HTTP 403. */
  status_code: UserCreate403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserCreate401StatusCode =
  (typeof UserCreate401StatusCode)[keyof typeof UserCreate401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate401Name =
  (typeof UserCreate401Name)[keyof typeof UserCreate401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserCreate401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate401Name
  /** Código de status HTTP 401. */
  status_code: UserCreate401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserCreate400StatusCode =
  (typeof UserCreate400StatusCode)[keyof typeof UserCreate400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserCreate400Name =
  (typeof UserCreate400Name)[keyof typeof UserCreate400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserCreate400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserCreate400Name
  /** Código de status HTTP 400. */
  status_code: UserCreate400StatusCode
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório.
 */
export type UserCreate201UserRole =
  (typeof UserCreate201UserRole)[keyof typeof UserCreate201UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreate201UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserCreate201User = {
  /** Indica se a conta do usuário está ativa. Valor booleano. Campo obrigatório. */
  accountStatus: boolean
  /**
   * URL da imagem de avatar do usuário. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  avatar: string | null
  /** Email do usuário. Endereço de email válido. Campo obrigatório. */
  email: string
  /** Identificador único do usuário no formato UUID v4. Campo obrigatório. */
  id: string
  /**
   * Nome completo do usuário. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  name: string | null
  /**
   * Apelido do usuário. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  nickName: string | null
  /** Indica se o usuário precisa redefinir a senha no próximo login. Valor booleano. Campo obrigatório. */
  passwordResetRequired: boolean
  /**
   * Número de telefone do usuário. Pode ser nulo. Campo obrigatório.
   * @nullable
   */
  phone: string | null
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Campo obrigatório. */
  role: UserCreate201UserRole
}

/**
 * Detalhes do usuário criado com sucesso
 */
export type UserCreate201 = {
  /** Mensagem informativa sobre o resultado da operação. Campo obrigatório. */
  message: string
  user: UserCreate201User
}

/**
 * Função do usuário no sistema. Aceita apenas: admin, user ou dev. Valor padrão: user. Campo obrigatório.
 */
export type UserCreateBodyRole =
  (typeof UserCreateBodyRole)[keyof typeof UserCreateBodyRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserCreateBodyRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserCreateBody = {
  /**
   * Email do usuário. Endereço de email válido. Campo obrigatório.
   * @minLength 3
   * @maxLength 254
   */
  email: string
  /**
   * Nome completo do usuário. String com nome e sobrenome. Campo obrigatório.
   * @minLength 2
   * @maxLength 100
   * @pattern ^\S+\s+\S+
   */
  name: string
  /** Função do usuário no sistema. Aceita apenas: admin, user ou dev. Valor padrão: user. Campo obrigatório. */
  role?: UserCreateBodyRole
}

/**
 * Código de status HTTP 500.
 */
export type UserMe500StatusCode =
  (typeof UserMe500StatusCode)[keyof typeof UserMe500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe500Name = (typeof UserMe500Name)[keyof typeof UserMe500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type UserMe500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe500Name
  /** Código de status HTTP 500. */
  status_code: UserMe500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type UserMe403StatusCode =
  (typeof UserMe403StatusCode)[keyof typeof UserMe403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe403Name = (typeof UserMe403Name)[keyof typeof UserMe403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type UserMe403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe403Name
  /** Código de status HTTP 403. */
  status_code: UserMe403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type UserMe401StatusCode =
  (typeof UserMe401StatusCode)[keyof typeof UserMe401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe401Name = (typeof UserMe401Name)[keyof typeof UserMe401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type UserMe401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe401Name
  /** Código de status HTTP 401. */
  status_code: UserMe401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type UserMe400StatusCode =
  (typeof UserMe400StatusCode)[keyof typeof UserMe400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type UserMe400Name = (typeof UserMe400Name)[keyof typeof UserMe400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type UserMe400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: UserMe400Name
  /** Código de status HTTP 400. */
  status_code: UserMe400StatusCode
}

export type UserMe201UserRole =
  (typeof UserMe201UserRole)[keyof typeof UserMe201UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UserMe201UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type UserMe201User = {
  accountStatus: boolean
  /** @nullable */
  avatar: string | null
  calendarAuthorized: boolean
  email: string
  id: string
  /** @nullable */
  name: string | null
  /** @nullable */
  nickName: string | null
  passwordResetRequired: boolean
  /** @nullable */
  phone: string | null
  role: UserMe201UserRole
}

/**
 * Dados do usuário recuperados com sucesso
 */
export type UserMe201 = {
  message: string
  user: UserMe201User
}

/**
 * Código de status HTTP 500.
 */
export type RefreshUserSession500StatusCode =
  (typeof RefreshUserSession500StatusCode)[keyof typeof RefreshUserSession500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession500Name =
  (typeof RefreshUserSession500Name)[keyof typeof RefreshUserSession500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type RefreshUserSession500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession500Name
  /** Código de status HTTP 500. */
  status_code: RefreshUserSession500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type RefreshUserSession403StatusCode =
  (typeof RefreshUserSession403StatusCode)[keyof typeof RefreshUserSession403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession403Name =
  (typeof RefreshUserSession403Name)[keyof typeof RefreshUserSession403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type RefreshUserSession403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession403Name
  /** Código de status HTTP 403. */
  status_code: RefreshUserSession403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type RefreshUserSession401StatusCode =
  (typeof RefreshUserSession401StatusCode)[keyof typeof RefreshUserSession401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession401Name =
  (typeof RefreshUserSession401Name)[keyof typeof RefreshUserSession401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type RefreshUserSession401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession401Name
  /** Código de status HTTP 401. */
  status_code: RefreshUserSession401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type RefreshUserSession400StatusCode =
  (typeof RefreshUserSession400StatusCode)[keyof typeof RefreshUserSession400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type RefreshUserSession400Name =
  (typeof RefreshUserSession400Name)[keyof typeof RefreshUserSession400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type RefreshUserSession400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: RefreshUserSession400Name
  /** Código de status HTTP 400. */
  status_code: RefreshUserSession400StatusCode
}

export type RefreshUserSession201UserRole =
  (typeof RefreshUserSession201UserRole)[keyof typeof RefreshUserSession201UserRole]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RefreshUserSession201UserRole = {
  admin: 'admin',
  user: 'user',
  dev: 'dev',
} as const

export type RefreshUserSession201User = {
  accountStatus: boolean
  /** @nullable */
  avatar: string | null
  calendarAuthorized: boolean
  email: string
  id: string
  /** @nullable */
  name: string | null
  /** @nullable */
  nickName: string | null
  passwordResetRequired: boolean
  /** @nullable */
  phone: string | null
  role: RefreshUserSession201UserRole
}

/**
 * Sessão atualizada com sucesso
 */
export type RefreshUserSession201 = {
  message: string
  sessionId: string
  user: RefreshUserSession201User
}

/**
 * Código de status HTTP 500.
 */
export type LogoutUser500StatusCode =
  (typeof LogoutUser500StatusCode)[keyof typeof LogoutUser500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser500Name =
  (typeof LogoutUser500Name)[keyof typeof LogoutUser500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type LogoutUser500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser500Name
  /** Código de status HTTP 500. */
  status_code: LogoutUser500StatusCode
}

/**
 * Código de status HTTP 403.
 */
export type LogoutUser403StatusCode =
  (typeof LogoutUser403StatusCode)[keyof typeof LogoutUser403StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser403StatusCode = {
  NUMBER_403: 403,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser403Name =
  (typeof LogoutUser403Name)[keyof typeof LogoutUser403Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser403Name = {
  ForbiddenError: 'ForbiddenError',
} as const

/**
 * Schema to represent Forbidden errors (HTTP 403)
 */
export type LogoutUser403 = {
  /** Ação recomendada para acessar o recurso. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando acesso proibido. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser403Name
  /** Código de status HTTP 403. */
  status_code: LogoutUser403StatusCode
}

/**
 * Código de status HTTP 401.
 */
export type LogoutUser401StatusCode =
  (typeof LogoutUser401StatusCode)[keyof typeof LogoutUser401StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser401StatusCode = {
  NUMBER_401: 401,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser401Name =
  (typeof LogoutUser401Name)[keyof typeof LogoutUser401Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser401Name = {
  UnauthorizedError: 'UnauthorizedError',
} as const

/**
 * Schema to represent Unauthorized errors (HTTP 401)
 */
export type LogoutUser401 = {
  /** Ação recomendada para a autenticação. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que o acesso não está autorizado. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser401Name
  /** Código de status HTTP 401. */
  status_code: LogoutUser401StatusCode
}

/**
 * Código de status HTTP 400.
 */
export type LogoutUser400StatusCode =
  (typeof LogoutUser400StatusCode)[keyof typeof LogoutUser400StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser400StatusCode = {
  NUMBER_400: 400,
} as const

/**
 * Nome fixo do erro.
 */
export type LogoutUser400Name =
  (typeof LogoutUser400Name)[keyof typeof LogoutUser400Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LogoutUser400Name = {
  BadRequestError: 'BadRequestError',
} as const

/**
 * Schema to represent Bad Request errors (HTTP 400)
 */
export type LogoutUser400 = {
  /** Ação recomendada para corrigir a requisição. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando que a requisição é inválida. */
  message: string
  /** Nome fixo do erro. */
  name: LogoutUser400Name
  /** Código de status HTTP 400. */
  status_code: LogoutUser400StatusCode
}

/**
 * Usuário desconectado com sucesso
 */
export type LogoutUser200 = {
  message: string
}

/**
 * Código de status HTTP 500.
 */
export type LoginUserGoogleCallback500StatusCode =
  (typeof LoginUserGoogleCallback500StatusCode)[keyof typeof LoginUserGoogleCallback500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type LoginUserGoogleCallback500Name =
  (typeof LoginUserGoogleCallback500Name)[keyof typeof LoginUserGoogleCallback500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type LoginUserGoogleCallback500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: LoginUserGoogleCallback500Name
  /** Código de status HTTP 500. */
  status_code: LoginUserGoogleCallback500StatusCode
}

/**
 * Código de status HTTP 422.
 */
export type LoginUserGoogleCallback422StatusCode =
  (typeof LoginUserGoogleCallback422StatusCode)[keyof typeof LoginUserGoogleCallback422StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback422StatusCode = {
  NUMBER_422: 422,
} as const

/**
 * Nome fixo do erro.
 */
export type LoginUserGoogleCallback422Name =
  (typeof LoginUserGoogleCallback422Name)[keyof typeof LoginUserGoogleCallback422Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback422Name = {
  UnprocessableEntityError: 'UnprocessableEntityError',
} as const

export type LoginUserGoogleCallback422DetailsItem = {
  /** Mensagem de validação para o campo. */
  message: string
  /** Caminho para o campo com erro. */
  path: string[]
}

/**
 * Schema to represent Unprocessable Entity errors (HTTP 422)
 */
export type LoginUserGoogleCallback422 = {
  /** Ação recomendada para corrigir os dados enviados. */
  action: string
  /** Detalhes adicionais sobre os erros de validação. */
  details?: LoginUserGoogleCallback422DetailsItem[]
  /** Mensagem indicando que a entidade não pôde ser processada. */
  message: string
  /** Nome fixo do erro. */
  name: LoginUserGoogleCallback422Name
  /** Código de status HTTP 422. */
  status_code: LoginUserGoogleCallback422StatusCode
}

/**
 * Redireciona para o frontend após configurar cookies de sessão
 * @nullable
 */
export type LoginUserGoogleCallback302 =
  | (typeof LoginUserGoogleCallback302)[keyof typeof LoginUserGoogleCallback302]
  | null

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogleCallback302 = {
  null: 'null',
} as const

export type LoginUserGoogleCallbackParams = {
  /**
   * Código de autorização do Google
   */
  code?: string
  /**
   * Escopos autorizados pelo usuário
   */
  scope?: string
  /**
   * Índice da conta Google usada na autenticação
   */
  authuser?: string
  /**
   * Tipo de prompt utilizado no login
   */
  prompt?: string
  /**
   * Valor usado para proteção contra CSRF
   */
  state?: string
}

/**
 * Código de status HTTP 500.
 */
export type LoginUserGoogle500StatusCode =
  (typeof LoginUserGoogle500StatusCode)[keyof typeof LoginUserGoogle500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogle500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type LoginUserGoogle500Name =
  (typeof LoginUserGoogle500Name)[keyof typeof LoginUserGoogle500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogle500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type LoginUserGoogle500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: LoginUserGoogle500Name
  /** Código de status HTTP 500. */
  status_code: LoginUserGoogle500StatusCode
}

/**
 * Redirecionamento para o Google OAuth
 * @nullable
 */
export type LoginUserGoogle302 =
  | (typeof LoginUserGoogle302)[keyof typeof LoginUserGoogle302]
  | null

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LoginUserGoogle302 = {
  null: 'null',
} as const

/**
 * Código de status HTTP 500.
 */
export type ServerHealthCheck500StatusCode =
  (typeof ServerHealthCheck500StatusCode)[keyof typeof ServerHealthCheck500StatusCode]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ServerHealthCheck500StatusCode = {
  NUMBER_500: 500,
} as const

/**
 * Nome fixo do erro.
 */
export type ServerHealthCheck500Name =
  (typeof ServerHealthCheck500Name)[keyof typeof ServerHealthCheck500Name]

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ServerHealthCheck500Name = {
  InternalServerError: 'InternalServerError',
} as const

/**
 * Schema to represent Internal Server errors (HTTP 500)
 */
export type ServerHealthCheck500 = {
  /** Ação recomendada para lidar com o erro interno. */
  action: string
  /** Detalhes adicionais sobre o erro. */
  details?: unknown
  /** Mensagem indicando um erro interno no servidor. */
  message: string
  /** Nome fixo do erro. */
  name: ServerHealthCheck500Name
  /** Código de status HTTP 500. */
  status_code: ServerHealthCheck500StatusCode
}

/**
 * Server health check
 */
export type ServerHealthCheck200 = {
  message: string
}
