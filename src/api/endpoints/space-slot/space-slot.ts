/**
 * Generated by orval v7.3.0 🍺
 * Do not edit manually.
 * BBZ App Backend API
 * API documentation for BBZ App Backend
 * OpenAPI spec version: 1.0.0
 */
import type { Arguments, Key, SWRConfiguration } from 'swr'
import useSwr from 'swr'
import type { SWRMutationConfiguration } from 'swr/mutation'
import useSWRMutation from 'swr/mutation'
import { customFetch } from '../../mutator/custom-fetch'
import type {
  CreateSpaceSlotPreReserve201,
  CreateSpaceSlotPreReserve400,
  CreateSpaceSlotPreReserve401,
  CreateSpaceSlotPreReserve403,
  CreateSpaceSlotPreReserve404,
  CreateSpaceSlotPreReserve409,
  CreateSpaceSlotPreReserve422,
  CreateSpaceSlotPreReserve500,
  CreateSpaceSlotPreReserveBody,
  DeleteSpaceSlotPreReserve200,
  DeleteSpaceSlotPreReserve400,
  DeleteSpaceSlotPreReserve401,
  DeleteSpaceSlotPreReserve403,
  DeleteSpaceSlotPreReserve404,
  DeleteSpaceSlotPreReserve422,
  DeleteSpaceSlotPreReserve500,
  GetSpaceSlotAvailability200,
  GetSpaceSlotAvailability400,
  GetSpaceSlotAvailability401,
  GetSpaceSlotAvailability403,
  GetSpaceSlotAvailability404,
  GetSpaceSlotAvailability422,
  GetSpaceSlotAvailability500,
  GetSpaceSlotAvailabilityParams,
  ListSpaceSlots200,
  ListSpaceSlots400,
  ListSpaceSlots401,
  ListSpaceSlots403,
  ListSpaceSlots422,
  ListSpaceSlots500,
  ListSpaceSlotsParams,
} from '../bBZAppBackendAPI.schemas'

type SecondParameter<T extends (...args: any) => any> = Parameters<T>[1]

/**
 * Este endpoint permite listar espaços disponíveis para uma data e, opcionalmente, um horário específico, com suporte a paginação.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Acessível a usuários com perfil 'admin', 'dev' ou 'user'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.
* **Regras de busca**:
  1. Quando a data é fornecida com hora zerada (00:00:00), retorna espaços com pelo menos um horário livre entre 07:00 e 20:00
  2. Quando data e hora específica são fornecidos, retorna espaços disponíveis nesse horário específico
  3. Apenas espaços ativos (is_active = true) são considerados
  4. Espaços pré-reservados ou reservados para o horário solicitado são excluídos dos resultados

* **Parâmetros**:
  - datetime (obrigatório): String ISO com timezone para data/hora
  - page (opcional): Número da página para paginação, começando em 1 (padrão: 1)
  - pageSize (opcional): Quantidade de resultados por página, entre 1 e 100 (padrão: 12)
  - type (opcional): Tipo de espaço, 'room' para salas ou 'workstation' para estações de trabalho (padrão: 'room')

* **Exemplo de uso**:
  - Consulta por data: `GET /v1/private/space-slot/list?datetime=2025-05-22T00:00:00-03:00`
  - Consulta por horário específico: `GET /v1/private/space-slot/list?datetime=2025-05-22T14:30:00-03:00`
  - Com paginação: `GET /v1/private/space-slot/list?datetime=2025-05-22T00:00:00-03:00&page=2&pageSize=10`
  - Filtro por tipo: `GET /v1/private/space-slot/list?datetime=2025-05-22T00:00:00-03:00&type=workstation`

* **Formato da resposta**:
  - spaces: Array de objetos representando os espaços disponíveis com suas propriedades
  - totalCount: Número total de espaços disponíveis
  - totalPages: Número total de páginas disponíveis
  - currentPage: Página atual sendo exibida
  - message: Mensagem de sucesso ou informação adicional

* **Notas**:
  - O parâmetro datetime deve ser uma string ISO com timezone (exemplo: "2025-05-22T00:00:00-03:00")
  - Se a hora for 00:00:00, o sistema busca espaços com pelo menos um horário livre no dia
  - Se a hora for diferente de 00:00:00 (ex: 14:30:00), o sistema busca espaços livres especificamente nesse horário
  - A duração padrão de cada slot é de 1 hora
  - A capacidade máxima de um espaço pode afetar sua disponibilidade
 * @summary Listar espaços disponíveis por data ou horário específico
 */
export type listSpaceSlotsResponse = {
  data: ListSpaceSlots200
  status: number
  headers: Headers
}

export const getListSpaceSlotsUrl = (params: ListSpaceSlotsParams) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  return normalizedParams.size
    ? `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/list?${normalizedParams.toString()}`
    : `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/list`
}

export const listSpaceSlots = async (
  params: ListSpaceSlotsParams,
  options?: RequestInit,
): Promise<listSpaceSlotsResponse> => {
  return customFetch<Promise<listSpaceSlotsResponse>>(
    getListSpaceSlotsUrl(params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getListSpaceSlotsKey = (params: ListSpaceSlotsParams) =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/list`,
    ...(params ? [params] : []),
  ] as const

export type ListSpaceSlotsQueryResult = NonNullable<
  Awaited<ReturnType<typeof listSpaceSlots>>
>
export type ListSpaceSlotsQueryError =
  | ListSpaceSlots400
  | ListSpaceSlots401
  | ListSpaceSlots403
  | ListSpaceSlots422
  | ListSpaceSlots500

/**
 * @summary Listar espaços disponíveis por data ou horário específico
 */
export const useListSpaceSlots = <
  TError =
    | ListSpaceSlots400
    | ListSpaceSlots401
    | ListSpaceSlots403
    | ListSpaceSlots422
    | ListSpaceSlots500,
>(
  params: ListSpaceSlotsParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof listSpaceSlots>>,
      TError
    > & { swrKey?: Key; enabled?: boolean }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const isEnabled = swrOptions?.enabled !== false
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getListSpaceSlotsKey(params) : null))
  const swrFn = () => listSpaceSlots(params, requestOptions)

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  )

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint retorna informações detalhadas sobre a disponibilidade de um espaço específico em um intervalo de datas, mostrando slots ocupados (reservados ou pré-reservados).

* **Segurança**: Requer autenticação via JWT.
* **Autorização**: Acessível a usuários com conta ativa.

* **Funcionalidade**:
  1. Retorna detalhes completos do espaço solicitado (ID, nome, descrição, recursos, etc.)
  2. Lista todos os slots ocupados (reservados ou pré-reservados) no período informado
  3. Para cada slot ocupado, fornece informações do usuário responsável pela reserva
  4. Diferencia entre slots com status 'reserved' (confirmados) e 'pre_reserved' (temporários)

* **Parâmetros**:
  - spaceId (obrigatório, path): Identificador UUID do espaço
  - startDate (obrigatório, query): Data inicial no formato YYYY-MM-DD
  - endDate (obrigatório, query): Data final no formato YYYY-MM-DD

* **Exemplo de uso**:
  - Consultar período: `GET /v1/private/space-slot/a1b2c3d4-e5f6-7890-abcd-1234567890ab/availability?startDate=2023-08-15&endDate=2023-08-20`
  - Consultar único dia: `GET /v1/private/space-slot/a1b2c3d4-e5f6-7890-abcd-1234567890ab/availability?startDate=2023-08-15&endDate=2023-08-15`

* **Formato da resposta**:
  - space: Objeto com informações do espaço (id, nome, recursos, capacidade, etc.)
  - slots: Array de slots ocupados no período, contendo:
    - id: Identificador UUID do slot
    - date: Data do slot (YYYY-MM-DD)
    - time: Horário (HH:MM formato 24h)
    - status: Estado ('reserved' ou 'pre_reserved')
    - preReservedBy: Usuário que fez a reserva (id, nome, email)
    - preReservedUntil: Data e hora de expiração da pré-reserva

* **Informações adicionais**:
  - As datas no formato YYYY-MM-DD são convertidas para o fuso horário adequado no processamento
  - São retornados apenas os slots que já estão ocupados (não mostra horários disponíveis)
  - Um slot em estado 'pre_reserved' tem prazo de expiração e pode se tornar disponível novamente se não for confirmado

* **Limites de consulta por tipo de espaço**:
  - **Salas de reunião (room)**: Período máximo de 7 dias corridos
  - **Estações de trabalho (workstation)**: Período até o sábado da próxima semana (considerando semana de domingo a sábado)
  - Exemplo para workstation: Se hoje é segunda (20/01), pode consultar até sábado (31/01)
 * @summary Visualizar disponibilidade detalhada de um espaço específico em um período
 */
export type getSpaceSlotAvailabilityResponse = {
  data: GetSpaceSlotAvailability200
  status: number
  headers: Headers
}

export const getGetSpaceSlotAvailabilityUrl = (
  spaceId: string,
  params: GetSpaceSlotAvailabilityParams,
) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  })

  return normalizedParams.size
    ? `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/${spaceId}/availability?${normalizedParams.toString()}`
    : `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/${spaceId}/availability`
}

export const getSpaceSlotAvailability = async (
  spaceId: string,
  params: GetSpaceSlotAvailabilityParams,
  options?: RequestInit,
): Promise<getSpaceSlotAvailabilityResponse> => {
  return customFetch<Promise<getSpaceSlotAvailabilityResponse>>(
    getGetSpaceSlotAvailabilityUrl(spaceId, params),
    {
      ...options,
      method: 'GET',
    },
  )
}

export const getGetSpaceSlotAvailabilityKey = (
  spaceId: string,
  params: GetSpaceSlotAvailabilityParams,
) =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/${spaceId}/availability`,
    ...(params ? [params] : []),
  ] as const

export type GetSpaceSlotAvailabilityQueryResult = NonNullable<
  Awaited<ReturnType<typeof getSpaceSlotAvailability>>
>
export type GetSpaceSlotAvailabilityQueryError =
  | GetSpaceSlotAvailability400
  | GetSpaceSlotAvailability401
  | GetSpaceSlotAvailability403
  | GetSpaceSlotAvailability404
  | GetSpaceSlotAvailability422
  | GetSpaceSlotAvailability500

/**
 * @summary Visualizar disponibilidade detalhada de um espaço específico em um período
 */
export const useGetSpaceSlotAvailability = <
  TError =
    | GetSpaceSlotAvailability400
    | GetSpaceSlotAvailability401
    | GetSpaceSlotAvailability403
    | GetSpaceSlotAvailability404
    | GetSpaceSlotAvailability422
    | GetSpaceSlotAvailability500,
>(
  spaceId: string,
  params: GetSpaceSlotAvailabilityParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof getSpaceSlotAvailability>>,
      TError
    > & { swrKey?: Key; enabled?: boolean }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const isEnabled = swrOptions?.enabled !== false && !!spaceId
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getGetSpaceSlotAvailabilityKey(spaceId, params) : null))
  const swrFn = () => getSpaceSlotAvailability(spaceId, params, requestOptions)

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  )

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite que um usuário crie uma pré-reserva de um slot (horário) em um espaço específico.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Acessível a usuários com perfil 'admin', 'dev' ou 'user'.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.

* **Funcionalidade**:
  1. Cria uma pré-reserva para um slot (combinação de espaço e horário específico)
  2. Define o status como 'pre_reserved'
  3. Associa o usuário atual à pré-reserva
  4. Define um tempo limite de 5 minutos para confirmação da reserva
  5. Verifica automaticamente se o espaço existe e está ativo
  6. Impede pré-reservas de slots já reservados ou pré-reservados
  7. Impede pré-reservas para datas passadas

* **Fluxo de reserva**:
  1. O usuário faz uma pré-reserva (este endpoint)
  2. O sistema reserva o slot por 5 minutos para o usuário
  3. O usuário deve confirmar a reserva em até 5 minutos
  4. Caso contrário, o slot fica disponível novamente após o tempo limite

* **Parâmetros no corpo**:
  - spaceId (obrigatório): Identificador UUID do espaço
  - slotStart (obrigatório): Data e hora de início no formato ISO com timezone (ex: 2023-08-15T14:30:00-03:00)
  - slotEnd (obrigatório): Data e hora de término no formato ISO com timezone (ex: 2023-08-15T15:30:00-03:00)
  - status (opcional): Status da reserva, padrão 'pre_reserved'

* **Exemplo de uso**:
  - Requisição básica: `POST /v1/private/space-slot/pre-reserve` com body:
  ```json
  {
    "spaceId": "a1b2c3d4-e5f6-7890-abcd-1234567890ab",
    "slotStart": "2023-08-15T14:30:00-03:00",
    "slotEnd": "2023-08-15T15:30:00-03:00"
  }
  ```

* **Formato da resposta**:
  - slot: Objeto com todas as informações do slot pré-reservado
  - message: Mensagem informativa de sucesso

* **Notas**:
  - As datas devem estar no formato ISO com informação de timezone
  - O slot reservado tem duração de 1 hora para salas de reunião ou 5 horas para workstations
  - Uma pré-reserva expira automaticamente após 5 minutos se não for confirmada
  - Um usuário não pode pré-reservar um slot já reservado ou pré-reservado
  - Não é possível pré-reservar slots para datas passadas
  - O ID do usuário que faz a pré-reserva é automaticamente capturado do token JWT

* **Regras específicas para Workstations**:
  - Workstations só podem ser reservadas até o sábado da próxima semana (considerando semana de domingo a sábado)
  - Reservas de workstations não podem ser feitas às sextas-feiras (bloqueio de dia da semana)
  - Duração do slot: 5 horas (das 8h às 13h, por exemplo)
  - Exemplo: Se hoje é segunda-feira (20/01), pode-se reservar workstations até sábado (31/01)
 * @summary Criar pré-reserva de slot em um espaço
 */
export type createSpaceSlotPreReserveResponse = {
  data: CreateSpaceSlotPreReserve201
  status: number
  headers: Headers
}

export const getCreateSpaceSlotPreReserveUrl = () => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/pre-reserve`
}

export const createSpaceSlotPreReserve = async (
  createSpaceSlotPreReserveBody: CreateSpaceSlotPreReserveBody,
  options?: RequestInit,
): Promise<createSpaceSlotPreReserveResponse> => {
  return customFetch<Promise<createSpaceSlotPreReserveResponse>>(
    getCreateSpaceSlotPreReserveUrl(),
    {
      ...options,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', ...options?.headers },
      body: JSON.stringify(createSpaceSlotPreReserveBody),
    },
  )
}

export const getCreateSpaceSlotPreReserveMutationFetcher = (
  options?: SecondParameter<typeof customFetch>,
) => {
  return (
    _: Key,
    { arg }: { arg: CreateSpaceSlotPreReserveBody },
  ): Promise<createSpaceSlotPreReserveResponse> => {
    return createSpaceSlotPreReserve(arg, options)
  }
}
export const getCreateSpaceSlotPreReserveMutationKey = () =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/pre-reserve`,
  ] as const

export type CreateSpaceSlotPreReserveMutationResult = NonNullable<
  Awaited<ReturnType<typeof createSpaceSlotPreReserve>>
>
export type CreateSpaceSlotPreReserveMutationError =
  | CreateSpaceSlotPreReserve400
  | CreateSpaceSlotPreReserve401
  | CreateSpaceSlotPreReserve403
  | CreateSpaceSlotPreReserve404
  | CreateSpaceSlotPreReserve409
  | CreateSpaceSlotPreReserve422
  | CreateSpaceSlotPreReserve500

/**
 * @summary Criar pré-reserva de slot em um espaço
 */
export const useCreateSpaceSlotPreReserve = <
  TError =
    | CreateSpaceSlotPreReserve400
    | CreateSpaceSlotPreReserve401
    | CreateSpaceSlotPreReserve403
    | CreateSpaceSlotPreReserve404
    | CreateSpaceSlotPreReserve409
    | CreateSpaceSlotPreReserve422
    | CreateSpaceSlotPreReserve500,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof createSpaceSlotPreReserve>>,
    TError,
    Key,
    CreateSpaceSlotPreReserveBody,
    Awaited<ReturnType<typeof createSpaceSlotPreReserve>>
  > & { swrKey?: string }
  request?: SecondParameter<typeof customFetch>
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey = swrOptions?.swrKey ?? getCreateSpaceSlotPreReserveMutationKey()
  const swrFn = getCreateSpaceSlotPreReserveMutationFetcher(requestOptions)

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
/**
 * Este endpoint permite que um usuário delete uma pré-reserva de um slot (horário) em um espaço específico.

* **Segurança**: Protegido por autenticação JWT (token de sessão) e CSRF via cookie/header.
* **Autorização**: Acessível a qualquer usuário autenticado, com verificação de propriedade ou privilégio administrativo.
* **Validação de conta**: Verifica se a conta do usuário autenticado está ativa e não requer reset de senha.

* **Funcionalidade**:
  1. Deleta uma pré-reserva existente para um slot (horário em um espaço)
  2. Remove completamente o registro do slot do banco de dados
  3. Libera o horário para que outros usuários possam reservá-lo
  4. Apenas o usuário que fez a pré-reserva ou administradores podem deletá-la

* **Fluxo de deleção**:
  1. O usuário solicita a deleção de uma pré-reserva específica através do ID do slot
  2. O sistema extrai automaticamente o ID e perfil do usuário do token JWT
  3. O sistema verifica se o slot existe e está no estado 'pre_reserved'
  4. O sistema verifica se o usuário tem permissão para deletar (é o dono da reserva ou tem perfil admin/dev)
  5. O sistema remove o registro da pré-reserva e libera o horário

* **Parâmetros na rota**:
  - slotId (obrigatório): Identificador UUID do slot pré-reservado

* **Exemplo de uso**:
  - Requisição básica: `DELETE /v1/private/space-slot/a1b2c3d4-e5f6-7890-abcd-1234567890ab/pre-reserve`

* **Formato da resposta**:
  - message: Mensagem informativa de sucesso
  - deletedSlotId: UUID do slot que foi deletado

* **Notas**:
  - A deleção é definitiva e não pode ser desfeita
  - Apenas o proprietário da pré-reserva ou usuários com perfil 'admin' ou 'dev' podem deletar
  - Caso o slot já tenha sido completamente reservado (status 'reserved'), este endpoint não funcionará
  - Caso o slot não exista ou já tenha sido deletado, retorna erro 404 (não encontrado)
  - A operação é idempotente (chamar duas vezes não causa erro, mas retorna 404 na segunda vez)
 * @summary Deletar pré-reserva de slot em um espaço
 */
export type deleteSpaceSlotPreReserveResponse = {
  data: DeleteSpaceSlotPreReserve200
  status: number
  headers: Headers
}

export const getDeleteSpaceSlotPreReserveUrl = (slotId: string) => {
  return `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/${slotId}/pre-reserve`
}

export const deleteSpaceSlotPreReserve = async (
  slotId: string,
  options?: RequestInit,
): Promise<deleteSpaceSlotPreReserveResponse> => {
  return customFetch<Promise<deleteSpaceSlotPreReserveResponse>>(
    getDeleteSpaceSlotPreReserveUrl(slotId),
    {
      ...options,
      method: 'DELETE',
    },
  )
}

export const getDeleteSpaceSlotPreReserveMutationFetcher = (
  slotId: string,
  options?: SecondParameter<typeof customFetch>,
) => {
  return (
    _: Key,
    __: { arg: Arguments },
  ): Promise<deleteSpaceSlotPreReserveResponse> => {
    return deleteSpaceSlotPreReserve(slotId, options)
  }
}
export const getDeleteSpaceSlotPreReserveMutationKey = (slotId: string) =>
  [
    `${process.env.NEXT_PUBLIC_API_URL}/v1/private/space-slot/${slotId}/pre-reserve`,
  ] as const

export type DeleteSpaceSlotPreReserveMutationResult = NonNullable<
  Awaited<ReturnType<typeof deleteSpaceSlotPreReserve>>
>
export type DeleteSpaceSlotPreReserveMutationError =
  | DeleteSpaceSlotPreReserve400
  | DeleteSpaceSlotPreReserve401
  | DeleteSpaceSlotPreReserve403
  | DeleteSpaceSlotPreReserve404
  | DeleteSpaceSlotPreReserve422
  | DeleteSpaceSlotPreReserve500

/**
 * @summary Deletar pré-reserva de slot em um espaço
 */
export const useDeleteSpaceSlotPreReserve = <
  TError =
    | DeleteSpaceSlotPreReserve400
    | DeleteSpaceSlotPreReserve401
    | DeleteSpaceSlotPreReserve403
    | DeleteSpaceSlotPreReserve404
    | DeleteSpaceSlotPreReserve422
    | DeleteSpaceSlotPreReserve500,
>(
  slotId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof deleteSpaceSlotPreReserve>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof deleteSpaceSlotPreReserve>>
    > & { swrKey?: string }
    request?: SecondParameter<typeof customFetch>
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {}

  const swrKey =
    swrOptions?.swrKey ?? getDeleteSpaceSlotPreReserveMutationKey(slotId)
  const swrFn = getDeleteSpaceSlotPreReserveMutationFetcher(
    slotId,
    requestOptions,
  )

  const query = useSWRMutation(swrKey, swrFn, swrOptions)

  return {
    swrKey,
    ...query,
  }
}
